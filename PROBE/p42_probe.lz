struct Multiplier =
    factor: int

impl Multiplier =
    def __call__(ref self, x: int) -> int =
        return self.factor * x

def main() =
    mul3 = Multiplier(factor: 3)
    print(mul3(5))
    print(mul3(10))
