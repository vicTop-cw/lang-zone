struct MyConfig =
    width: int
    height: int
    name: str

impl MyConfig =
    def __buildparams__(ref self) -> (int, int, str) =
        return (self.width, self.height, self.name)

def main() =
    cfg = MyConfig(width: 1920, height: 1080, name: "screen")
    print(cfg.__buildparams__())
