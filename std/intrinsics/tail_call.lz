// ============================================================
// @tail_call — 尾递归优化保证
//
// 语法：
//   @tail_call
//   def func_name(param1: Type1, ..., acc: Accumulator) -> ReturnType =
//       if base_condition:
//           base_result
//       else:
//           func_name(..., recursive_call)  // 必须在尾位置
//
// 编译期检查：
//   1. 函数最后一条语句（或所有分支的最后一条）必须是递归调用
//   2. 递归调用的结果必须直接返回（不能有额外计算）
//   3. 循环或 match 末端分支也必须是尾递归
//   4. 不符合则报编译错误
//
// 注意：
//   Rust 不保证 TCO（尾调用优化）。
//   @tail_call 的主要价值是编译期验证——确保代码结构是尾递归的，
//   以便未来 Rust 编译器或 LLVM 能执行 TCO。
//   在 LLVM -O2+ 下，简单尾递归通常会被优化为循环。
// ============================================================
