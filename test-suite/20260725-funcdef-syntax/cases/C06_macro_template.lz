#!bin macro
// 仓颉宏模版（macro template）：def name!<T>(...) -> Tokens 在编译期展开
def make_const!<T>(name: str, value: int) -> Tokens =
    f```
        def $(name)() -> $(T) = $(value)
    ```

make_const!<int>("answer", 42)

def get_answer() -> int = answer()
