struct Person =
    name: str

trait Greet = def greet(self: Self) -> str

impl Greet for Person:
    def greet(self: Person) -> str = f"Hi, {self.name}"

def main() =
    p = Person(name: "Tom")
    print(p.greet())
