struct Counter =
    val: int

magic __iter__(self: Counter) -> Counter = self
magic __next__(self: mut Counter) -> int =
    if self.val > 0:
        self.val = self.val - 1
    else:
        None

def main() =
    c = Counter(val: 3)
    for x in c:
        print(x)
