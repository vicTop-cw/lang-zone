struct Vec2 =
    x: int
    y: int

magic __add__(self: Vec2, other: Vec2) -> Vec2 = Vec2(x: self.x + other.x, y: self.y + other.y)

def main() =
    a = Vec2(x: 10, y: 20)
    b = Vec2(x: 20, y: 20)
    c = a + b
    print(c.x)
    print(c.y)
