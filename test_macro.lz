// 宏系统 E2E 测试
// 定义一个简单的标识宏，验证编译流程

macro id(input: Tokens) -> Tokens =
    input

macro twice(input: Tokens) -> Tokens =
    ```
        input
    ```

def main() =
    print("Hello from lz macro test!")
