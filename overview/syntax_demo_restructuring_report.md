# SYNTAX 规范 + DEMO 重构报告

> 日期: 2026-07-27 | 基于 bug 报告 `josn_parse/lz/bug/20260727_不合理与不完善之处报告.md`

---

## 完成内容

### 1. SYNTAX/ 权威语法规范（新建）

12 份文档，覆盖 LZ 语言全部语法：

| 编号 | 文档 | 行数（估） |
|:---:|------|:---:|
| README | 索引与概览 | — |
| 00 | 词法基础 | ~200 |
| 01 | 类型系统 | ~180 |
| 02 | 变量与绑定 | ~140 |
| 03 | 函数 | ~250 |
| 04 | 表达式 | ~300 |
| 05 | 控制流 | ~270 |
| 06 | 数据结构 | ~260 |
| 07 | 模块与导入 | ~150 |
| 08 | 宏与编译期 | ~140 |
| 09 | 错误处理 | ~160 |
| 10 | 并发与异步 | ~120 |

每篇包含：
- 正确语法（代码示例）
- 错误语法边界（❌ 标记）
- 交叉引用链接

### 2. DEMO/ 演示文件（重写）

- 52 个 .lz 文件（从 46 个扩展）
- 所有 .rs 文件已清理（0 残留）
- 新增 6 个缺失演示：closure.lz、sum_prod.lz、result.lz、operators_bitwise.lz、import_demo.lz、gen_block.lz

### 3. 语法统一修正

所有文件符合新规范：
- `::` → `.`（路径分隔符）
- `enum Name:` 用 `:`（struct 用 `=`）
- `guard else:` 必须换行缩进
- `case pat => val` 内联用 `=>`；多行用 `:`
- `comptime:` 块内只允许 `const`
- 类型名：`int` `str` `f64` `bool` `()` `Option<T>` `Result<T,E>`

---

## 与 bug 报告的对应

| Bug 报告问题 | 对应处理 |
|-------------|---------|
| 1.1 冒号换行不一致 | ✅ 规范统一：for/while/guard 强制换行 |
| 1.2 文档与实现不符 | ✅ 新规范基于源码行为 |
| 1.3 `::` 未彻底移除 | ✅ 规范中 `::` = 词法错误 |
| 1.4 `let mut` 语义混乱 | ✅ 规范明确：`let`=不可变，裸名=可变，`mut`=no-op |
| 6.4 文档分散 | ✅ 统一到 SYNTAX/ + DEMO/ |
