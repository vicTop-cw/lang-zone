struct Parser =
    input: str
    pos: int

    def try_len(self: Parser)-> bool =
        p = self.pos + 1
        p < self.input.len()

def main() =
    p = Parser("hello", 0)
    print(p.try_len())
