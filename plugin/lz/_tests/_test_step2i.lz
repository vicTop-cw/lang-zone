enum JsonValue =
    Null
    Bool(bool)
    Number(f64)
    String(str)
    Array(List<JsonValue>)
    Object(Dict<str, JsonValue>)

def parse_true()-> (int, JsonValue?) =
    return (0, Some(JsonValue.Bool(True)))

def main() =
    print("ok")