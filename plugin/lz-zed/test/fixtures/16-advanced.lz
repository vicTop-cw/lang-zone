// lz-zed test fixture: 16-advanced — 表达式进阶
// Authority: SYNTAX/04-表达式.md + 12-操作符.md §二

# 管道
let r = data |> process
let r2 = [1, 2, 3]
    |> map(|x| x * 2)
    |> filter(|x| x > 3)
    |> collect()

# 海象
if (n := len(xs)) > 0:
    print(n)

count = 0
while (n := next()) < 10:
    print(n)

# 区间与推导式
let half = 0..5
let full = 0..=5

let squares = [x * x for x in 1..10]
let evens_list = [x for x in xs if x % 2 == 0]
let d = {k: v for (k, v) in pairs}

# 字典 / 集合 / 列表 / 元组
let dict = {"a": 1, "b": 2}
let empty_dict = {}
let set = {1, 2, 3}
let list = [1, 2, 3]
let tuple = (1, "hi")
let nested = [[1, 2], [3, 4]]

# 空值合并链
let cfg = config.get("host") ?? config.get("addr") ?? "localhost"

# 内建函数：容器/迭代/格式化
let has = contains(xs, 1)
let it = iter(xs)
let zipped = zip(a, b)
let sorted = sort(xs)
let rev = reverse(xs)
let copy = clone(x)
let fm = format("x={}", 1)
let h = hash(obj)
let cb = callable(f)
let gone = drop(tmp)

# 安全导航链
let city = user?.address?.city

# 比较链（语法糖 a < b < c）
let between = 1 < n < 10

# 优先级示例
let pr1 = a + b * c
let pr2 = (a + b) * c
let pr3 = -2 ** 2
let pr4 = (-2) ** 2

# 带值循环表达式
let answer = loop:
    for i in 0..10:
        if i == 7:
            break with i

# 逻辑与位运算混合
let mask = (a & b) == 0
let cond = not a or (b and c)
