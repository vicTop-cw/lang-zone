# ============================================================
# Round 4 综合重测: 装饰器、Lambda 与杂项
# ============================================================

# Bug-R4-1: @decorator 装饰器语法不支持
# @decorator  # 取消注释测试 Bug-R4-1
# def decorated() -> int:
#     return 42

# Bug-R4-2: test 关键字未注册到作用域
# test def test_simple() -> int:  # 取消注释测试 Bug-R4-2
#     return 42

# Bug-R4-3: in 运算符在 if 条件中不支持
# def test_in() -> int:
#     if "a" in "abc":  # 取消注释测试 Bug-R4-3
#         return 1
#     return 0

# Bug-R4-4: is 运算符在 if 条件中不支持
# def test_is() -> int:
#     let x: int = 1
#     if x is 1:  # 取消注释测试 Bug-R4-4
#         return 1
#     return 0

# Bug-R4-5: not 关键字在 if 条件中不支持
# def test_not() -> int:
#     if not 1 == 2:  # 取消注释测试 Bug-R4-5
#         return 1
#     return 0

# Bug-R4-6: del 关键字未定义
# def test_del() -> int:
#     let x: int = 1
#     del x  # 取消注释测试 Bug-R4-6
#     return 0

# Bug-R4-7: with 语句在代码生成阶段丢失
# def test_with() -> int:
#     with open("test.txt", "r") as f:  # 取消注释测试 Bug-R4-7
#         return 0
#     return 0

# Bug-R4-8: None 不能赋值给 int 类型
# def test_none_int() -> int:
#     let x: int = None  # 取消注释测试 Bug-R4-8
#     return 0

def main() -> int:
    return 0