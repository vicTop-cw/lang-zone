# ============================================================
# Round 3 综合重测: 指针、泛型与并发
# ============================================================

# Bug-R3-3: malloc 参数应为 sizeof() 而非数值
def test_malloc() -> int:
    let p: int* = malloc(sizeof(int))
    return 0

# Bug-R3-4: 指针算术返回类型错误
def test_ptr_arith() -> int:
    let p: int* = malloc(sizeof(int))
    # let q: int* = p + 1  # 取消注释测试 Bug-R3-4
    return 0

# Bug-R3-5: 泛型类型参数 T 未注册到作用域
# def identity[T](x: T) -> T:  # 取消注释测试 Bug-R3-5
#     return x

# Bug-R3-6: spawn 语句在代码生成阶段丢失
def test_spawn() -> int:
    spawn print("spawned")
    return 0

# Bug-R3-7: go 语句在代码生成阶段丢失
def test_go() -> int:
    go print("go_routine")
    return 0

# Bug-R3-8: nogil 块语法不支持
# nogil:  # 取消注释测试 Bug-R3-8
#     let x: int = 1 + 1

def main() -> int:
    return 0