# ============================================================
# Bug 复测: Round 2 - 生成器与模块系统
# ============================================================

# Bug-R2-1: yield 语句在代码生成阶段全部丢失
def simple_gen() -> int:
    yield 1
    yield 2
    yield 3
    return 0

# Bug-R2-2: yield from 语法不支持
# def delegating_gen() -> int:
#     yield from simple_gen()  # 如果报错则 Bug-R2-2 仍存在
#     return 0

# Bug-R2-3: f-string 在代码生成中丢失引号类型
def test_fstring() -> str:
    let s: Simple = Simple {value: 42}
    return f"Simple.x = {s.value}"

# 正常特性验证
import os
from sys import path