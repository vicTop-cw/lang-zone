def test_0() -> int:
    x = z
    defer:
        let cleanup: int = 0
    let x: int = 100
    guard z else:
        return -1
    defer:
        let cleanup: int = 0

def test_1() -> int:
    return total
    total
    guard 0 else:
        return -1
    defer:
        let cleanup: int = 0
    guard print("a") else:
        return -1

def main() -> int:
    return 0