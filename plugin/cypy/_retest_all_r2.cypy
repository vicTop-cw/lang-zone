# ============================================================
# Round 2 综合重测: 生成器与模块系统
# ============================================================

# Bug-R2-1: yield 语句在代码生成阶段全部丢失
def simple_gen() -> int:
    yield 1
    yield 2
    yield 3

# Bug-R2-2: yield from 语法不支持
# def sub_gen() -> int:
#     yield 10
# 
# def delegating_gen() -> int:
#     yield from sub_gen()  # 取消注释测试 Bug-R2-2

# Bug-R2-3: f-string 在代码生成中丢失引号类型
def test_fstring() -> str:
    let s: str = "hello"
    return f"value = {s}"

def main() -> int:
    return 0