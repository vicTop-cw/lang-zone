# Bug-R5-6: case other: 变量绑定模式不支持
def test_match_bind(x: int) -> int:
    match x:
        case 0:
            return 0
        case other:
            return other
        case _:
            return -1

def main() -> int:
    return 0