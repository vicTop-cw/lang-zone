// ============================================================
// error_method_not_found_put.lz — 负向测试用例（ERROR_BUG）
// 所属：lib_hashmap | 期望检查点：调用不存在的 put/get 方法，应报方法未定义错误
// 级别：L2
//   L1 = 完全漏放行（lang-zone 放行且生成的 Rust 也通过，最严重）
//   L2 = 前端漏检（lang-zone 放行，生成的 Rust 被 rustc 拒绝）
// 实际：lang-zone 放行（EXIT 0）| rustc 拒绝 E0308,E0599
// 验证：lang-zone <本文件> 当前返回 0；修复后应返回非 0 并报错
// ============================================================
def main() =
    let m = {}
    m.put(1, "one")
    m.put(2, "two")
    let v = m.get(1)
    print(v)
