# 规范文档待更新清单

> 基于全面审查发现的文档问题，按文件排列

---

## 一、06a-struct.md

### 1.1 L262 — 布尔字面量大小写

```
- let p2 = Pair(first: true, second: 3.14)       // Pair<bool, f64>
+ let p2 = Pair(first: True, second: 3.14)       // Pair<bool, f64>
```

**原因**：`true` 是小写，但 00-词法基础.md 规定布尔字面量为 `True`/`False`（首字母大写）。

> ✅ **已修复**（本次审查中已修改）

---

## 二、03d-可变参数.md

### 2.1 L79-95 — `dict`/`Dict` 大小写不统一

表格用大写 `Dict<K, V>`（L65），代码示例用小写 `dict<K, V>`（L79-80, L85, L95）。

**修改方式**：将所有 `dict<...>` 统一为大写 `Dict<...>`（L79, L80, L85, L95 共 4 处）。

> ✅ **已修复**（本次审查中已修改）

---

## 三、08b-comptime编译期.md

### 3.1 §4.2（L153-167）— "不支持"列表与实际示例矛盾

**问题**：§4.2 列出"方法调用 `obj.method()`"和"索引 `arr[0]`"为**不支持**，但规范自身示例大量使用：
- L177: `table.push(i * i)` ← 方法调用
- L115: `table.push(i * i)` ← 方法调用
- L238: `config.contains("database")` ← 方法调用
- L239: `config["database"]` ← 索引
- L260: `members.len()` ← 方法调用

**修改方式**：
1. 从"不支持"列表中删除"方法调用"和"索引"两项
2. 将注释放宽为"方法调用和索引在 comptime 块内创建的局部变量上可用"
3. 同步更新 L167 的注意说明

> ✅ **已修复**（本次审查中已修改）

### 3.2 L182 — comptime def 调用规则与示例不一致

**问题**：规则说"只能在 `comptime:` 块或 `const X = comptime expr` 中被调用"，但示例 L178 直接用 `const LOOKUP = build_table(256)`（无 `comptime` 关键字）。

**修改方式**：将规则改为"只能在 `comptime:` 块或 `const` 初始化表达式中调用（`const` 上下文隐式为编译期）"。

> ✅ **已修复**（本次审查中已修改）

---

## 四、DEMO/08_modules/import_demo.lz

### 4.1 §5 — 误出现 `lazy import`（非 LZ 关键字）

**问题**：import_demo.lz 的注释中出现了 `lazy import` / `lazy from`，但 00-词法基础.md §1.6 中 `lazy` **不是关键字**，规范中也不存在延迟导入的语法。

**修改方式**：删除 import_demo.lz 中 `lazy import` 和 `lazy from` 的注释行。

> ❌ **待修复**（本次未修改）

---

## 五、12-操作符.md

### 5.1 §二 优先级表（L315-333）— `??` 缺少优先级

**问题**：`??`（空值合并）在 §1.13 中定义并给出代码示例，但优先级表（L315-333）中完全没有 `??` 的行。读者无法知道 `??` 的优先级。

**修改方式**：在优先级表中新增一行。`??` 语义上类似 `or` 的短路行为，建议放在优先级 3-4 附近或与 `?.` 同组：

```
| 3 | `??` | 左 | 空值合并 |
```

建议位置：紧接在 `or`/`||` 行之后（优先级 3）。

> ❌ **待修复**（本次未修改）

---

## 六、00-词法基础.md

### 6.1 §6.2（L305-330）— 冒号规则与 `test` 单行写法可能冲突

**问题**：§6.2 规定"冒号后必须换行 + 缩进"的场景包括 if/for/while/guard/case。但 15-测试框架.md 展示了 `test "add": assert 1 + 1 == 2` 的单行写法，这与通用冒号规则冲突。

**修改方式**：在 §6.2 末尾的冒号规则说明中补充一条：

```markdown
> **例外**：`test` 关键字的冒号后支持单行表达式（`test "name": expr`），属于测试框架的特有语法。
```

> ❌ **待修复**（本次未修改）

---

## 七、05-控制流.md

### 7.1 §6.1 — 嵌套循环中 `break` 作用域未定义

**问题**：规范只定义了单层 `break`，"仅支持跳出单层"。但 DEMO 中演示了 `loop` 内嵌套 `for` 并用 `break` 带值的场景，规范未说明嵌套时的作用域规则。

**修改方式**：在 §6.1 补充说明：

```markdown
> 嵌套循环中 `break`/`continue` 始终作用于**最内层**循环。如需跳出外层，需通过 flag 变量或 `return` 实现。
```

> ❌ **待修复**（本次未修改）

---

## 总结

| 序号 | 文件 | 问题 | 状态 |
|:---:|------|------|:----:|
| 1 | 06a-struct.md | `true` → `True` | ✅ 已修 |
| 2 | 03d-可变参数.md | `dict` → `Dict` 统一 | ✅ 已修 |
| 3 | 08b-comptime编译期.md | §4.2 不支持列表矛盾 | ✅ 已修 |
| 4 | 08b-comptime编译期.md | comptime def 规则矛盾 | ✅ 已修 |
| 5 | DEMO/08_modules/import_demo.lz | 误出现 `lazy import`（非关键字） | ❌ 待修 |
| 6 | 12-操作符.md | `??` 缺优先级 | ❌ 待修 |
| 7 | 00-词法基础.md | 冒号规则冲突 | ❌ 待修 |
| 8 | 05-控制流.md | break 嵌套未定义 | ❌ 待修 |
