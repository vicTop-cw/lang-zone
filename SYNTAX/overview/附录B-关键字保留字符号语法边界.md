# 附录 B — 关键字/保留字/符号/语法边界

> 版本: 3.2 · 2026-07-28

---

## 一、关键字全集

### 1.1 声明关键字

| 关键字 | 用途 | 可嵌套位置 |
|--------|------|-----------|
| `def` | 函数定义 | 顶层, struct/trait/impl 内, 函数内 |
| `struct` | 结构体定义 | 仅顶层 |
| `enum` | 枚举定义 | 仅顶层 |
| `trait` | Trait 定义 | 仅顶层 |
| `impl` | Trait 实现 | 仅顶层 |
| `type` | 类型别名 | 顶层, 函数内, trait 内(关联类型) |
| `const` | 编译期常量 | 顶层, comptime 块内 |
| `mut` | 可变修饰 | 变量/参数声明(no-op于变量) |
| `ref` | 引用绑定 | 变量/参数声明 |
| `let` | 不可变绑定 | 任何块内 |
| `owned` | 所有权转移(仅形参) | 仅函数参数 |
| `magic` | 魔法方法声明 | 顶层, struct 内 |
| `test` | 测试用例 | 顶层, suite 内 |
| `suite` | 测试套件 | 仅顶层 |
| `setup` | 套件初始化 | 仅 suite 内 |
| `teardown` | 套件清理 | 仅 suite 内 |
| `import` | 模块导入 | 仅顶层 |
| `from` | 选择性导入 | 仅顶层 |
| `as` | 导入别名/类型转换 | 导入/表达式中 |
| `Number` | 内置数值 trait | 泛型约束 |

### 1.2 控制流关键字

| 关键字 | 用途 | 嵌套限制 |
|--------|------|---------|
| `if` | 条件分支 | 函数/块内 |
| `elif` | 多条件 | 仅 `if` 后 |
| `else` | 否则/循环结束/try成功 | `if`/`for`/`while`/`try` 后 |
| `match` | 模式匹配 | 函数/块内 |
| `case` | 匹配臂 | 仅 `match` 内 |
| `guard` | 条件守卫 | 函数/块内 |
| `for` | 迭代循环 | 函数/块内 |
| `while` | 条件循环 | 函数/块内 |
| `loop` | 无限循环 | 函数/块内 |
| `pass` | 占位符 | 任何块内 |
| `break` | 退出循环(单层) | 仅 `for`/`while`/`loop` 内 |
| `continue` | 下一轮迭代(单层) | 仅 `for`/`while`/`loop` 内 |
| `return` | 函数返回 | 仅函数/构建块内 |
| `with` | 上下文管理器 | 函数/块内 |
| `defer` | 延迟执行(LIFO) | 函数/块内 |

### 1.3 异常关键字

| 关键字 | 用途 |
|--------|------|
| `panic` | 不可恢复错误(内置函数) |
| `raise` | 抛出异常 |
| `raises` | 异常类型标注 |
| `try` | 异常捕获 |
| `catch` | 捕获分支 |
| `finally` | 最终执行块 |

### 1.4 异步与并发

| 关键字 | 用途 |
|--------|------|
| `async` | 异步函数修饰 |
| `await` | 等待异步结果 |
| `spawn` | 启动并发任务 |
| `go` | 启动并行任务(设计阶段) |

### 1.5 生成器

| 关键字 | 用途 |
|--------|------|
| `yield` | 生成器产出值 |

### 1.6 泛型与约束

| 关键字 | 用途 |
|--------|------|
| `where` | 泛型约束子句 |
| `Self` | 自身类型占位 |

### 1.7 宏与编译期

| 关键字 | 用途 |
|--------|------|
| `macro` | 宏定义 |
| `template` | 宏模板定义 |
| `comptime` | 编译期求值(块形式 `comptime:` 或表达式 `comptime expr`) |

### 1.8 测试

| 关键字 | 用途 |
|--------|------|
| `test` | 测试用例 |
| `assert` | 硬断言 |
| `check` | 软断言 |
| `suite` | 测试套件 |

### 1.9 布尔/字面量

| 关键字 | 对应 Rust | 说明 |
|--------|-----------|------|
| `True` | `true` | 布尔真 |
| `False` | `false` | 布尔假 |
| `None` | `None::<T>` | Option 空值 |
| `Some` | `Some(...)` | Option 包装 |
| `Ok` | `Ok(...)` | Result 成功 |
| `Err` | `Err(...)` | Result 失败 |
| `Never` | `!` | 发散类型 |
| `Unit` | `()` | 单元类型 |
| `Nil` | `()` | 空列表类型别名(设计阶段) |

---

## 二、禁止使用的写法

| 错误写法 | 原因 | 正确写法 |
|----------|------|---------|
| `void` | 不是关键字 | `()` |
| `string` | 不是类型名 | `str` |
| `null` | 不是关键字 | `None` |
| `::` | 废弃路径分隔符 | `.` |
| `;` | 不支持分号 | 省略 |
| `#` 作为注释 | `#` 是属性宏标记 | `//` |
| `float` | 不是类型名 | `f64` |
| `lazy` | 不是 LZ 关键字 | (无对应——Rust 后端不需要) |

---

## 三、运算符全集

| 类别 | 符号 | 优先级 |
|------|------|:---:|
| 成员访问 | `.` `?.` | 17 |
| 调用/索引 | `()` `[]` | 16 |
| 一元 | `-` `!` `not` `*`(取指针) | 14 |
| 幂 | `**` | 13 |
| 乘除模 | `*` `/` `%` | 12 |
| 加减 | `+` `-` | 11 |
| 位移 | `<<` `>>` | 10 |
| 位与 | `&` | 9 |
| 位异或 | `^` | 8 |
| 位或 | `\|` | 7 |
| 比较 | `<` `>` `<=` `>=` `is` | 6 |
| 等于 | `==` `!=` | 5 |
| 空值合并 | `??` | 3 |
| 逻辑与 | `and` `&&` | 4 |
| 逻辑或 | `or` `\|\|` | 3 |
| 管道 | `\|>` | 2 |
| 范围 | `..` `..=` | 2 |
| 三元 | `if/else` | 1 |
| 赋值 | `=` `+=` `-=` `*=` `/=` `%=` | 1 |
| 海象 | `:=` | 1 |
| 构建块 | `=:` `~:` `*:` | — |
| 复合赋值位 | `&=` `\|=` `^=` `<<=` `>>=` | 1 |
| 所有权移动 | `^`(后缀) | — |

---

## 四、语法边界 — 嵌套限制

| 语法 | 允许位置 | 不允许位置 |
|------|---------|-----------|
| `def` | 顶层, struct/trait/impl 内, 函数内 | — |
| `struct` | 仅顶层 | 函数内, 其他 struct 内 |
| `enum` | 仅顶层 | 函数内 |
| `trait` | 仅顶层 | 函数内 |
| `impl` | 仅顶层 | 函数内 |
| `suite` | 仅顶层 | 函数内, 其他 suite 内 |
| `test` | 顶层, suite 内 | 函数内, 其他 test 内 |
| `setup` | 仅 suite 内 | 顶层, 函数内 |
| `teardown` | 仅 suite 内 | 顶层, 函数内 |
| `import` | 仅顶层(文件头部) | 函数内 |
| `type` | 顶层, 函数内, trait 内 | — |
| `macro` | 仅顶层 | 函数内 |
| `template` | 仅顶层 | 函数内 |
| `comptime` | 函数/块内, 顶层 | — |
| `const` | 顶层, comptime 块内 | 函数体内(直接写 `const` 报错) |
| `break` | `for`/`while`/`loop` 内 | 循环外 |
| `continue` | `for`/`while`/`loop` 内 | 循环外 |
| `return` | 函数/构建块内 | 顶层 |
| `yield` | 生成器函数内 | 普通函数内 |
| `async` | 仅修饰 `def` | — |
| `await` | 仅 `async` 函数内 | 普通函数内 |

---

## 五、特殊限制

| 限制 | 说明 |
|------|------|
| 缩进 | 4 空格，不允许 Tab |
| 分号 | LZ 不支持分号，语句以换行分隔 |
| `::` | 词法层识别但语法层废弃，路径分隔用 `.` |
| 单行 if | `if cond: expr` 仅支持三元 `a if cond else b`。`if cond: stmt` 块体必须换行缩进 |
| 单行 guard | `guard cond else expr` 支持；`guard let Pattern = expr else:` 块体必须换行缩进 |
| 单行 for/while | 不支持，冒号后必须换行缩进 |
| `for`/`while` 不可赋值 | `let x = for ...` 编译错误 |
| `loop` 可赋值 | `let x = loop: ... break value` |
| 所有权 `^` | 后缀操作符，仅用于变量/字段移动 |
| `mut` 在变量中 | no-op，默认已可变；`mut self` 在方法中表示 `&mut Self` |
| struct 字段 | 必须带类型注解 |
| 闭包参数 | 必须带类型注解 |
