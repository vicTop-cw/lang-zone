# LZ 语言规范 — 三次审查报告

> 审查时间：2026-07-28（二修后复检）
> 审查范围：SYNTAX/ 全部文件

---

## 一、修复状态总览

### 本轮已修复（7 项）

| 编号 | 问题 | 文件 | 修复确认 |
|:---:|------|------|:-------:|
| 1 | README 第 15 行表格损坏 | README.md | ✅ `| 03 | [函数基础](03-函数基础.md) |` 恢复正常 |
| 2 | 03-函数基础.md 重复的两个 `## 六` | 03-函数基础.md | ✅ 已合并为一个 `## 六` |
| 3 | 14-生成器.md 多出的孤立 `上一章` | 14-生成器.md | ✅ 只剩一行合并链接 |
| 4 | 15-测试框架.md 多出的孤立 `上一章` | 15-测试框架.md | ✅ 只剩一行合并链接 |
| 5 | 01-类型系统.md 中英标点混用、多余空格 | 01-类型系统.md | ✅ `若未实现 ，使用` → `若未实现，使用` |
| 6 | 06a-struct.md `__new__` 位置参数不一致 | 06a-struct.md | ✅ 统一为 `Self(x: x, y: y)` 命名参数 |
| 7 | 02-变量与绑定.md `只 修饰` 多余空格 | 02-变量与绑定.md | ✅ 改为 `只修饰` |

### 累计已修复（13 项）

从初次审查至今，共计修复 **13 个问题**，包括最关键的 `ref` 语义矛盾、README 表格重复、交叉链接缺失等。

---

## 二、仍未修复的问题（4 处乱码）

以下 **4 处乱码字符**在三轮审查中均被发现，至今未修复。推测为文件编码问题（UTF-8 编码损毁或复制粘贴导致的字符截断）。

| 文件 | 行号 | 原文 | 应为 | 建议修复命令 |
|:----|:---:|------|------|-------------|
| **06a-struct.md** | 134 | `可修改字段��` | **可修改字段** | 将 `字段��` 替换为 `字段` |
| **06g-魔法综合.md** | 484 | `Truthiness��）` | **Truthiness）** | 去掉 `��` |
| **14-生成器.md** | 115 | `产��出` | **产出** | 将 `产��出` 替换为 `产出` |
| **99-内置预导入库.md** | 170 | `弹出末��` | **弹出末尾** | 将 `末��` 替换为 `末尾` |

> 这 4 个乱码的共同特征是：**中文字符的最后一个字节被截断**，导致显示为 `��`。建议用 VS Code 全局搜索 `末��`、`字段��`、`产��出`、`Truthiness��` 这四个字符串来批量定位修复。

---

## 三、新增的微小格式问题

### 🟡 03-函数基础.md 多余的空白行

line 186-191 末尾：
```
---
                       ← line 187
                       ← line 188 (空)
                       ← line 189 (空)
                       ← line 190 (空)
---
*上一章：...*
```
`---` 分隔符和结尾之间多了 3 行空行。建议删除 line 188-190。

---

## 四、最终文档完整性检查

| 检查项 | 状态 |
|--------|:---:|
| README 表格 20 行格式正确、链接可跳转 | ✅ |
| 每章首尾有 `上一章`/`下一章` 链接 | ✅ 不重复、不缺失 |
| `// ❌` 错误边界前缀一致 | ✅ |
| 全文无重复标题 | ✅ |
| 中文语境下标点统一 | ✅ |
| 无乱码字符 | ❌ **4 处** |

---

## 五、终极修复建议

1. **修复 4 处乱码**（P0）—— 全局搜索替换即可，1 分钟
2. **删除 03-函数基础.md 末尾 3 行空行**（P2）—— 30 秒

其余问题已全部清零。以上两项修完后，全系列文档可达到 **零错误** 状态。
