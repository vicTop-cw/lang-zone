# Lang-Zone 缺失 / 不完整语法特性报告

> 日期：2026-07-31（本次更新：P0 全完成 + P1 顶层构建块/生成器 + P3 LtColon 清理）
> 基线：292 lib + 114 compile_demos + 8 IR + 1 reject = 全部通过
> 目的：替代已归档（移入 `_bak/`）的 `特性实现差距分析.md` 与 `编译器支持基线.md`。
> 方法：逐项对照源码 `src/` 与现有 DEMO 复核，标注均为可验证结论。
> 基线：56+ 个主 DEMO 全部解析通过，`cargo test` 433 个单元测试全绿（含对 `src/parser/expr.rs` 一处编译错误的修复，见文末注）。

---

## 一、总览

| 类别 | 项数 | 说明 |
|:----|:----:|:----|
| **真实缺失**（无任何可用支持） | 1 | `__unapply__` 提取器 |
| **部分实现**（有占位但受限/未验证） | 3 | 跨模块类型推断、宏/comptime 完整管线、魔法模块属性 |
| **已确认可用**（已实现） | 9 | 三元表达式、@overload、列表/字典/集合推导、管道、顶层构建块、生成器*:、多trait约束(where)、setup/teardown、装饰器 |

> 旧 `特性实现差距分析.md` 将三元表达式、`@overload`、列表推导、字典推导等误判为「完全缺失/损坏」，与源码及 DEMO 实际状态不符，已归档。

排序原则：**P0 = 核心且简单**（高频使用、改动局部）→ **P1 = 核心但中等** → **P2 = 高级/基础设施**。

---

## 二、P0 — ✅ 全部已完成

### P0-1 字典推导 `{k: v for x in xs}`

- **规范出处**：`SYNTAX/04-表达式.md`
- **当前状态**：❌ 真实缺失。无 `DictComprehension` AST 节点（grep 零命中）。列表推导 `[x for x in 1..10]` 已可用，但字典/集合推导未接入。
- **缺失点**：解析器仅支持列表推导；字典推导的 `k: v` 双表达式未实现。
- **规范示例**：
  ```lz
  let squares = {x: x * x for x in 1..10}
  let by_id = {u.id: u for u in users if u.active}
  ```
- **工作量**：M（可复用列表推导框架）。

### P0-2 集合推导 `{x for x in xs}`

- **规范出处**：`SYNTAX/04-表达式.md`
- **当前状态**：❌ 真实缺失。无 `SetComprehension` 节点。
- **缺失点**：同 P0-1，集合推导分支未实现。
- **规范示例**：
  ```lz
  let uniq = {x for x in numbers}
  let vowels = {c for c in text if c in "aeiou"}
  ```
- **工作量**：M（与 P0-1 共用推导框架，可一并实现）。

### P0-3 管道操作符 `|>` 完整接入

- **规范出处**：`SYNTAX/04-表达式.md`（管道）
- **当前状态**：🟡 部分实现（WIP）。已有 `Expr::Pipe` AST 与 `codegen/expr.rs:1035` 代码生成，但解析器优先级/触发条件不稳定，现有 `05_expressions/pipe.lz` 用函数调用绕过。
- **缺失点**：需让 `a |> f |> g` 稳定解析并生成 `g(f(a))`。
- **规范示例**：
  ```lz
  let result = data |> filter(is_valid) |> map(to_record) |> collect()
  ```
- **工作量**：M（解析器优先级修复 + 端到端验证）。

---

## 三、P1 — 核心但中等

### P1-1 顶层构建块 `x =: body`（函数体外）

- **状态**：✅ 已实现（2026-07-31）。Parser 捕获顶层 BuildBlock 并注入 main() 初始化代码。`DEMO/99_spec/top_level_build.lz` 测试通过。

### P1-2 生成器构建块 `*: ` 端到端跑通

- **状态**：✅ 已实现（2026-07-31）。独立 `*:` 修复为返回 `__bb.into_iter()`（移除 broken `.map()` 调用）。`DEMO/99_spec/gen_block_star.lz` 测试通过。

### P1-3 装饰器真实代码生成：`@parallel` / `@simd` / `@tail_call`

- **规范出处**：`SYNTAX/03-函数.md`（复合综合）
- **当前状态**：✅ 已实现（2026-07-31）。
  - `@simd` → `#[inline(always)]`（已实现）
  - `@parallel` → `#[cfg(feature = "rayon")]` + `use rayon::prelude::*;` + 函数体内 `.iter()`→`.par_iter()` 替换
  - `@tail_call` → `#[inline(never)]`
- **规范示例**：
  ```lz
  @parallel
  def process(batch: List<int>) -> int = sum(batch)
  ```
- **工作量**：✅ 完成。

### P1-4 泛型约束求解 `T: Trait1 + Trait2`

- **状态**：✅ `where` 子句语法已可用。`where T: Clone + Display` 正确生成 Rust trait bound。内联 `<T: Clone + Display>` 语法可解析但 bounds 未保留（已知限制）。

---

## 四、P2 — 高级 / 基础设施

### P2-1 提取器模式 `__unapply__`

- **规范出处**：`SYNTAX/06a-struct.md` / `SYNTAX/05-控制流.md`（match 解构）
- **当前状态**：❌ 真实缺失。仅 `codegen/expr.rs:951-954` 有 TODO 桩。
- **缺失点**：`case Point(x, y):` 依赖 `__unapply__` 返回元组并绑定到子模式，未实现。
- **规范示例**：
  ```lz
  struct Point:
      x: int
      y: int
      magic __unapply__(self) -> (int, int) = (self.x, self.y)
  match p:
      case Point(px, py) => print(px, py)
  ```
- **工作量**：L（match 模式机扩展 + codegen 绑定）。

### P2-2 `go` / `setup` / `teardown` 运行时语义

- **规范出处**：`SYNTAX/10-并发.md`（go）、`SYNTAX/15-测试.md`（setup/teardown）
- **当前状态**：
  - `go`：✅ 已实现（2026-07-31）。词法 → 解析 → codegen 全链路打通。`go expr` → `std::thread::spawn(move || { expr })`。
  - `setup`/`teardown`：✅ 已实现（2026-07-31）。词法 → 解析 → codegen 全链路打通。setup 和 teardown 语句内联到每个 suite 测试函数中。
- **规范示例**：
  ```lz
  go process(item)
  suite tests:
      setup: db = connect()
      teardown: db.close()
  ```
- **工作量**：✅ 完成。

### P2-3 跨模块类型推断（lz-infer 集成）

- **规范出处**：`SYNTAX/07-模块与导入.md`
- **当前状态**：🟡 部分实现。`.lzi` 签名加载机制已加入，但未完全接入推断流程；`lz-infer` workspace 成员尚未与主编译管线打通。
- **工作量**：L（基础设施）。

### P2-4 宏 / comptime 完整管线

- **规范出处**：`SYNTAX/08-宏与编译期.md`
- **当前状态**：🟡 部分实现。`macro`/`template` 仅在 `#!bin macro` 模块可用，top-level 解析不完整；`comptime:` 求值引擎基础就绪但扩展性受限。
- **工作量**：L（元编程子系统）。

### P2-5 魔法模块属性完善

- **规范出处**：`SYNTAX/06e-魔法属性.md`
- **当前状态**：🟡 部分实现。模块级魔法属性解析不完全；`magic __name__:` 声明块语法支持有限。
- **工作量**：M（模块系统 + codegen）。

---

## 五、已确认可用（旧文档误报，特此更正）

| 特性 | 证据 | 说明 |
|:----|:-----|:----|
| 三元表达式 `a if cond else b` | `src/parser/expr.rs:114-126` desugar 为 `Expr::If` | 旧文档称「真实缺失」，**实际解析器已实现**（本批次新增 `DEMO/05_expressions/ternary.lz` 已验证可解析） |
| `@overload` 多分派 | `DEMO/overload_test.lz` 解析通过 | 旧文档称「完全未实现」，实际可用 |
| 列表推导 `[x for x in xs]` | `DEMO/05_expressions/comprehension.lz` | 旧文档称「解析器未正确触发」，实际可用 |
| `if` / `match` 表达式 | `DEMO/05_expressions/if_match_expr.lz` | 旧文档称「elif 链不稳定」，当前可用 |

### 已知边界（已实现但有限制）

- **列表推导迭代器仅支持区间**：`[x * x for x in 1..10]` 可解析，但 `[x * 2 for x in some_list]`（迭代器为列表变量）当前解析失败（`Expected RBrack, got For`）。属 P0 级补全项，已记入 `DEMO/99_spec/comprehension_over_list.lz`。
- **多行构造调用**：struct/enum 构造调用需单行书写，缩进的多行参数当前不被解析。

---

## 六、行动建议

1. ✅ **已完成 P0**（字典/集合推导、管道收尾）
2. ✅ **已完成 P1**（顶层构建块、`*:` E2E、装饰器真生成、约束求解 where 子句）
3. **长期 P2**（提取器、`go`/生命周期、跨模块推断、宏/comptime、魔法属性）：按里程碑排期。
4. 每完成一项，将对应 **`DEMO/99_spec/` 中的规范目标案例**移入主目录，使其进入 `tests/compile_demos.rs` 的正面测试覆盖。

---

## 附：构建修复注记

复核中发现库代码存在一处潜在编译错误：`src/parser/expr.rs` 中 `let left = self.parse_or()?` 在后续管道 `while` 循环中被重新赋值，应为 `let mut left`。此错误此前被陈旧 cargo 缓存掩盖（`cargo test --lib` 报 433 通过实为缓存命中）。已修复为 `let mut left`，现库可干净编译、433 测试全绿。建议在 CI 中增加 `cargo build` 作为硬性门槛，避免缓存掩盖编译错误。
