# LZ 语言规范 — 二次审查报告

> 审查时间：2026-07-28（版本修复后复检）
> 审查范围：SYNTAX/ 全部文件

---

## 一、修复状态总览

### ✅ 已修复（6 项）

| 编号 | 问题 | 文件 | 修复方式 |
|:---:|------|------|----------|
| 1 | 🔴 `ref` 语义矛盾（§一 说不可变，§五 说可变） | 02-变量与绑定.md | §一 表格改为"可变引用"；§五 新增原理说明和 Rust 映射表 |
| 2 | 🔴 README 🀄 `03` 和 `03索引` 重复 | README.md | 合并为一行 `03`，删除 `03索引`，链接指向 `03-函数基础.md` |
| 3 | 🔴 `&&`/`||` 分类不一致 | 00-词法基础.md | 新增 §1.10 尾注说明关键字与符号等价关系 |
| 4 | 🔵 14/15 缺少下一章链接 | 14-生成器.md、15-测试框架.md | 补上 `下一章` 链接 |
| 5 | 🔵 03-函数.md 索引页无区分 | 03-函数.md | 标题改为"函数总览（索引页）"，加 ⚠ 提示 |
| 6 | 🟠 00-词法基础.md 遗漏 `f"""` `r"""` | 00-词法基础.md | 补上 f-多行字符串和 r-多行字符串 |

### ❌ 未修复（6 项）

| 编号 | 问题 | 文件 | 行号 |
|:---:|------|------|:---:|
| 7 | 🟠 `可修改字段��` 乱码 | 06a-struct.md | 134 |
| 8 | 🟠 `Truthiness��` 乱码 | 06g-魔法综合.md | 484 |
| 9 | 🟠 `产��出` 乱码 | 14-生成器.md | 115 |
| 10 | 🟠 `末��尾` 乱码 × 2 | 99-内置预导入库.md | 170, 172 |
| 11 | 🟠 `若未实现 ，使用会报错` 多余空格 | 01-类型系统.md | 206, 208 |
| 12 | 🟠 `06a-struct.md __new__` 位置参数 vs 命名参数不一致 | 06a-struct.md | 342-343 |

---

## 二、新发现的问题（修复引入）

### 🔴 2.1 README.md 表格行损坏（第 15 行）

```
C-函数基础.md) | 03 系列入口（基础语法） |
```

第 `03` 行的 Markdown 管道符和链接语法被破坏。完整行应为：

```
| 03 | [函数基础](03-函数基础.md) | 03 系列入口（基础语法） |
```

**文件**：README.md 第 15 行

---

### 🔴 2.2 03-函数基础.md 重复的 "六、错误语法边界"

文件末尾出现了**两个** `## 六、错误语法边界` 标题：

- 第一个（line 165）：包含 `冒号不匹配`、`等式体换行不缩进`
- 中间的"扩展函数形式"表格（line 179）
- 第二个（line 188）：包含 `匿名函数语法错误`

导致标题编号重复，且"扩展函数形式"被夹在两个"错误语法边界"之间。

**文件**：03-函数基础.md 第 165 行和第 188 行

---

### 🔴 2.3 14-生成器.md / 15-测试框架.md 重复的上一章链接

两个文件末尾各出现了**两个** `上一章` 链接：

**14-生成器.md（line 489-491）：**
```
*上一章：[13-指针与引用](13-指针与引用.md)*

*上一章：[13-指针与引用](13-指针与引用.md)* · *下一章：[15-测试框架](15-测试框架.md)*
```

**15-测试框架.md（line 174-176）：**
```
*上一章：[14-生成器](14-生成器.md)*

*上一章：[14-生成器](14-生成器.md)* · *下一章：[99-内置预导入库](99-内置预导入库.md)*
```

各多了一行单独的 `上一章`，应删除孤立的那行，只保留合并行。

---

### 🟡 2.4 03-函数基础.md 缺少 `// ❌` 标记

line 169：
```lz
def bad(-> int): ...         // 错误：-> 必须在参数列表后
```
前面的 `// ❌` 缺失，与上下文的错误示例风格不一致。

---

## 三、仍存在的旧问题

### 🟠 3.4 01-类型系统.md 多余空格

line 206-208：
```lz
def get_user(id: UserId) -> str = ... // 抽象函数，若未实现 ，使用会报错,不使用没事

def get_user2(id: UserId) -> str = pass // 占位待实现 ，若未实现 ，使用会报错，不使用报警告
```

- `若未实现 ，使用` → 中文逗号前有多余空格
- `会报错,不使用` → 中文语境下用英文逗号，应统一为 `会报错，不使用`

### 🟠 3.5 14-生成器.md 注释中的 `yield 产��出` 乱码

line 115：
```
**注意**：生成器的返回类型标注（`-> T`）表示 **yield 的元素类型**，而非迭代器类型。
例如 `def counter() -> int = ...` 中 `-> int` 表示每次 yield 产��出 `int` 值。
```
"产��出" 应为 "产出"。

---

## 四、文档完整性检查（交叉引用）

| 检查项 | 状态 |
|--------|:---:|
| 00–99 每章都有 `上一章`/`下一章` 链接 | ⚠️ 14、15 多了重复的上一章链接 |
| README 表格所有链接可跳转 | ⚠️ 第 15 行格式损坏 |
| 所有 `详见` 引用指向存在的文件 | ✅ |
| 所有错误边界示例使用 `// ❌` 前缀 | ⚠️ 03-函数基础.md 缺 1 处 |

---

## 五、建议修复优先级

| 优先级 | 问题 | 难度 |
|:-----:|------|:---:|
| **P0** | README.md 第 15 行表格损坏——首页可见，影响第一印象 | 1 分钟 |
| **P0** | 03-函数基础.md 两个 `## 六` —— 读者困惑 | 1 分钟 |
| **P0** | 14/15 多出的孤立 `上一章` 行——去掉即可 | 30 秒 |
| **P1** | 6 处乱码字符——批量查找替换 | 2 分钟 |
| **P1** | 01-类型系统.md 多余空格 + 中英混用标点 | 2 分钟 |
| **P2** | 03-函数基础.md 缺 `// ❌` | 30 秒 |
| **P2** | 06a-struct.md `__new__` 示例位置参数 vs 命名参数统一 | 1 分钟 |
