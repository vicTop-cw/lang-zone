# LZ 想法仓库（冻结期语义冲动登记）

> 建立日期：2026-08-04
> 关联：[语法冻结基线.md](语法冻结基线.md) §三通道 3 / §四解锁条件
> 规则：本仓库只**记录**，不实现。每条含「痛点 / 宏替代 / 语法影响面 / 是否服务自举」。冷静期 30 天，到期仍强烈需要才进入评估（且仅在自举后）。

---

## 登记模板

```
### <序号> <标题>

- **痛点**：
- **宏/库替代**：
- **语法影响面**（若必须动语法：动哪里、影响哪些文档）：
- **服务自举**：是 / 否（说明）
- **登记日期**：
- **冷静期结束**：
- **状态**：🟡 冷静期 → ⏳ 待评估（自举后）→ ❌ 已否决 → ✅ 已采纳
```

---

## 已登记条目

### 01 `__Params.args` 异构元组化（type-pack 依赖）

- **痛点**：`ps.args[0] as int` 需显式 `as`，同构 `List<dyn>` 丢失实参类型信息。
- **宏/库替代**：无（类型系统能力，宏不可达）。
- **语法影响面**：`SYNTAX/03c-检查站.md` §4.5；依赖变长泛型（type-pack）；连带 `03d-可变参数.md` §2.8 类型保真目标。
- **服务自举**：否（自举编译器不需要；属类型系统增强）。
- **登记日期**：2026-08-04（由 03c 正文「未来升级」段落迁移，见缺失语法特性报告 P2-6）。
- **冷静期结束**：2026-09-03。
- **状态**：🟡 冷静期。

---

> 后续新想法按模板追加。冻结期禁止直接修改 SYNTAX 语法文档以实施本条内容。
