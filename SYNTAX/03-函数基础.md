# LZ 函数基础

> 规范版本: 3.3 · 基于编译器源码 · 最后校订: 2026-08-04

本文档定义 Lang-Zone 函数的核心语法：`def` 声明、参数、返回类型、return 语句、异常函数及常见语法错误边界。

---

## 一、基本定义

### 1.1 `def` 声明

```lz
def function_name(param1: Type1, param2: Type2) -> ReturnType =
    body
```

### 1.2 等式体（单行）

`=` 后跟表达式在同一行：

```lz
def add(a: int, b: int) -> int = a + b
def double(x) = x * 2
def greet(name: str) -> str = f"Hello, {name}"
```

### 1.3 块体（多行）

`=` 后换行 + 缩进：

```lz
def max(a: int, b: int) -> int =
    if a > b:
        a
    else:
        b
```

---

## 二、参数

### 2.1 基本参数

```lz
def add(a: int, b: int) -> int = a + b
```

### 2.2 无注解参数，需要数学表达（`@math` 自动泛型化）

```lz
@math  // 自动为每个无注解参数生成独立泛型参数: sq<T: Number, U: Number>(x: T, y: U)
def sq(x, y) = x * x + y * y
// sq(3, 4)       → 9 + 16 = 25 (int)
// sq(3.0, 4.0)   → 25.0 (f64)
// sq(3, 4.0)     → 25.0 (f64 — 自动类型提升)
```

### 2.3 参数修饰符

| 修饰符 | 示例 | 语义 | Rust 映射 | 强制消费 |
|--------|------|------|-----------|:---:|
| 无 | `x: int` | 默认按值传递，所有权转移（仿射，可丢弃） | `x: T` | 否 |
| `ref` | `ref x: str` | 不可变引用传递（借用） | `x: &T` | 否 |
| `mut ref` | `mut ref x: int` | 可变引用传递（可变借用） | `x: &mut T` | 否 |
| `mut` | `mut x: int` | 可变按值传递，函数内可修改 | `mut x: T` | 否 |
| `owned` | `owned x: str` | **必须拿到所有权**；调用方原变量毒化/失效；函数内必须显式消费 | `x: T` | **是** |
| `mut owned` | `mut owned x: str` | 可变 + 强制消费（可改内容，但仍须消费） | `mut x: T` | **是** |
| `owned ref` | `owned ref x: str` | ❌ 非法：引用无所有权可消费，语义矛盾 | — | — |
| 函数实参 | `owned cb: fn(T)` | ❌ 非法：**函数作为实参永远不能用 `owned` 修饰** | — | — |

```lz
def read_data(ref data: List<int>) -> int =
    data.len()

def modify(mut ref data: List<int>) =
    data.push(42)

def double(mut x: int) -> int =      // 可变按值：函数内可改 x
    x *= 2
    x

def consume(owned name: str) -> str = // 强制所有权：调用方 name 被毒化
    name^                              // 内部必须显式消费（见下）
```

**`owned` 形参内部强制消费的三种形式**（三者缺一 → 编译错误）：

| 消费形式 | 示例 | 说明 |
|----------|------|------|
| 显式转移 | `name^` | 后缀 `^` 把所有权交给他人 |
| 返回 | `return name` | 作为返回值交出所有权 |
| 传给 owned 形参 | `use_owned(name)` | 作为实参传给另一个 `owned` 修饰形参的函数 |

> **毒化语义**：调用方实参传入 `owned` 形参后，原变量立即失效（无论栈/堆）——再使用即编译错误。函数内部拿到所有权后同样必须按上述三种形式之一显式消费。

### 2.4 默认参数值

```lz
def greet(name: str = "world") -> str =
    f"Hello, {name}"

greet()              // "Hello, world"
greet("Lang-Zone")   // "Hello, Lang-Zone"
```

---

## 三、返回类型

### 3.1 显式返回类型

```lz
def add(a: int, b: int) -> int = a + b
def is_positive(x: int) -> bool = x > 0
```

### 3.2 无返回类型（默认 int）

```lz
def double(x) = x * 2     // -> int（推断）
```

### 3.3 main 函数（默认 ()）

```lz
def main() =
    print("hello")        // -> ()  不需要显式标注
```

### 3.4 无返回值 `()`

```lz
def log(msg: str) -> () =
    print(msg)
```

### 3.5 发散函数 `Never`

```lz
def abort(msg: str) -> Never =
    raise Exception(msg)
```

---

## 四、return 语句

```lz
def early_exit(x: int) -> int =
    if x < 0:
        return 0          // 提前返回
    x + 1                 // 尾表达式作为返回值
```

- 块体**尾表达式**自动作为返回值（无需 `return`）
- 也支持显式 `return expr` 提前退出
- `return` 不带值仅在 `-> ()` 函数中合法

---

## 五、异常函数

### 5.1 raises 声明

```lz
def divide(a: int, b: int) -> int raises str =
    if b == 0:
        raise "division by zero"
    a / b
```

### 5.2 raise 语义检查

如果函数体内有 `raise`，则**必须**声明 `raises` 类型。

```lz
// ❌ 错误：有 raise 但无 raises 声明
def bad() -> int =
    raise "oops"
    // 错误：缺少 raises 子句
```

---

## 六、错误语法边界

```lz
// ❌ 冒号不匹配
def bad(x:): ...             // 错误：: 后缺类型
def bad(-> int): ...         // ❌ -> 必须在参数列表后

// ❌ 等式体换行不缩进
def bad() =
x + 1                        // 错误：必须缩进
```

---

## 扩展函数形式

| 形式 | 说明 | 详见 |
|------|------|------|
| `async def` | 异步函数，返回 `Future<T>` | [10-并发与异步.md](10-并发与异步.md) |
| `comptime def` | 编译期函数，编译时执行 | [08b-comptime编译期.md](08b-comptime编译期.md) |

---

## 本系列文档

函数主题拆分为以下专题，按需深入：

| 编号 | 文档 | 内容 |
|:---:|------|------|
| 03 | 函数基础（本文） | `def` 声明、参数、返回类型、等式体与块体、装饰器、闭包 |
| 03b | [函数泛型](03b-函数泛型.md) | 泛型函数、约束、`where` 子句、泛型默认参数 |
| 03c | [检查站](03c-检查站.md) | `[checker]` 语法、运行时检查、`__Params`、代码生成 |
| 03d | [可变参数](03d-可变参数.md) | `..` 分隔符、位置/关键字参数、args/kwargs、类型收集 |
| 03e | [复合综合](03e-复合综合.md) | 函数嵌套、装饰器、闭包、嵌套定义、函数综合示例 |

---

*上一章：[02-变量与绑定](02-变量与绑定.md)* · *下一章：[03b-函数泛型](03b-函数泛型.md)*
