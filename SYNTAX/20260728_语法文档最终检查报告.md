# LZ 语法文档最终检查报告 v6.0

> 检查范围：`E:\IDEProjects\AI\lang-zone\SYNTAX\` 全部 29 份文档
> 检查日期：2026-07-28
> 基于上一轮 v5.0 报告，对比本轮文档更新后的状态

---

## 变更摘要

| 类别 | v5.0 | 本轮已修复 | 本轮新增 | 合计 |
|:----:|:----:|:--------:|:--------:|:----:|
| P0 关键矛盾 | 4 | 2 | **0** | **2** |
| P1 重要欠缺 | 16 | 11 | **5** | **10** |
| P2 文档质量 | 23 | 11 | **12** | **24** |
| **合计** | **43** | **24** | **17** | **36** |

> 本轮修复率 56%（24/43），新增 17 个问题。

---

## P0 — 关键矛盾 / 阻塞问题（2 项）

### 1. `await` 使用规则自相矛盾 [仍存在]

**文件**：[10-并发与异步.md](10-并发与异步.md)

**问题描述**：
- §1.2 明确声明："`await` 只能在 `async` 函数中使用"
- §1.3 示例中却在同步函数 `def main() =` 中直接使用 `await f1`、`await f2`

```lz
// §1.2 规则：await 只能在 async 函数中使用
// §1.3 示例：
def main() =                 // 同步函数
    let f1 = spawn fetch_data(10)
    let r1 = await f1        // ← 在非 async 函数中使用 await
```

**建议**：明确规则 — 要么允许同步函数中使用 `await`（需更新 §1.2），要么修改 §1.3 示例将 `main()` 改为 `async def main()`。

---

### 2. `self` 语义在普通方法与魔法方法间不一致 [仍存在]

**文件**：[06a-struct.md](06a-struct.md) vs [06g-魔法综合.md](06g-魔法综合.md)

**问题描述**：
- 06a-struct.md §3.2：普通方法 `self` = `&Self`（不可变引用）
- 06g-魔法综合.md §一：魔法方法 `self` = owned（消费 self）

```lz
// 普通方法（06a-struct.md）
def area(self) -> f64 = self.width * self.height   // self 是 &Self

// 魔法方法（06g-魔法综合.md）
def __add__(self, other: T) -> T                  // self 是 owned Self
```

**建议**：在 06a-struct.md 中新增一个对照表，明确列出所有 self 模式及其语义差异，并在 06g-魔法综合.md 中交叉引用。

---

## P1 — 重要欠缺 / 遗漏（10 项）

### 3. `raises` 函数返回类型语义混乱 [仍存在]

**文件**：[09-错误处理.md](09-错误处理.md) §5.2

```lz
def read_config() -> Dict<str, str> raises str =
    let content = read_file("config.json")?
    let parsed = parse_json(content)?
    Ok(parsed)       // ← 返回 Ok(parsed)，但返回类型是裸类型 Dict<str, str>
```

**问题**：函数体内使用 `Ok(parsed)` 包装，但返回类型标注为 `Dict<str, str>`。`?` 操作符（§4.3）又说仅对 `Result` 类型有效。

**建议**：统一 `raises` 函数的返回类型模型 — 要么要求返回类型标注为 `Result<T, E>`，要么明确 `raises` 函数返回的是裸类型 `T`（由编译器自动包装）。

---

### 4. `99-内置预导入库.md` 全局函数签名与魔法方法签名不匹配 [仍存在]

**文件**：[99-内置预导入库.md](99-内置预导入库.md)

| 全局函数 | 文档签名 | 实际魔法方法签名 | 差异 |
|---------|---------|----------------|------|
| `str(x)` | `(T) -> str` | `fn fmt(&self, f: &mut Formatter) -> fmt::Result` | 参数和返回值完全不同 |
| `clone(x)` | `(T) -> T` | `fn clone(&self) -> Self` | 借用 self vs 消费 |
| `drop(x)` | `(T) -> ()` | `fn drop(&mut self)` | 可变借用 vs 消费 |

**建议**：在全局函数表中标注这些函数是魔法方法的**包装器**（wrapper），并添加交叉引用链接到 06d-内置魔法trait.md 中的实际签名。

---

### 5. `sort`/`reverse` 与 `filter`/`map` 签名风格不一致 [仍存在]

**文件**：[99-内置预导入库.md](99-内置预导入库.md)

| 函数 | 文档签名 |
|------|---------|
| `sort(xs)` | `(List<T>) -> List<T>` |
| `reverse(xs)` | `(List<T>) -> List<T>` |
| `map(xs, f)` | `(Iter<T>, fn(T)->R) -> Iter<R>` |
| `filter(xs, f)` | `(Iter<T>, fn(T)->bool) -> Iter<T>` |

**问题**：`sort`/`reverse` 接受 `List<T>` 而 `map`/`filter` 接受 `Iter<T>`，但迭代器通常也支持排序和反转操作。

**建议**：统一为迭代器风格，或明确说明 `sort`/`reverse` 只能用于具体 List 类型的原因。

---

### 6. `__new__`/`__init__` 设计存在未决问题 [仍存在]

**文件**：[06a-struct.md](06a-struct.md) §6.4

**问题**：§6.4 明确承认：
- "实现了 `__new__` 后，编译器默认的关键字构造是否仍可用**有待确定**"
- "与 trait `Constructible` 的交互仍在**设计阶段**"

这意味着 struct 构造机制的核心行为尚未确定，用户无法依赖稳定的构造语义。

**建议**：尽快确定构造器规则，至少在文档中标注"实验性"状态。

---

### 7. [NEW] `05-控制流.md` 章节编号重复

**文件**：[05-控制流.md](05-控制流.md)

- 第 381 行：`## 十二、错误语法边界`
- 第 416 行：`## 十二、`else` 关键字用法汇总`

两个"十二"章节，第一个应为 `## 十二、错误语法边界`，第二个应为 `## 十三、else 关键字用法汇总`。

---

### 8. [NEW] `01-类型系统.md` 子章节编号错误

**文件**：[01-类型系统.md](01-类型系统.md)

"## 七、复合类型" 下的子章节编号为 `6.1`、`6.2`、`6.3`，应改为 `7.1`、`7.2`、`7.3`。

---

### 9. [NEW] `03d-可变参数.md` 版本号不一致

**文件**：[03d-可变参数.md](03d-可变参数.md)

该文档版本号为 `3.2`，而其他所有文档均为 `3.1`。需统一版本号。

---

### 10. [NEW] `Never`/`Unit` 类型状态矛盾

**涉及文件**：
- [00-词法基础.md](00-词法基础.md) §1.11：`Never` 和 `Unit` 均标注为"设计阶段"
- [01-类型系统.md](01-类型系统.md) §1.2：将 `Unit` 和 `Never` 作为正式类型文档化，提供完整示例
- [03-函数基础.md](03-函数基础.md) §3.5：`def abort(msg: str) -> Never = raise Exception(msg)` 作为正式示例

**问题**：`00-词法基础.md` 说它们是"设计阶段"，但其他文档把它们当作已实现的功能使用。

**建议**：统一状态 — 如果已实现，更新 00-词法基础.md 移除"设计阶段"标注；如果未实现，标注其他文档中的示例为"规划中"。

---

### 11. [NEW] `setup`/`teardown` 实现状态矛盾

**涉及文件**：
- [00-词法基础.md](00-词法基础.md) §1.9：`setup`/`teardown` 均标注为"设计阶段，编译器未实现"
- [15-测试框架.md](15-测试框架.md) §三：将 `setup`/`teardown` 作为正常工作功能完整文档化

**建议**：统一状态标注。

---

### 12. [NEW] `StopIteration` 异常类型未定义

**涉及文件**：[14-生成器.md](14-生成器.md)

生成器文档中使用 `raise StopIteration("limit reached")`，但 `StopIteration` 在 [09-错误处理.md](09-错误处理.md) 中完全未定义，[99-内置预导入库.md](99-内置预导入库.md) 中也未列出。

**建议**：在 09-错误处理.md 或 99-内置预导入库.md 中定义 `StopIteration` 异常类型。

---

## P2 — 文档质量问题（24 项）

### 13. `open()` 函数归属混淆 [仍存在]

**文件**：[99-内置预导入库.md](99-内置预导入库.md)

`open()` 列在 prelude 全局函数表中，但标注"需 `import std.fs`"。这造成困惑：它到底是不是 prelude 的一部分？

**建议**：将 `open()` 从 prelude 表中移除，在 §四标准库模块中说明。

---

### 14. 生成器返回类型标注语义不清 [仍存在]

**文件**：[14-生成器.md](14-生成器.md)

```lz
def counter(n: int) =           // 无返回类型
def limited(n: int) -> int =    // 返回类型 int
```

生成器返回的是迭代器而非 `int`，`-> int` 可能表示 yield 的元素类型，但文档未明确说明。

**建议**：在文档中明确生成器返回类型标注的语义（表示 yield 的元素类型，而非迭代器类型）。

---

### 15. [NEW] `Dict<K,V>` 和 `Set<T>` 缺少 `Hash`/`Eq` 约束文档

**涉及文件**：[01-类型系统.md](01-类型系统.md)、[99-内置预导入库.md](99-内置预导入库.md)

`Dict<K,V>` 要求 `K: Hash + Eq`，`Set<T>` 要求 `T: Hash + Eq`，但这些约束从未在文档中说明。用户定义自定义类型作为字典键或集合元素时会遇到编译错误却不知原因。

**建议**：在 01-类型系统.md 或 99-内置预导入库.md 中说明这些隐式约束。

---

### 16. [NEW] `_` 特殊标识符中的 `..` 解构语法未展开

**文件**：[00-词法基础.md](00-词法基础.md) §4.3

```lz
let (first, ..,) = xs      // 取第一个，其余忽略
```

`..` 在元组解构中的用法仅在此处出现，但 [01-类型系统.md](01-类型系统.md) 和 [02-变量与绑定.md](02-变量与绑定.md) 中的解构部分均未提及此语法。

**建议**：在 02-变量与绑定.md 的解构部分补充 `..` 忽略剩余元素的语法说明。

---

### 17. [NEW] `__slots__` 归属混乱

**文件**：[06e-模块级魔法属性.md](06e-模块级魔法属性.md) §六

`__slots__` 被列在**模块级**魔法属性中，但它的实际用途是在 **struct 内部**（如 `struct MyStruct = __slots__ = "C"`）。

**建议**：将 `__slots__` 移至 06a-struct.md，或在 06e 中明确标注它是 struct 级别的属性。

---

### 18. [NEW] `assert_eq` 命名与 `assert` 关键字混淆

**涉及文件**：
- [08-宏与编译期.md](08-宏与编译期.md) §3.6：`assert_eq` 是宏内置函数
- [15-测试框架.md](15-测试框架.md)：`assert` 是测试关键字

`assert_eq` 作为宏函数名与测试框架的 `assert` 关键字语义相近但用途不同，可能造成混淆。

**建议**：在 08-宏与编译期.md 中为 `assert_eq` 添加注释说明与测试 `assert` 的区别。

---

### 19. [NEW] `List<T>.length()` 与 `len(x)` 的关系未说明

**文件**：[99-内置预导入库.md](99-内置预导入库.md)

文档中 `List<T>` 和 `str` 都有 `.length()` 方法，同时还有全局函数 `len(x)` 映射到 `__len__`。但这三者之间的关系（`.length()` 是否调用 `__len__`？`len()` 是否等同于 `.length()`？）完全没有说明。

**建议**：在 99-内置预导入库.md 中添加说明，明确 `.length()`、`len()`、`__len__` 三者的调用关系。

---

### 20. [NEW] `04-表达式.md` 中 `in` 运算符语义不完整

**文件**：[04-表达式.md](04-表达式.md) §4

`in` 被列为比较运算符，映射到 `__contains__`。但 `in` 对不同类型（`str` 子串匹配、`List` 元素检查、`Dict` 键检查、`Set` 成员检查）的具体语义行为未在表达式文档中说明。

**建议**：在 04-表达式.md 或 12-操作符.md 中补充 `in` 运算符对各内置类型的语义表。

---

### 21. [NEW] `14-生成器.md` 中 `return` 带错误类语法不标准

**文件**：[14-生成器.md](14-生成器.md) §2.3

```lz
def safe_read(items: List<Option<int>>) -> int =
    ...
    return ValueError("encountered None in stream")
```

`return ErrorType(msg)` 在生成器中的语义与 Python 不同。Python 中 `return` 只是停止迭代，不携带错误类。这种特殊语法需要更详细的说明。

**建议**：在文档中明确说明 `return ErrorType` 在生成器中的特殊语义及其与 `raise` 的区别。

---

### 22. [NEW] `03-函数.md` 与 `03-函数基础.md` 命名混淆

**涉及文件**：
- [03-函数.md](03-函数.md)：纯索引页（hub）
- [03-函数基础.md](03-函数基础.md)：实际函数基础文档

README 中 `03-函数.md` 的标题是"函数基础"但实际上是索引页，容易造成 navigational confusion。

**建议**：将 03-函数.md 的 README 条目改为"函数总览（索引）"或重命名文件为 `03-函数索引.md`。

---

### 23. [NEW] `06a-struct.md` 中 `mut self` 与 `02-变量与绑定.md` 中 `mut` no-op 语义冲突

**涉及文件**：
- [02-变量与绑定.md](02-变量与绑定.md) §2.3：`mut` 是 no-op（"默认已是可变，`mut` 为 no-op"）
- [06a-struct.md](06a-struct.md) §3.2：`mut self` 表示可变引用 `&mut Self`

在 struct 方法中 `mut self` 有实际语义（可变引用），但在变量绑定中 `mut` 是 no-op。同一个关键字的语义在不同上下文中完全不同。

**建议**：在 02-变量与绑定.md 中补充说明 `mut` 在方法 self 参数中的特殊语义。

---

### 24. [NEW] `^` 运算符双重语义

**文件**：[12-操作符.md](12-操作符.md)

- §1.6：`^` 是位异或运算符（二元，`a ^ b`）
- §1.14：`^` 是所有权移动后缀（`x^`）

虽然二元 vs 后缀的上下文可区分，但同一符号的双重角色可能造成阅读混淆。

**建议**：在文档中明确标注这是同一符号的两种不同用途，避免用户误以为它们有关联。

---

### 25. [NEW] `06a-struct.md` `__new__` 使用 `magic` 关键字语法不清晰

**文件**：[06a-struct.md](06a-struct.md) §6.2

```lz
struct Point =
    x: f64
    y: f64
    magic __new__(x: f64, y: f64) -> Self =
        Self(x: x, y: y)
```

`magic __new__` 语法出现在 struct 体内，但 `magic` 关键字通常用于定义独立的 magic 块（见 06f-magic用法.md）。这种在 struct 内部使用 `magic` 的语法在其他文档中未充分说明。

**建议**：在 06f-magic用法.md 中补充"在 struct 内部直接定义魔法方法"的语法说明。

---

### 26. [NEW] 生成器 `def gen() -> int` 返回类型标注矛盾

**文件**：[14-生成器.md](14-生成器.md) §5.4

```lz
def gen() -> int =
    yield 1
    yield 2
    yield 3
```

如果 `-> int` 表示 yield 元素类型，那么 `yield from gen()` 在 §5.4 中递归调用自身时，`gen()` 返回的迭代器如何与 `-> int` 匹配？生成器返回的是迭代器，`-> int` 的语义需要明确。

---

### 27. [NEW] `99-内置预导入库.md` 字符串方法使用 `.to_str()` 但未定义

**文件**：[99-内置预导入库.md](99-内置预导入库.md) §2.6

```lz
// str 类型方法列表
.to_str()    // 转为字符串（通常恒等）
```

`str` 类型有 `.to_str()` 方法，但 `to_str()` 与 `str()` 全局函数、`__str__` 魔法方法的关系未说明。

---

### 28. [NEW] `04-表达式.md` 列表推导中 `for ... in` 语法未展开

**文件**：[04-表达式.md](04-表达式.md) §十六

```lz
[x * 2 for x in lst if x > 0]
[x + y for x in xs for y in ys]
```

列表推导语法仅给出两个示例，但未说明：
- 是否支持多个 `if` 过滤条件
- 推导式的变量作用域（`x` 是否泄漏到外部）
- 是否支持字典推导和集合推导

**建议**：补充列表/字典/集合推导的完整语法说明。

---

### 29. [NEW] `03e-复合综合.md` 装饰器实现状态不明确

**文件**：[03e-复合综合.md](03e-复合综合.md) §2.2

装饰器列表中的 `@overload` 标注为"预留多分派"，但未说明哪些装饰器是**已实现**的、哪些是**设计阶段**的。`@tail_call`、`@simd`、`@parallel` 等也有类似问题。

**建议**：为每个装饰器添加实现状态标注（已实现 / 设计阶段 / 预留）。

---

### 30. [NEW] `12-操作符.md` 优先级表中 `?` 和 `?.` 优先级相同但语义不同

**文件**：[12-操作符.md](12-操作符.md) §二

`?`（错误传播）和 `?.`（安全导航）在优先级 15 中并列，但 `?` 是后缀运算符而 `?.` 是成员访问前缀。两者优先级相同可能在某些表达式中产生歧义。

**建议**：确认 `x?.field?` 的求值顺序，并在文档中说明。

---

### 31. [NEW] `08b-comptime编译期.md` comptime 不支持列表推导但未说明

**文件**：[08b-comptime编译期.md](08b-comptime编译期.md) §4.2

comptime 不支持列表推导（`[x for x in xs]`），但 `04-表达式.md` 中将列表推导作为标准表达式。用户可能期望在 comptime 中使用列表推导。

**建议**：在 08b §4.2 的不支持列表中明确添加"列表推导"。

---

### 32. [NEW] `00-词法基础.md` 缺少 `Number` trait 关键字

**文件**：[00-词法基础.md](00-词法基础.md) §1.1

`03-函数基础.md` 和 `01-类型系统.md` 中使用了 `Number` trait（如 `@math` 装饰器将无注解参数默认标记为 `Number`），但 `Number` 不在 00-词法基础.md 的关键字或预导入类型列表中。

**建议**：在 00-词法基础.md 或 99-内置预导入库.md 中补充 `Number` trait 的定义。

---

### 33. [NEW] `06d-内置魔法trait.md` 缺少 `__mod__` vs `__rem__` 说明

**文件**：[06d-内置魔法trait和全局函数.md](06d-内置魔法trait和全局函数.md)

`%` 运算符映射到 `__rem__`（remainder，取余）而非 `__mod__`（modulo，取模）。Python 使用 `__mod__`，而 Rust 使用 `Rem` trait。两者的语义差异（对负数处理不同）未被说明。

**建议**：在 12-操作符.md 或 06d 中明确说明 `%` 是 remainder 语义（而非 Python 的 modulo 语义）。

---

### 34. [NEW] `05-控制流.md` `guard` 条件检查不完整

**文件**：[05-控制流.md](05-控制流.md) §7.1

`guard cond else:` 的文档说 cond 必须是 bool 表达式，但没说明 cond 是否支持 truthiness 判定链（`__bool__` → `__len__` → 默认 true），如 06g-魔法综合.md §九所述。

**建议**：在 05-控制流.md 中添加对 truthiness 判定链的交叉引用。

---

### 35. [NEW] `10-并发与异步.md` `Futures<T1, T2, ...>` 类型边界不清晰

**文件**：[10-并发与异步.md](10-并发与异步.md) §2.5

`Futures<str, str, int>` 的变长泛型参数语法在文档中仅出现一次，没有说明：
- 最大支持的参数数量
- 是否支持 0 个或 1 个参数
- 与 `List<Future<T>>` 的关系

**建议**：补充 `Futures` 的边界条件说明。

---

### 36. [NEW] `09-错误处理.md` 异常类型体系仍然不完整

**文件**：[09-错误处理.md](09-错误处理.md) §4.1

内置异常类型仅列出 `IOError` 和 `str`。但文档中多处使用了未定义的异常类型：
- `Exception`（03-函数基础.md §3.5）
- `ValueError`（14-生成器.md §2.3）
- `TypeError`（09-错误处理.md §3.3）
- `ParseError`（09-错误处理.md §4.3）

**建议**：在 09-错误处理.md 中补充标准异常类型层次结构，或在 99-内置预导入库.md 中列出。

---

## 已修复问题（24 项）

以下问题在 v5.0 报告中提出，经本轮文档更新后已修复：

| # | 问题 | 涉及文件 |
|:--|------|------|
| 1 | `::` 语法冲突 `__Params.downcast::<int>()` | 03c-检查站.md → 已改为 `downcast.<int>()` |
| 2 | `...` 抽象方法标记不在关键字列表 | 00-词法基础.md → 已添加到 §1.1 |
| 3 | `continue` 带值语义不清 | 05-控制流.md → 已补充 §6.3 |
| 4 | `for/while else` 子句缺失 | 05-控制流.md → 已补充 §4.2 |
| 5 | `lazy` 导入缺失 | 07-模块与导入.md → 已补充 §2.5 |
| 6 | `go` 关键字缺失 | 10-并发与异步.md → 已补充 §三 |
| 7 | `as` 双重语义冲突 | 01-类型系统.md → 已补充 §六 |
| 8 | `owned` 语义描述方向错误 | 02-变量与绑定.md → 已修正 |
| 9 | `with` 语句缺失 | 05-控制流.md → 已补充 §十 |
| 10 | `pass` 返回值语义不清 | 05-控制流.md → 已补充 §九 |
| 11 | `is` 类型检查缺失 | 04-表达式.md → 已补充 §4.3 |
| 12 | `len(x)` vs `x.len()` 关系不清 | 99-内置预导入库.md → 已创建 |
| 13 | `comptime def` 缺失 | 08b-comptime编译期.md → 已补充 §4.5 |
| 14 | `PartialFunc` 类型缺失 | 99-内置预导入库.md、14-生成器.md → 已补充 |
| 15 | `Futures<T1,T2,...>` 变长泛型缺失 | 10-并发与异步.md → 已补充 §2.5 |
| 16 | 装饰器文档缺失 | 03e-复合综合.md → 已补充 §2.2 |
| 17 | 属性宏列表缺失 | 08-宏与编译期.md → 已补充 §五 |
| 18 | 相对导入语法缺失 | 07-模块与导入.md → 已补充 §2.6 |
| 19 | 元组解包赋值缺失 | 02-变量与绑定.md → 已补充 |
| 20 | `Ordering`/`Formatter` 类型缺失 | 06d-内置魔法trait.md、06g-魔法综合.md → 已补充 |
| 21 | 标准库函数文档缺失 | 99-内置预导入库.md → 已创建 |
| 22 | 异常类型体系缺失 | 09-错误处理.md → 已补充 §4 |
| 23 | `in` 运算符语义不清 | 04-表达式.md → 已补充 §4.3 |
| 24 | `_` 特殊标识符文档缺失 | 00-词法基础.md → 已补充 §4.3 |

---

## 总结

本轮检查发现文档质量有明显提升（56% 的历史问题已修复），但仍有 2 个 P0 级矛盾（`await` 规则和 `self` 语义）和 10 个 P1 级问题待解决。新增的 17 个问题主要涉及**章节编号错误**、**类型状态标注矛盾**和**跨文档语义不一致**，修复成本较低。

**优先建议**：
1. 解决 P0-1（`await` 规则）和 P0-2（`self` 语义）— 直接影响用户理解
2. 统一 `Never`/`Unit`/`setup`/`teardown` 的实现状态标注（P1-10, P1-11）
3. 修复章节编号错误（P1-7, P1-8, P1-9）
4. 补充 `StopIteration` 和标准异常类型层次（P1-12, P2-36）