# LZ 语法设计最终检查报告

> 检查时间：2026-07-28
> 检查范围：`E:\IDEProjects\AI\lang-zone\SYNTAX` 全部 34 个语法文档
> 版本：v4.1（第三次完整重检，新增 7 个问题）

---

## 检查总结

| 类别 | v4.0 | 本轮新增 | 合计 |
|:----:|:--------:|:--------:|:--------:|
| P0 关键矛盾 / 阻塞问题 | 4 | **0** | **4** |
| P1 重要欠缺 / 遗漏 | 10 | **3** | **13** |
| P2 文档质量问题 | 12 | **4** | **16** |
| **合计** | **26** | **7** | **33** |

---

## 一、P0 关键问题

### 1. `self` 语义在普通方法与魔法方法中不一致

**问题描述**：
- `06a-struct.md` §3.2：普通 struct 方法中 `self`（无 `mut`）= **不可变引用 `&Self`**
- `06g-魔法综合.md` §1：魔法方法中 `self`（无 `mut`，owned 模式）= **消费所有权 `self: Self`**
- 同一语法 `self` 在不同上下文中表示相反的所有权语义

```lz
// 普通方法：self = &Self（不可变借用）
struct Point =
    x: f64
    def get_x(self) -> f64 = self.x   // self 是 &Self

// 魔法方法：self = Self（消费所有权）
magic add<T> =
    def __add__(self, other: T) -> T  // self 是 Self（owned）
```

**影响程度**：P0 严重设计不一致。同一语法不同语义，用户极易混淆。

**建议**：在 `06a-struct.md` 中明确列出所有 self 模式（`self`/`mut self`/`owned self` 等），并添加与魔法方法 self 模式的对照表。

---

### 2. `await` 使用规则自相矛盾

**问题描述**：
- `10-并发与异步.md` §1.2 明确规则："`await` 只能在 `async` 函数中使用"
- `10-并发与异步.md` §1.3 示例却展示在同步 `def main()` 中直接使用 `await`：

```lz
def main() =                 // 同步函数！
    let f1 = spawn fetch_data(10)
    let f2 = spawn fetch_data(20)
    let r1 = await f1        // ← 在同步函数中使用 await？
    let r2 = await f2
```

**影响程度**：P0 文档矛盾。用户无法确定 `await` 的正确使用场景。

**建议**：明确规则——要么 `await` 只能在 `async` 函数中使用（此时 §1.3 示例需修正），要么允许在同步函数中对 `spawn` 返回的 `Future` 使用 `await`（此时 §1.2 规则需修正）。

---

### 3. `as` 双重语义冲突（未解决，设计层面）

**问题描述**：
- `as` 同时用于 **导入别名** (`import std.io as io`) 和 **类型转换** (`x as f64`)
- 词法层面同一个关键字承担两种完全不同语义

**影响程度**：P0 严重设计冲突。

**建议方案**：
- 方案 A：保留现状，接受双重语义
- 方案 B：类型转换改用 `as!(T)` 语法，`as` 仅用于导入别名
- 方案 C：类型转换改用 `cast::<T>(x)` 全局函数

---

### 4. 异常类型体系完全未定义（未解决，文档层面）

**问题描述**：
- `09-错误处理.md` 提到 `IOError` 预导入，但标准库中缺乏完整的异常类型层次定义
- `Exception`、`ValueError`、`TypeError` 等常用异常类型在哪里定义、如何自定义异常层次，完全没有文档说明
- `raise` 可以抛出任意类型（`raise "msg"` 抛出 `str`），但最佳实践未说明

**影响程度**：P0 关键缺失。

**建议**：在 `99-内置预导入库.md` 补充标准异常类型定义。

---

## 二、P1 重要欠缺 / 遗漏

### 1. `contains(x, v)` 全局函数与 `a in b` 操作符参数顺序相反

**问题描述**：
- `99-内置预导入库.md` §2.6：`contains(x, v)` → `x.__contains__(v)`
- `12-操作符.md` §3.6：`a in b` → `b.__contains__(a)`

两个调用方式的 receiver 位置相反！`contains(xs, item)` 中 `xs` 是第一个参数，但 `item in xs` 中 `xs` 是第二个操作数。

**影响程度**：P1 语义不一致。用户可能写出 `contains(item, xs)` 的错误代码。

---

### 2. `StopIteration` 异常类型未定义

**问题描述**：
- `14-生成器.md` §2.3 使用 `raise StopIteration("limit reached")` 
- 但 `StopIteration` 在 `09-错误处理.md` §4.1 内置异常类型表中未列出
- `99-内置预导入库.md` 中也未列出

**影响程度**：P1 缺失。生成器核心异常类型无定义。

---

### 3. `with ... as` 子句语法未完整文档化

**问题描述**：
- `05-控制流.md` §10 示例使用 `with open("file.txt") as f:` 
- 但 `as` 在 `with` 语句中的绑定语义、变量作用域、类型推断规则均未说明
- `as` 在 `with` 中的语义（将 `__enter__` 返回值绑定到变量）与 `import ... as`（导入别名）和 `x as T`（类型转换）完全不同

**影响程度**：P1 文档不完整。

---

### 4. `open()` 在 prelude 列表中的定位混乱

**问题描述**：
- `99-内置预导入库.md` §2.6 将 `open(path)` 列在"常用内置函数速查"表中
- 但同一行标注"需 `import std.fs`"
- `05-控制流.md` §10 示例 `with open("file.txt") as f:` 没有 import 语句
- 用户无法确定 `open()` 是否需要 import

**影响程度**：P1 文档混乱。示例代码可能无法编译。

---

### 5. `@export(Rust)` 与 `#!export(Rust)` 被错误描述为等价

**问题描述**：
- `08-宏与编译期.md` §1.2 说 `@export(Rust)` 和 `#!export(Rust)` 等价
- 但 `@` 是函数级装饰器，`#!` 是文件级属性宏（`!` 表示应用于整个模块）
- 两者作用域完全不同，不应描述为等价

**影响程度**：P1 文档错误。误导用户。

---

### 6. 枚举变体具名字段匹配模式语法未在控制流文档中说明

**问题描述**：
- `06b-enum.md` §6 示例使用 `Shape.Circle(x: x, y: y, radius: r)` 解构语法
- 但 `05-控制流.md` §2.3 模式类型表中没有列出"具名字段变体解构"模式
- 用户不知道 `x: x` 这种解构语法是否通用，是否支持重命名（如 `x: my_x`）

**影响程度**：P1 文档缺失。关键模式匹配语法无说明。

---

### 7. 泛型方法 impl 块语法缺失（未解决）

**问题描述**：
- 当前文档只描述了泛型 struct/enum/trait/function，但**泛型方法在非泛型 impl 块中的语法**完全未文档化

**影响程度**：P1 重要缺失。

---

### 8. `Futures<T1, T2, ...>` 变参泛型未文档化（未解决）

**问题描述**：
- `10-并发与异步.md` 提到 `Futures<T1, T2, ...>` 但类型系统文档中完全没有说明

**影响程度**：P1 重要缺失。

---

### 9. `Ordering` / `Formatter` 基础类型未文档化（未解决）

**问题描述**：
- `Ordering` 用于 `__cmp__` 返回类型，`Formatter` 用于 `__str__`/`__repr__` 参数
- 这两个类型在 `99-内置预导入库.md` 中没有列出

**影响程度**：P1 重要缺失。

---

### 10. `pass` 返回值语义未说明（未解决）

**问题描述**：
- `pass` 求值为 `()`，但在需要特定返回类型的表达式上下文中是否合法未说明

**影响程度**：P1 语义不明确。

---

### 11. [NEW] `__rem__` vs `__mod__` 命名对 Python 用户造成混淆

**问题描述**：
- `%` 取模运算符映射到 `__rem__`（remainder）而非 `__mod__`（modulo）
- Python 的 `%` 对应 `__mod__`，Rust 的 `%` 是 remainder 语义
- 文档未说明为何选择 `__rem__` 而非 `__mod__`，也未说明 remainder vs modulo 的语义差异

**影响程度**：P1 命名混淆。从 Python 迁移的开发者会自然寻找 `__mod__`。

**建议**：在 `12-操作符.md` 或 `06d-内置魔法trait.md` 中明确说明 `%` 使用 remainder 语义（与 Rust 一致），并添加 `__rem__` vs `__mod__` 的说明。

---

### 12. [NEW] `list(x)` / `set(x)` / `dict(x)` 内置函数缺少文档和示例

**问题描述**：
- `99-内置预导入库.md` §2.6 列出了 `list(x)`、`set(x)`、`dict(x)` 三个内置函数
- 但全文档中没有任何示例展示它们的用法
- 语义不明确：`list(x)` 是否将任意可迭代对象转为 List？参数为空时行为如何？
- 小写 `list` vs 大写 `List` 的命名区分也不清晰

**影响程度**：P1 文档缺失。用户无法知道这些函数的存在和用法。

---

### 13. [NEW] `format` 函数与 f-string 关系未文档化

**问题描述**：
- `99-内置预导入库.md` §2.6 列出 `format(tmpl, args..)` 作为内置函数
- 但 `04-表达式.md` §2.2 主要展示 f-string 作为字符串插值方式
- 全文档中没有任何 `format()` 函数的使用示例
- 用户不知道何时用 `format()` 而非 f-string

**影响程度**：P1 文档缺失。内置函数存在但完全未展示。

---

## 三、P2 文档质量问题

### 1. `setup`/`teardown` 设计状态矛盾

**问题描述**：
- `00-词法基础.md` §1.9 标注 `setup`/`teardown` 为"设计阶段，编译器未实现"
- `15-测试框架.md` §3 完整展示其用法，无任何"设计阶段"警告

**影响程度**：P2 文档矛盾。用户无法判断该功能是否可用。

---

### 2. `Never`/`Unit`/`Nil` 设计状态矛盾

**问题描述**：
- `00-词法基础.md` §1.11 标注 `Never`、`Unit`、`Nil` 为"设计阶段"
- `01-类型系统.md` §1.2 将 `Unit` 和 `Never` 作为有效类型，给出完整示例
- 两处对同一类型的可用性描述矛盾

**影响程度**：P2 文档矛盾。

---

### 3. `go` 关键字设计状态矛盾

**问题描述**：
- `00-词法基础.md` §1.4 标注 `go` 为"设计阶段，编译器未实现"
- `10-并发与异步.md` §3 完整展示 `go` 语法（内联、赋值、块语法），无"设计阶段"警告

**影响程度**：P2 文档矛盾。

---

### 4. `PartialFunc` 设计状态矛盾

**问题描述**：
- `99-内置预导入库.md` §3.6 标注 `PartialFunc` 为"设计阶段"
- `14-生成器.md` §4 完整展示其定义和使用，无"设计阶段"警告

**影响程度**：P2 文档矛盾。

---

### 5. `__slots__` 在 struct 文档中缺失

**问题描述**：
- `06e-模块级魔法属性.md` §6 详细说明 `__slots__` 控制 struct 内存布局
- 但 `06a-struct.md` 完全没有提及 `__slots__` 属性
- 用户看 struct 文档时不会知道有这个功能

**影响程度**：P2 文档不完整。功能存在但入口文档未提及。

---

### 6. `int(x, base)` 函数签名未详细说明

**问题描述**：
- `99-内置预导入库.md` §2.6 列出 `int(x, base)` 但未说明：
  - `base` 的有效范围是多少？
  - `x` 必须是 `str` 还是可以其他类型？
  - 解析失败时行为（panic？返回 Result？）

**影响程度**：P2 文档不完整。

---

### 7. `::` 废弃说明不一致

**问题描述**：
- `00-词法基础.md` §1.12：`::` "词法分析器识别但语法层已废弃"（即词法层仍接受）
- `07-模块与导入.md` §5：`::` "已废弃，产生词法错误"（即词法层就拒绝）

两处对 `::` 的处理层级描述矛盾。

**影响程度**：P2 细节不一致。

---

### 8. `Tuple` vs `tuple` 拼写不一致

**问题描述**：
- `03d-可变参数.md` §2.4 使用 `tuple(str, int)` 和 `tuple(T)` 标注 `..` 类型
- `99-内置预导入库.md` §2.0 使用 `Tuple(A,B)` 作为类型名
- 同一概念两种拼写，`tuple` vs `Tuple`

**影响程度**：P2 细节不一致。

---

### 9. `let mut` 语义仍然混乱不一致（未解决）

**问题描述**：
- 顶层绑定：`mut x = 42` ⇒ 等价于 `x = 42`（no-op）
- 参数修饰：`mut ref x: T` ⇒ `&mut T`（有效语义）
- `mut` 在不同上下文语义不同

**影响程度**：P2 设计不一致。

---

### 10. 装饰器 (`@overload`, `@tail_call` 等) 缺乏详细说明（未解决）

**问题描述**：
- `03e-复合综合.md` 列出了内置装饰器表格，但 `@overload` 多分派如何工作完全没有说明

**影响程度**：P2 文档不完整。

---

### 11. 属性宏 `#!no_std` 等缺乏说明（未解决）

**问题描述**：
- `08-宏与编译期.md` 只在表格中列出 `#!no_std`，但没有说明作用和用法

**影响程度**：P2 文档不完整。

---

### 12. [NEW] `= ...` 语法重载（抽象方法 vs 默认参数值）

**问题描述**：
- `06c-trait和impl.md` §2：`def method() = ...` 表示抽象方法（无默认实现）
- `06g-魔法综合.md` §8.3：`def server(cfg: Config = ...)` 表示参数默认值由 `__implicit_default__` 提供
- 同一个 `= ...` 语法在不同上下文有完全不同的语义：
  - trait 中 → 抽象方法标记
  - 函数参数中 → 隐式默认值标记

**影响程度**：P2 语法重载。用户可能混淆两种 `...` 用法。

**建议**：在 `00-词法基础.md` 中明确列出 `...` 的所有语义，或在函数参数默认值场景使用不同的语法标记。

---

### 13. [NEW] `__iter__` 与 `__into_iter__` 区分不清

**问题描述**：
- `06d-内置魔法trait和全局函数.md` §8 将 `__iter__` 和 `__into_iter__` 都映射到 `IntoIterator`
- 但两者的语义理应不同：`__iter__` 是借用迭代（`&self` → Iterator），`__into_iter__` 是消费迭代（`self` → Iterator）
- 文档未说明何时应实现 `__iter__` vs `__into_iter__`

**影响程度**：P2 文档不清晰。两个魔法方法映射到同一个 Rust trait 但语义不同。

---

### 14. [NEW] `IterStopException`(Rust) 与 `StopIteration`(LZ) 命名脱节

**问题描述**：
- `99-内置预导入库.md` §3.1 使用 Rust 类型 `IterStopException` 作为生成器构建块终止信号
- `14-生成器.md` §2.3 使用 LZ 层名称 `StopIteration` 作为异常类型
- 两个名称之间的关系（是否映射、如何映射）未在任何文档中说明

**影响程度**：P2 命名不一致。同一概念两个名字，用户困惑。

---

### 15. [NEW] `str` 类型方法的所有权语义不明确

**问题描述**：
- `99-内置预导入库.md` §2.6 列出 `str` 类型的方法如 `.length()`、`.starts_with()`、`.contains()` 等
- 但未说明这些方法是在 `String`（owned）还是 `&str`（borrowed）上调用
- 在 Rust 中，`String` 和 `&str` 的方法集不同，LZ 的 `str` 映射到 `String` 但方法列表包含了 `&str` 的方法

**影响程度**：P2 文档不清晰。

---

### 16. [NEW] `__ne__` 被列为独立魔法方法但与 `__eq__` 自动派生关系不明确

**问题描述**：
- `06d-内置魔法trait和全局函数.md` 速查索引中将 `__ne__` 列为独立魔法方法
- `12-操作符.md` §3.3 说 `__ne__` 由 `__eq__` 自动派生
- 用户看到 `__ne__` 被列为独立条目可能会尝试手动实现它，但实际只需实现 `__eq__`

**影响程度**：P2 文档不一致。`__ne__` 应标注为"自动派生"而非独立魔法方法。

---

## 四、已解决问题回顾

### 已解决的 P0 问题：
1. **`::` 语法冲突** ✅ — 文档已更正为 `.` 语法
2. **缺少内置函数系统文档** ✅ — 新增 `99-内置预导入库.md`

### 已解决的 P1 问题（16 项）：
`continue` 带值语义、`with` 与魔法方法关联、`is` 类型判断、`len(x)` vs `x.len()`、`comptime def`、元组解包、`PartialFunc` 类型、相对导入、`...` 抽象方法标记等。

### 已解决的 P2 问题（15 项）：
运算符优先级表一致性、`owend` 拼写、`for/while else`、`lazy` 导入、`go` 关键字等。

---

## 五、一致性检查结论

### 语法规则一致性

| 检查项 | 状态 |
|--------|------|
| 冒号后必须换行（if/while/for/guard/match/comptime） | ✅ 一致 |
| 所有文档链接 | ✅ 都正确 |
| 关键字列表全覆盖 | ✅ 无遗漏 |
| 魔法方法与操作符映射 | ✅ 一致 |
| 路径分隔符统一用 `.` | ✅ 一致 |

### 设计一致性评分

- **冒号换行规则**：✅ 完全一致
- **`self` 语义**：❌ 普通方法与魔法方法不一致（P0 #1）
- **`await` 规则**：❌ 文档内部自相矛盾（P0 #2）
- **设计阶段标注**：❌ 4 处矛盾（`setup`/`teardown`、`Never`/`Unit`/`Nil`、`go`、`PartialFunc`）
- **`and`/`or` 与 `&&`/`||`**：功能重复但可接受
- **`*`/`&` 语义与 C/Rust 相反**：文档已明确标注，可接受

---

## 六、最终统计

| 优先级 | 数量 | 状态 |
|:------:|:----:|:------|
| P0 | 4 | 待解决 |
| P1 | 13 | 待解决 |
| P2 | 16 | 待解决 |
| **总计** | **33** | **待解决** |

### 本轮新增 7 个问题（3 P1 + 4 P2）：

**P1 新增**：
1. `__rem__` vs `__mod__` 命名对 Python 用户造成混淆
2. `list(x)` / `set(x)` / `dict(x)` 内置函数缺少文档和示例
3. `format` 函数与 f-string 关系未文档化

**P2 新增**：
4. `= ...` 语法重载（抽象方法 vs 默认参数值）
5. `__iter__` 与 `__into_iter__` 区分不清
6. `IterStopException`(Rust) vs `StopIteration`(LZ) 命名脱节
7. `str` 类型方法的所有权语义不明确
8. `__ne__` 被列为独立魔法方法但与 `__eq__` 自动派生关系不明确

### 问题分布

| 文档 | 涉及问题数 |
|------|:--------:|
| `99-内置预导入库.md` | 10 |
| `10-并发与异步.md` | 4 |
| `06a-struct.md` / `06g-魔法综合.md` (self) | 3 |
| `00-词法基础.md` | 4 |
| `05-控制流.md` | 2 |
| `08-宏与编译期.md` | 2 |
| `12-操作符.md` | 2 |
| `06d-内置魔法trait.md` | 2 |
| 其他文档 | 4 |