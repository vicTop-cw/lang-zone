# Generated by lzcyc — LZ → Cython compiler
# cython: language_level=3
# cython: boundscheck=False
# cython: wraparound=False
# cython: cdivision=True
# cython: initializedcheck=False
# cython: nonecheck=False

import cython

class _Moved:
    """Ownership sentinel: access after move raises RuntimeError"""
    def __getattr__(self, name): raise RuntimeError(f"access to moved value: {name}")
    def __setattr__(self, name, v): raise RuntimeError("assign to moved value")
    def __bool__(self): raise RuntimeError("use of moved value")
    def __repr__(self): return "<_Moved>"

_MOVED = _Moved()
__name__ = "guard"
__file__ = "../DEMO/06_control_flow/guard.lz"
__all__ = ["main"]

def main():
    def safe_div(a: int, b: int) -> int:
        if not (b != 0):
            return 0
        return a / b

    print(safe_div(10, 2))
    print(safe_div(10, 0))
    def safe_div2(a: int, b: int) -> int:
        if not (b != 0):
            return 0
        return a / b

    print(safe_div2(10, 2))
    print(safe_div2(10, 0))
    def process(val: Optional[int]) -> str:
        _pat_ = val
        if not _pat_:
            return "empty"
        return f"value: {n}"

    print(process(Some(42)))
    print(process(None))
    opt_name = Some("Alice")  # type: object
    _pat_ = opt_name
    if not _pat_:
        print("no name")
        return
    print(f"hello {name}")
