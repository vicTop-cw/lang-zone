# Generated by lzcyc — LZ → Cython compiler
# cython: language_level=3
# cython: boundscheck=False
# cython: wraparound=False
# cython: cdivision=True
# cython: initializedcheck=False
# cython: nonecheck=False

import cython

class _Moved:
    """Ownership sentinel: access after move raises RuntimeError"""
    def __getattr__(self, name): raise RuntimeError(f"access to moved value: {name}")
    def __setattr__(self, name, v): raise RuntimeError("assign to moved value")
    def __bool__(self): raise RuntimeError("use of moved value")
    def __repr__(self): return "<_Moved>"

_MOVED = _Moved()
__name__ = "test_functions"
__file__ = "E:\IDEProjects\AI\lang-zone\CY\TESTS\99_self_test\test_functions.lz"
__all__ = ["double", "main"]

cpdef Py_ssize_t double(x: int):
    return x * 2

def main():
    x = 42  # type: Py_ssize_t
    assert x == 42
    y = 10
    y += 5
    assert y == 15
    assert double(5) == 10
    assert double(0) == 0
    assert double(-3) == -6
    def greet(name: str = "world") -> str:
        return f"Hello, {name}"

    assert greet() == "Hello, world"
    assert greet("LZ") == "Hello, LZ"
    def max(a: int, b: int) -> int:
        if a > b:
            return a
        else:
            return b

    assert max(3, 7) == 7
    assert max(10, 5) == 10
    def abs(n: int) -> int:
        if n < 0:
            return -n
        return n

    assert abs(5) == 5
    assert abs(-5) == 5
    print("✅ 所有变量和函数测试通过")
