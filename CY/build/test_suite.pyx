# Generated by lzcyc — LZ → Cython compiler
# cython: language_level=3
# cython: boundscheck=False
# cython: wraparound=False
# cython: cdivision=True
# cython: initializedcheck=False
# cython: nonecheck=False

import cython

class _Moved:
    """Ownership sentinel: access after move raises RuntimeError"""
    def __getattr__(self, name): raise RuntimeError(f"access to moved value: {name}")
    def __setattr__(self, name, v): raise RuntimeError("assign to moved value")
    def __bool__(self): raise RuntimeError("use of moved value")
    def __repr__(self): return "<_Moved>"

_MOVED = _Moved()
__name__ = "test_suite"
__file__ = "E:\IDEProjects\AI\lang-zone\CY\TESTS\99_self_test\test_suite.lz"
__all__ = ["main"]

def main():
    print("✅ 所有综合测试通过")

# suite: lz_self_test
def test_arithmetic_basics():
    assert 1 + 1 == 2
    assert 5 * 3 == 15
    if not (10 / 2):
        print("CHECK: check failed: 10 / 2")

def test_string_operations():
    s = "hello"
    assert s == "hello"
    fs = f"value: {42}"
    assert fs == "value: 42"

def test_list_operations():
    xs = [1, 2, 3]
    assert xs == [1, 2, 3]
    assert xs[0] == 1

def test_function_basics():
    def add(a: int, b: int) -> int:
        return a + b

    assert add(1, 2) == 3
