# Generated by lzcyc — LZ → Cython compiler
# cython: language_level=3
# cython: boundscheck=False
# cython: wraparound=False
# cython: cdivision=True
# cython: initializedcheck=False
# cython: nonecheck=False

import cython

class _Moved:
    """Ownership sentinel: access after move raises RuntimeError"""
    def __getattr__(self, name): raise RuntimeError(f"access to moved value: {name}")
    def __setattr__(self, name, v): raise RuntimeError("assign to moved value")
    def __bool__(self): raise RuntimeError("use of moved value")
    def __repr__(self): return "<_Moved>"

_MOVED = _Moved()
__name__ = "basic"
__file__ = "../DEMO/04_functions/basic.lz"
__all__ = ["add", "double", "greet", "max", "multiply", "sq", "read_data", "modify", "greet_default", "is_positive", "inferred_double", "main", "log", "abort", "early_exit", "divide"]

cpdef Py_ssize_t add(a: int, b: int):
    return a + b

def double(x):
    x * 2

def greet(name: str) -> str:
    return f"Hello, {name}"

def max(a: int, b: int) -> int:
    if a > b:
        return a
    else:
        return b

def multiply(a: int, b: int) -> int:
    return a * b

def sq(x, y):
    x * x + y * y

def read_data(data: list[int]) -> int:
    return data.len()

def modify(data: list[int]):
    data.push(42)

def greet_default(name: str = "world") -> str:
    return f"Hello, {name}"

def is_positive(x: int) -> bool:
    return x > 0

def inferred_double(x):
    x * 2

def main():
    print("=== basic.lz ===")
    print(add(3, 5))
    print(double(7))
    print(greet("LZ"))
    print(max(10, 20))
    print(multiply(4, 6))
    print(sq(3, 4))
    print(sq(3.0, 4.0))
    print(read_data([1, 2, 3]))
    print(greet_default())
    print(greet_default("LZ"))
    print(is_positive(-1))
    print(inferred_double(5))
    print(divide(10, 2))
    print(early_exit(-5))
    print(early_exit(3))

def log(msg: str) -> ():
    return print(msg)

def abort(msg: str) -> NoReturn:
    raise Exception(msg)

def early_exit(x: int) -> int:
    if x < 0:
        return 0
    return x + 1

def divide(a: int, b: int) -> int:
    if b == 0:
        raise "division by zero"
    return a / b
