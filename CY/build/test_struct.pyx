# Generated by lzcyc — LZ → Cython compiler
# cython: language_level=3
# cython: boundscheck=False
# cython: wraparound=False
# cython: cdivision=True
# cython: initializedcheck=False
# cython: nonecheck=False

import cython

class _Moved:
    """Ownership sentinel: access after move raises RuntimeError"""
    def __getattr__(self, name): raise RuntimeError(f"access to moved value: {name}")
    def __setattr__(self, name, v): raise RuntimeError("assign to moved value")
    def __bool__(self): raise RuntimeError("use of moved value")
    def __repr__(self): return "<_Moved>"

_MOVED = _Moved()
__name__ = "test_struct"
__file__ = "E:\IDEProjects\AI\lang-zone\CY\TESTS\99_self_test\test_struct.lz"
__all__ = ["main", "Point", "Color"]

cdef class Point:
    cdef public double x
    cdef public double y
    def __init__(self, double x, double y):
        self.x = x
        self.y = y
    cpdef double area(self):
        return self.x * self.y

    @staticmethod
    def new(x: float, y: float) -> object:
        return Point(x = x, y = y)


class Color:
    def __init__(self, variant):
        self._variant = variant

Color.Red = Color(0)
Color.Green = Color(1)
Color.Blue = Color(2)

# impl Point (methods injected into class)
def main():
    p = Point(x = 3.0, y = 4.0)
    assert p.area() == 12.0
    p2 = Point.new(5.0, 6.0)
    assert p2.area() == 30.0
    c = Color.Red
    assert c._variant == 0
    g = Color.Green
    assert g._variant == 1
    b = Color.Blue
    assert b._variant == 2
    squares = [x * x for x in range(0, 5) if x > 0]
    assert squares == [1, 4, 9, 16]
    def describe(n: int) -> str:
        return (lambda __v: "zero" if __v == 0 else "one" if __v == 1 else "other")(n)

    assert describe(0) == "zero"
    assert describe(1) == "one"
    assert describe(5) == "other"
    print("✅ 所有数据结构测试通过")
