# Generated by lzcyc — LZ → Cython compiler
# cython: language_level=3
# cython: boundscheck=False
# cython: wraparound=False
# cython: cdivision=True
# cython: initializedcheck=False
# cython: nonecheck=False

import cython

class _Moved:
    """Ownership sentinel: access after move raises RuntimeError"""
    def __getattr__(self, name): raise RuntimeError(f"access to moved value: {name}")
    def __setattr__(self, name, v): raise RuntimeError("assign to moved value")
    def __bool__(self): raise RuntimeError("use of moved value")
    def __repr__(self): return "<_Moved>"

_MOVED = _Moved()
__name__ = "with_defer"
__file__ = "../DEMO/06_control_flow/with_defer.lz"
__all__ = ["main", "MyResource"]

cdef class MyResource:
    cdef public str name
    def __init__(self, str name):
        self.name = name
    cpdef object __enter__(self):
        print(f"acquiring {self.name}")
        return self

    def __exit__(self):
        print(f"releasing {self.name}")


def main():
    try:
        with MyResource(name = "database"):
            print(f"using {res.name}")
        with print("standalone with block"):
            print("inside standalone with")
        def process_file():
            try:
                file = "data.txt"
                print(f"processing {file}")
            finally:
                print(f"closing {file}")

        process_file()
        if True:
            pass
        else:
            print("unreachable")
        (lambda __v: print("zero") if __v == 0 else None)(42)
        print("with and defer demonstrated")
    finally:
        print("cleanup step B")
        print("cleanup step C")
        print("cleanup step A")
