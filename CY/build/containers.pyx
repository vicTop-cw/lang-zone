# Generated by lzcyc — LZ → Cython compiler
# cython: language_level=3
# cython: boundscheck=False
# cython: wraparound=False
# cython: cdivision=True
# cython: initializedcheck=False
# cython: nonecheck=False

import cython

class _Moved:
    """Ownership sentinel: access after move raises RuntimeError"""
    def __getattr__(self, name): raise RuntimeError(f"access to moved value: {name}")
    def __setattr__(self, name, v): raise RuntimeError("assign to moved value")
    def __bool__(self): raise RuntimeError("use of moved value")
    def __repr__(self): return "<_Moved>"

_MOVED = _Moved()
__name__ = "containers"
__file__ = "../DEMO/02_types/containers.lz"
__all__ = ["main"]

def main():
    nums = [1, 2, 3]  # type: object
    nums.push(4)
    nums.push(5)
    first = nums[0]
    len = nums.length()
    popped = nums.pop()
    print(f"list: {nums}, first: {first}, len: {len}, popped: {popped}")
    inferred_nums = [10, 20, 30]
    mixed = [1, 2, 3]
    ages = {"alice": 30, "bob": 25}  # type: object
    ages["charlie"] = 35
    alice_age = ages["alice"]
    print(f"alice is {alice_age}")
    inferred_ages = {"alice": 30, "bob": 25}
    scores = {"math": 95.5, "physics": 88.0}  # type: object
    scores["chemistry"] = 91.2
    math_score = scores["math"]
    print(f"math: {math_score}")
    tags = {"rust", "compiler", "lz"}  # type: object
    tags.insert("demo")
    has_rust = tags.contains("rust")
    has_java = tags.contains("java")
    print(f"has rust: {has_rust}, has java: {has_java}")
    inferred_tags = {"a", "b", "c"}
    numbers = {1, 2, 3, 4, 5}  # type: object
    numbers.insert(6)
    has_three = numbers.contains(3)
    print(f"set has 3: {has_three}")
    annotated_list = [1.1, 2.2, 3.3]  # type: object
    annotated_dict = {1: "one", 2: "two"}  # type: object
    annotated_set = {True, False}  # type: object
    print(annotated_list, annotated_dict, annotated_set)
