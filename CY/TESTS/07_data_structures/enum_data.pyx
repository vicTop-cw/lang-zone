# Generated by lzcyc — LZ → Cython compiler
# cython: language_level=3
# cython: boundscheck=False
# cython: wraparound=False
# cython: cdivision=True
# cython: initializedcheck=False
# cython: nonecheck=False

import cython

class _Moved:
    """Ownership sentinel: access after move raises RuntimeError"""
    def __getattr__(self, name): raise RuntimeError(f"access to moved value: {name}")
    def __setattr__(self, name, v): raise RuntimeError("assign to moved value")
    def __bool__(self): raise RuntimeError("use of moved value")
    def __repr__(self): return "<_Moved>"

_MOVED = _Moved()

def _MovedCheck(v):
   """Check if value has been moved; raise RuntimeError if so."""
   if isinstance(v, _Moved):
       raise RuntimeError("use of moved value")
   return v
__name__ = "enum_data"
__file__ = "TESTS/07_data_structures/enum_data.lz"
__all__ = ["main", "Shape"]

class Shape:
    def __init__(self, variant, data = None):
        self._variant = variant
        self._data = data

Shape.Circle = lambda **kw: Shape(0, (p0, p1, p2))
Shape.Rect = lambda **kw: Shape(1, (p0, p1))
Shape.Point = Shape(2)

def main():
    c = Shape.Circle(0.0, 0.0, 5.0)
    r = Shape.Rect(10.0, 20.0)
    p = Shape.Point()
    print(c)
    print(r)
    print(p)
