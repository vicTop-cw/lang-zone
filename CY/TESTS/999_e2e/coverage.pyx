# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "coverage"
__file__ = "coverage.lz"
__all__ = ["Point", "add", "compute", "loops", "arithmetic", "use_point", "main"]

cdef class Point:
    cdef public int x
    cdef public int y
    def __init__(self, int x, int y):        self.x = x
        self.y = y

cdef int add(int a, int b):
    a + b

cdef int compute(int a):
    b = a + 1
    c = a * b
    (a if a > 0 else -a)

cdef void loops():
    for i in Range(start = 0, end = 5):    
        print(i)

cdef int arithmetic(int a, int b):
    a + b - a * b / 2

cdef int use_point(int x, int y):
    p = Point(_KwArg(name = "x", value = x), _KwArg(name = "y", value = y))
    p.x + p.y

cdef object main():
    r = compute(10)
    print(r)
    print(arithmetic(3, 4))
    print(use_point(5, 6))

