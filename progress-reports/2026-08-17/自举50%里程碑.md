# 自举 50% 里程碑进度报告（2026-08-17，v159）

> 本报告归档于 progress-reports/2026-08-17/，只增不删不改。口径与台账见 bootstrap/05-自举里程碑台账.md。

## 一、结论

- **50% 里程碑达成**：40% 已计权重 49%（含 G1 缓冲）+ C2（IR 构建 .lz 化）6% + C3（diff 对照自动化）6% = **61% ≥ 50%**。
- **自举路径可运行**：`--emit=ir-lz` 由 LZ 自身完成端到端处理（LZ 写 display 逻辑 → lzc 转译 → rustc → 运行），输出与 Rust 参照实现 `--emit=ir` 逐字符一致。
- **既有功能不回退**：cargo test 全量 319/0（40% 基线 314 +5）；DEMO 全量 261/261；bootstrap closed 闭环退出码 0。

## 二、C2 完成内容

- `src/ir/lz_ir_lib.lz` 与 `src/ir/display.rs` 逐字符对齐：Expr `[ty]` 前缀、FnDef body 缩进式、Block `{ [ty] }` 展示、StructDef/EnumDef 泛型签名。
- `src/ir/lz_codegen.rs` 构造代码同步：`BlockIR.Block`、`Option.Some`、`print_str` 切换、`StructDef` generics。
- 修复三个阻断缺陷：E0369（struct 缺 PartialEq → BlockIR 改单变体 enum）、E0308（print_str 尾表达式 → main 尾部 return 0）、UTF-8 BOM 解析失败。

## 三、C3 完成内容

- `bootstrap/work/lz_ir/diff_ir.ps1`：默认 8 个关键 DEMO 输入集，Start-Process 纯净捕获 stdout，退出码 0/1/2/3。
- `tests/lz_ir_bootstrap.rs::lz_emit_ir_lz_matches_ir_byte_exact`：cargo test 中逐字节断言两路输出相等。

## 四、验证证据

| 验证项 | 结果 | 证据 |
|---|---|---|
| cargo test 全量 | 319/0 | 2026-08-17 11:15 运行输出 |
| DEMO 全量回归 | 261/261 | PowerShell 等价回归（regression.sh 因 WSL 损坏不可用，见异常记录） |
| bootstrap closed 闭环 | 退出码 0 | `bootstrap/work/log/20260817-*.log`，两轮 manifest 一致 + PROMOTE |
| 双路 diff | 8/8 逐字符一致 | `powershell -File bootstrap\work\lz_ir\diff_ir.ps1` 退出码 0 |

## 五、异常记录

1. **WSL bash 损坏**：本机 `bash` 为 WSL bash，WSL 状态异常（无法访问 localhost），`div-tools/regression.sh` 只扫到 2 个文件。改用 PowerShell 等价脚本完成 DEMO 全量回归（逻辑一致：lzc → rustc → 运行，import 文件加 --project，排除 99_errors）。回归脚本本身未修改。
2. **link.exe 1104（瞬时）**：DEMO 全量回归中 `99_spec\extractor_unapply.lz` 一次链接失败（exit code 1104，文件锁），单独重跑通过。判定为瞬时基础设施错误，非编译器回归。
3. **closed 闭环日志拷贝文件锁**：首次跑 closed 时 build.ps1 尾部 Copy-Item latest.log 报"系统无法访问此文件"（日志句柄被占用），闭环本体（两轮 manifest 一致 + PROMOTE）已通过；重跑一次退出码 0。

## 六、后续计划（50% → 100%，剩 39%）

见 bootstrap/06-自举交接续推文档.md §2：G2 覆盖率（4%）、D1 codegen 关键路径 .lz 化（15%）、D2 全量行为一致（10%）、E1/E2 builtins .lz 化（10%）、G1 用例补全（4%）。
