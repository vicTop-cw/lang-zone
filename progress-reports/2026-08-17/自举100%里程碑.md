# 自举 100% 里程碑进度报告（2026-08-17，v160）

> 本报告归档于 progress-reports/2026-08-17/，只增不删不改。口径与台账见 bootstrap/05-自举里程碑台账.md。

## 一、结论

- **100% 里程碑达成**：50% 已计 61% + D1（codegen 关键路径 .lz 化）15% + D2（全量行为一致）10% + E1（builtins 核心子集 .lz 化）6% + E2（生成 .rs 并入 crate）4% + G2（覆盖率 ≥70%）4% = **100%**。
- **自举路径可运行**：`--emit=rs-lz` 由 LZ 自身完成 codegen（IR → Rust 源码），输出与 Rust 版 codegen 产物逐字符一致。
- **既有功能不回退**：cargo test 全量 320/0（50% 基线 319 +1）；DEMO 全量 261/261；bootstrap closed 闭环 13/13 RC=0。

## 二、D1 完成内容（15%）

- `src/ir/lz_codegen_lib.lz`：LZ 版 codegen（rust_type 映射/表达式/语句/Item/模块组装/f-string 模板解析/Range/Dict/Set/HashMap 索引 get/insert 形态/contains 参数剥离/while-true→loop/字符串转义/魔法常量 &str 渲染/f64 字面量 .0 后缀/force_ty 空容器强制标注）
- `src/ir/lz_codegen.rs`：`ir_module_to_rs_lz_source` + `LZ_CODEGEN_LIB`
- `src/main.rs`：`--emit=rs-lz` 递归管线（lzc→rustc→run）
- `tests/lz_ir_bootstrap.rs`：`lz_emit_rs_lz_matches_native_codegen` 逐字节断言 + rustc 编译 + 运行

## 三、D2 完成内容（10%）

- `bootstrap/work/lz_codegen/diff_rs.ps1`：默认 8 DEMO 输入集，退出码 0/1/2/3
- 实测逐字符一致 + 编译运行通过：literals/containers/const/ternary/compound_assign_more/comments/literals_more/const_more + d1_probe 探针（9 个）
- **已知缺口**（不伪装覆盖）：impl 块生成、列表推导、生成器/闭包/ref 绑定、match 模式、__lz_main 重命名

## 四、E1/E2 完成内容（10%）

- `bootstrap/work/lz_builtins/core_subset.lz`：8 个纯函数（lz_all/any/count_if/sum_ints/join_words/ends_with/abs/clamp）
- `lz_builtins/src/runtime/lz_bootstrap_builtins.rs`：生成 .rs 并入 crate，8 个单测全绿
- 3 个 LZ 编译器缺陷以规避写法绕过（str 切片 codegen、bool 提前返回、内联标注），记录于台账 05 §7 v4

## 五、G2 完成内容（4%）

- 覆盖映射表扩展至 15/15（06 §6），关键路径命中率 100% ≥ 70%

## 六、验证证据

| 验证项 | 结果 | 证据 |
|---|---|---|
| cargo test 全量 | 320/0 | 2026-08-17 13:00 运行输出 |
| DEMO 全量回归 | 261/261 | PowerShell 等价回归（regression.sh 因 WSL 损坏不可用） |
| bootstrap closed 闭环 | RC=0 | 13/13 两轮 manifest 一致 + PROMOTE |
| D1 双路 diff | 逐字符一致 | `lz_emit_rs_lz_matches_native_codegen` 通过 |
| D2 双路 diff | 9/9 逐字符一致 | `diff_rs.ps1` 实测 |

## 七、异常记录

1. **closed 闭环首跑污染**：E1 worker 遗留的 4 个语法探针 .lz（probe2/probe3/probe4/probe_syntax）与 50% 里程碑遗留的 ir_compare2/3.lzlz 使闭环扫描到无法编译/多余的文件（14/17、runout 残留不一致）。按项目清洁规范删除探针与陈旧产物后重跑 RC=0。
2. **WSL bash 损坏**：DEMO 全量回归沿用 PowerShell 等价脚本（与 50% 里程碑一致）。
3. **集成测试编码损坏**：首次用 PowerShell Add-Content 追加测试草稿导致中文注释双重编码损坏（编译失败），改用字节级追加后修复。

## 八、后续计划（100% 后维护）

见 bootstrap/06-自举交接续推文档.md §2：D2 缺口补齐（impl 块/列表推导/生成器/match 模式）、LZ 编译器缺陷修复、覆盖率行级报告、全量 DEMO 覆盖。
