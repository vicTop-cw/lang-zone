# 2026-08-15 进度报告 02 · 文件规模核查（3000 行铁律）

> 本报告遵循「只增不删不改」铁律；对应 tnr 建议 ③（单文件超 3000 行 = 规模超标，触发即拆）。

---

## 一、核查结果（2026-08-15，src/*.rs 全量）

| 文件 | 行数 | 状态 |
|---|---|---|
| `src/ir/codegen.rs` | **9556** | 🔴 超线（318%） |
| `src/ir/builder.rs` | **7807** | 🔴 超线（260%） |
| `src/typer/mod.rs` | 2635 | 🟡 接近（88%） |
| `src/parser/parser.rs` | 2598 | 🟡 接近（87%） |
| `src/macros/expand.rs` | 1777 | 🟢 |
| `src/parser/expr.rs` | 1506 | 🟢 |
| `src/parser/stmt.rs` | 1449 | 🟢 |
| `src/ir/duck_check.rs` | 1375 | 🟢 |
| 其余（bridge/std/comptime/interp/lexer/node…） | <1300 | 🟢 |

**结论**：2 个文件超线（codegen.rs 9556、builder.rs 7807），2 个接近（typer 2635、parser 2598）。

## 二、风险登记

1. **codegen.rs（9556 行）**：IR→Rust 代码生成核心，自举试点（v162-v165）连续暴露并修复
   7 处缺陷全部集中于此——超线后增量修改语义耦合风险高（改错会被自举回归门禁拦，但返工成本大）。
2. **builder.rs（7807 行）**：AST→IR 转换 + 类型推断 + comptime 求值入口，职责已混合。
3. **自举试点新文件**：`lz_codegen.rs`（~660 行）、`lz_ir_lib.lz`（~390 行）均远低于阈值，🟢。

## 三、拆分建议（登记，未实施——拆分属结构性重构，需单独排期）

| 文件 | 建议拆分 |
|---|---|
| `src/ir/codegen.rs` | 按生成域拆 `mod.rs` 入口 + 子模块：`expr.rs`（表达式渲染）、`stmt.rs`（语句渲染）、`item.rs`（Item/模块渲染）、`types.rs`（IrType→Rust 类型映射）、`helpers.rs`（collect_var_refs 等辅助） |
| `src/ir/builder.rs` | 按转换域拆：`items.rs`（Item 转换）、`expr.rs`（表达式转换）、`stmt.rs`（语句转换）、`infer.rs`（类型推断）、`comptime.rs`（comptime 求值入口） |

> 拆分时机建议：自举路线 B 下一里程碑（词法/语法前端 LZ 化）之前完成 codegen.rs 拆分，
> 避免在超线文件上继续叠加新生成逻辑（--emit=ir-lz 已证明自举会在生成路径频繁暴露缺陷）。
