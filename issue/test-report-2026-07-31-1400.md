# 测试报告：Lang-Zone 全面测试 — 2026-07-31 14:00

**测试工程师**: 高级测试工程师（自动化）
**日期**: 2026-07-31 14:00 CST
**环境**: Windows 10, Rust 1.89.0, cargo test (debug profile)
**范围**: 全项目功能测试 + 回归验证 + Bug 发现 + 文档审计

---

## 一、测试结果总览

| 测试套件 | 结果 | 通过/总计 |
|----------|:--:|-----------|
| cargo build | ✅ | 0 errors, 0 warnings |
| cargo test --lib | ✅ | 292/292 |
| cargo test --test compile_demos | ✅ | 1 harness (114 DEMO) |
| cargo test --test ir_snapshots | ✅ | 8/8 |
| cargo test --test mod | ✅ | 10/10 |
| cargo test --test reject_errors | ✅ | 1 harness (15/15 负例) |
| cargo test --doc | ✅ | 1/1 |

**结论: 313 个测试函数全部通过，0 失败。build 0 errors 0 warnings。**

---

## 二、P0 Bug 回归验证

### ✅ Bug #O1: 数字字面量静默变 0 — 已验证修复
- **来源**: `issue/fixed/lexer-literal-silent-zero.md`
- **验证**: 编译器实测 `0xG`、`99999999999999999999`、`123e` 均正确报错
```text
0xG          → Parse error: 无效的十六进制数字: 0x
999...       → Parse error: 无效的整数（可能溢出）: 99999999999999999999
123e         → Parse error: 科学计数法缺少指数: 123e
```

### ✅ Bug #O2: 未终止字符串字面量 — 已验证修复
- **来源**: `issue/findings-2026-07-31.md` P0 #3
- **验证**: `"abc`（无闭合引号）→ `Parse error: 未终止的字符串字面量`

### ✅ Bug #O3: LtColon 死代码 — 已验证清理
- **来源**: `issue/fixed/cleanup-ltcolon-deadcode.md`
- **验证**: `grep -rn "LtColon" src/` → 0 匹配

### ✅ Bug #O4: token.rs 死代码 Lexer — 已验证删除
- **来源**: `issue/token-rs-dead-lexer-p0.md`
- **验证**: `src/lexer/token.rs` 从 749 行缩减到 122 行

### ✅ Bug #O5: 99_errors 占位文件 — 已验证回填
- **来源**: `issue/errors-99-placeholder-files.md`
- **验证**: 15/15 文件含有效错误代码，reject_errors 100% 覆盖

---

## 三、新发现 Bug

### 🔴 Bug #N1: 可变参数 `..` 分隔符已文档化但解析器未实现（P1 — 功能缺口）

**复现步骤**:
```lz
// 文档 03d-可变参数.md 声称支持的语法
def f(a: int, .., b: str) = print(b)
def main() = f(1, b="hello")
```

**预期结果**: 成功编译，`..` 分隔仅位置参数（a）和仅关键字参数（b）

**实际结果**: `Parse error: Expected param name, got DotDot`

**技术根因**: `src/parser/parser.rs:663-719` 的 `parse_params()` 函���只匹配 `Ident` 和 `Self_` token，不处理 `DotDot`

**影响范围**: `SYNTAX/03d-可变参数.md` 全文文档化的 `..` 分隔符完全不可用。安全收集模式 `List<T>` 正常工作。

**严重等级**: P1（功能文档与实现不一致）

---

### 🟡 Bug #N2: 单行逗号分隔 struct 字段定义误导性报错（P2 — 错误消息）

**复现步骤**:
```lz
struct Point = x: f64, y: f64
def main() = print("ok")
```

**预期结果**: 成功编译或报 "struct 字段需换行缩进书写"

**实际结果**: `Parse error: Unexpected token at top level: Comma`

**技术根因**: 解析器将 struct 定义行尾的逗号后内容（`y: f64`）误判为顶层语句

**影响范围**: 单行 struct 带逗号分隔字段不可用；缩进多行书写正常。文档 `06a-struct` §一 只展示多行写法，未提及单行形式。

**严重等级**: P2（误导性错误消息，但多行写法 works fine）

---

### 🟡 Bug #N3: USAGE.md 内部不一致（P2 — 文档矛盾）

**复现步骤**: 阅读 `USAGE.md`

**证据**:
- L47: `> 计划中（尚未实现）: --cache-dir、--force、--lzi、--rust-crate`
- L95: `cargo run -- hello.lz --lzi lz-infer/hello.lzi`（← 与 L47 矛盾）

**影响范围**: 同一文档上下矛盾，用户照 L95 敲命令无效

**严重等级**: P2（文档矛盾）

---

### 🟡 Bug #N4: 项目根目录 28 个编译产物残留（P2 — 代码卫生）

**证据**: 根目录存在 28 个 `.exe`/`.pdb`/`.o` 文件：
`break_continue.exe/.pdb`, `comments.exe/.pdb`, `for_while_loop.exe/.pdb`, `if_elif_else.exe/.pdb`, `loop_demo.exe/.pdb`, `literals.exe/.pdb`, `literals_more.exe/.pdb`, `module_magic.exe/.pdb`, `struct.exe/.pdb`, `trait_impl.exe/.pdb`, `_ifstmt.pdb`, `_matchstmt.pdb`, `test_partial.pdb` + 3 个 `.o`

**影响范围**: 根目录混乱，易被误提交。建议移至 `_bak/` 或 `.gitignore`

**严重等级**: P2

---

## 四、已知限制验证（非 Bug，确认现状）

| 特性 | 状态 | 备注 |
|------|:--:|------|
| 元组字段访问 `p.0` | ❌ 不支持 | `Expected field/method, got IntLit(0)` |
| 推导式 over 列表变量 | ✅ **已支持** | `[x*2 for x in items]` 生成正确的 `.into_iter().map().collect()` |
| 匿名 lambda `\|x\| x+1` | ✅ 正常 | 编译通过 |
| match 守卫 `n if n > 3` | ✅ 正常 | 编译通过 |
| walrus in comprehension | ✅ 正常 | `[total := total + x for x in 1..4]` 编译通过 |
| struct 构造器 colon 语法 | ✅ 正常 | `Point(x: 1.0, y: 2.0)` 编译通过 |
| 安全收集 `List<T>` | ✅ 正常 | `def f(values: List<int>)` 自动收集 |
| 顶层构建块 `x =:` | ✅ 正常 | 生成 `let x = (\|\| ... )()` |
| go 语句 | ✅ 正常 | `go_stmt.lz` 编译通过 |
| pipe `\|>` | ✅ 正常 | `pipe_spec.lz` 编译通过 |
| 生成器 `gen` | ✅ 正常 | `gen_block_star.lz` 编译通过 |
| setup/teardown | ✅ 正常 | `setup_teardown.lz` 编译通过 |
| macro | ✅ 正常 | `macro_real.lz` 编译通过 |

---

## 五、文档版本合规

- `python SYNTAX/check_doc_versions.py`: **全部通过** ✅（规范版本 3.2）

---

## 六、Issue 整理操作

| 文件 | 操作 | 原因 |
|------|:--:|------|
| `test-report-2026-07-31-1159.md` | 删除 | 过时（53 warnings → 0） |
| `test-report-2026-07-31-1300.md` | 删除 | 过时（reject_errors 5/15 → 15/15） |
| `errors-99-placeholder-files.md` | → fixed/ | 已解决（15/15 回填） |
| `token-rs-dead-lexer-p0.md` | → fixed/ | 死代码已删除（749→122 行） |
| `demo-syntax-audit-2026-07-31.md` | → fixed/ | 已整改（REMEDIATION 全条目 ✅） |
| `syntax-docs-audit-2026-07-31.md` | → fixed/ | 已处理（verdict + 实测修复） |

---

## 七、优先级建议

1. **P1 Bug #N1**（`..` 分隔符未实现）— 要么实施解析器支持，要么标记文档为"计划中"
2. **P2 Bug #N3**（USAGE.md L95 `--lzi`）— 删除示例或统一标注"计划中"
3. **P2 Bug #N2**（单行 struct 报错混乱）— 改善错误消息
4. **P2 Bug #N4**（根目录产物残留）— 清理或 `.gitignore`
