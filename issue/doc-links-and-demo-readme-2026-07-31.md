# Open: 章节引用错误 + DEMO/README 覆盖清单失实审计（2026-07-31）

**状态**: Open
**日期**: 2026-07-31
**范围**: SYNTAX/ 文档间链接与章节引用（§x.x）验证 + DEMO/README.md 覆盖清单 vs 实际文件内容，不涉及源代码
**审计方式**: 全量链接提取→存在性验证；§ 引用逐条对照目标文档章节标题；README 声称行数/特性 vs 实际文件 wc -l / grep

---

## 一、链接检查结论（好消息）

对 SYNTAX/ 全部 34 个去重后的 `.md` 链接目标逐一验证，**无断链**。唯一疑点 `项目进度报告.md`（被 `overview/缺失语法特性报告.md:4` 引用）经核实与引用者同处 `overview/` 子目录，相对路径有效。此方向可关闭。

---

## 二、章节引用错误（§x.x 指向错误或不存在，共 7 处确认）

### 1. `01-类型系统.md:362` → 06d §三「类型转换」，实际 §三 是「一元运算符」

- 引用原文：「详见 [06d-内置魔法trait](06d-内置魔法trait和全局函数.md) §三 类型转换」
- 06d 实际章节：§三 = **一元运算符**（L44）、§六 = **类型转换**（L88）
- 应指向 §六。

### 2. `05-控制流.md:399` → 06d §十六「上下文管理器」，实际 §十六 是「类型缺口」

- 引用原文：「详见 [06d-内置魔法trait](06d-内置魔法trait和全局函数.md) §十六 上下文管理器」
- 06d 实际章节：§十六 = **类型缺口**（L262）、§十七 = **上下文管理器**（L276）
- 应指向 §十七。

### 3. `06d:179` → 05-控制流.md §六「match 解构」，实际 §六 是「break / continue」

- 引用原文：「详见 [06a-struct.md](06a-struct.md) 与 [05-控制流.md](05-控制流.md) §六 match 解构」
- 05-控制流 实际章节：§二 = **match / case**（L62）、§六 = **break / continue**（L256）
- 应指向 §二。

### 4. `00-词法基础.md:105` → 01-类型系统.md §七「Number trait」，§七 是「复合类型」

- 引用原文：「`Number` 是**内建数值 trait**（见 [01-类型系统.md](01-类型系统.md) §七）」
- 01-类型系统 实际章节：§七 = **复合类型**（L366，内容为元组/容器）；`Number` 全文只在 L191（`@math` 宏参数）出现，§七 内无 Number 定义
- 指向章节与实际内容不符。

### 5. `12-操作符.md:332` → 引用不存在的 §5.1

- 引用原文：「`~x` 前缀 = 位非（见 §3.1、§5.1）」
- 12-操作符.md 实际章节：§3.1 = 二元运算符映射（L411），**没有 §5.x 章节**（最大为 §3.7）
- §5.1 为凭空引用，应删或改指真实章节。

### 6. `12-操作符.md:362` → 文本与自身优先级表矛盾（管道级别）

- 引用原文：「在 §二 优先级表中为 **1 级**（仅高于赋值 `=`(0)，低于管道 `|>`(2) 与逻辑/比较/算术运算符）」
- §二 优先级表当前内容：`1 级 = :=` ✅、`0 级 = 赋值` ✅，但 **管道 `|>` 在 7 级**（L382），不是 2 级
- 文本「低于管道 `|>`(2)」与表（管道 7 级）矛盾；且 4 级 `not` 与 5 级比较实际位于 `:=` 与管道之间，顺序描述也不符。

### 7. `06f-magic用法.md:82` → dispatch「详见 §二」，§二 是「magic 块名规则」

- 引用原文：「**`dispatch`**：多分派策略，详见 §二」
- 06f 实际章节：§二 = **magic 块名规则**（L40）；dispatch 字段详解位于 **§三 配置字段表**（L58）下的「字段详解」（L71 起）
- 应指向 §三。

---

## 三、DEMO/README.md 覆盖清单失实（声称 vs 实际，已逐文件核实）

### 8. composite.lz：声称「144 行 / 9 种装饰器」，实际 26 行 / 3 种

- `DEMO/README.md:74`：「composite.lz | 144 | 函数嵌套捕获、**9 种装饰器**（@export/@memoize/@math/@simd/@parallel/@tail_call/@unsafe/@overload）、多装饰器堆叠、闭包」
- 实际 `DEMO/04_functions/composite.lz`（全文已读）：**26 行**，仅 `@export(Rust)`（L5）、`@memoize`（L8）、`@math`（L15）**3 种装饰器**；无 @simd/@parallel/@tail_call/@unsafe/@overload，无多装饰器堆叠示例

### 9. macro_demo.lz：声称 55 行，实际 2 行（空壳）

- `DEMO/README.md:111`：「macro_demo.lz | 55 | #!bin macro、macro 定义、template 定义、quote/f```/内置函数」
- 实际全文仅：
  ```lz
  #!bin macro
  // Macro module declaration
  ```
  声称覆盖的 macro/template/quote 定义**全部不存在**。

### 10. async_spawn.lz：声称 223 行 / go / Futures 聚合，实际 13 行

- `DEMO/README.md:114`：「async_spawn.lz | 223 | async/await、spawn 内联/块语法、go 进程、Futures<T1,T2> 聚合、spawn vs go」
- 实际仅 13 行：`async def` + `spawn` ×2 + `await`——**无 go、无 Futures 聚合、无 spawn vs go 对比**

### 11. 其余行数严重不符（README 声称 → 实际）

| 文件 | README 声称行数 | 实际行数 |
|------|:---:|:---:|
| test_suite.lz | 84 | **9** |
| yield_demo.lz | 77 | **11** |
| module_magic.lz | 120 | **12** |
| variadic.lz | 81 | **14** |
| checker.lz | 62 | **16** |
| box_rc_arc.lz | 57 | **14** |
| trait_impl.lz | 150 | **18** |
| pipe.lz | 75 | **19** |
| for_while_loop.lz | 62 | **18** |
| if_elif_else.lz | 79 | **21** |
| break_continue.lz | 48 | **21** |
| option_result.lz | 100 | **27** |
| keywords.lz | 172 | **82** |
| struct.lz | 110 | **37** |
| type_conversion.lz | 56 | **82**（反向：实际更多） |

**结论**: README 的行数/特性清单是**某个历史快照**，文件此后被大幅精简（或 README 未随改版刷新），已完全不能作为 DEMO 覆盖的依据。

---

## 四、修复建议（按优先级）

1. **P0**：`DEMO/README.md` 覆盖清单整体重写——以实际文件为准重计行数、重列特性（#8/#9/#10/#11）；无法一次性重写则至少删除「9 种装饰器」「55 行」「223 行」等失实声明
2. **P1**：章节引用 #1-#4 修正指向（06d §三→§六、06d §十六→§十七、05 §六→§二、01 §七→删除或改指）
3. **P1**：#5 删除不存在的 §5.1 引用；#6 同步 12-操作符 文本与优先级表（管道级别）；#7 改指 §三
4. **P2**：若 macro_demo/async_spawn 等文件确为待补全空壳，补充内容或改 README 状态标注为「占位」

## 五、验收标准（修正后核对）

1. `grep -rn "§" SYNTAX/` 中每个章节引用都能在目标文档找到对应标题
2. `12-操作符.md` 文本中管道级别与 §二 表一致
3. `DEMO/README.md` 每行声称行数与 `wc -l` 实际一致（±2）
4. `DEMO/README.md` 声称的装饰器/特性在对应 .lz 文件中 grep 可命中
