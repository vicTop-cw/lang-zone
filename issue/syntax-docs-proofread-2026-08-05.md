# SYNTAX/ 语法文档两轮纠错复查报告

> 日期：2026-08-05 · 审查人：AtomCode（deepseek-v4-flash）
> 范围：`SYNTAX/` 全部 36 篇文档（00~15、99、附录A/B、overview、README、check_doc_versions.py）
> 方法：逐篇精读 → 交叉核对 → 按《语法文档编写规范》扫描禁用措辞 → 字节级确认乱码 → 第二轮逐条复核
> 结论：**A 级 14 项、B 级 10 项、C 级 12 项、D 级 18 项**；1 项初报（原 D11）经复核为误报已撤除。

---

## 一、A 级 — 直接矛盾 / 事实错误（示例算错、语法互相冲突、乱码）

| # | 位置 | 问题 |
|---|------|------|
| A1 | 15-测试框架.md L133 vs 00-词法基础.md L435 | 15 说「test 冒号后必须换行缩进，**单行写法不支持**」；00 却称「`test` 关键字支持单行写法 `test "name": expr`（详见 15-测试框架.md）」。两文档互相矛盾。 |
| A2 | 03d-可变参数.md L53 | `sum_all2(3,1,2,3,b=7)` 注释「// 16 — 1,2,3 自动收集为 List<int>,b=5」。实参 b=7（命名覆盖位置 b=1），正确计算 3+7+2+3=**15**，「16」与「b=5」均错。 |
| A3 | 03d-可变参数.md L62 | `sum_all3(3, [1, 2, 3]) // 9 — 1,2,3 ,b=1`：b 未传应为默认 **0**，注释写「b=1」。 |
| A4 | 05-控制流.md L99 vs 00 L136 / 04 L105 / 附录B L134 | 05 模式表列「类型守卫 `case x if x is int:`」，把 `is` 当运行时类型判断；00/04/附录B 均明确「`is` 仅 duck 约束内可用、**不支持 `x is int`**」。示例与规范直接冲突。 |
| A5 | 05b-block命名块.md L67、L810 | 两处写「三元 `?:`」。LZ 三元是 `a if cond else b`（05 §1.4、12 L435），`?:` 运算符不存在。 |
| A6 | 05b-block命名块.md L337、L660 | 引用「[附录B] **§203**」，附录B 仅一~五章，无 §203。 |
| A7 | 02-变量与绑定.md L82 | 注释「需在 match/guard let/**if let** 中使用」——文档通篇无 `if let` 语法（只有 `guard let` / `while let`）。 |
| A8 | 01-类型系统.md L217-218 vs L160-161、L189 | §4.3 把 `def bar(a, b) = a + b` 标 ❌「参数无显式类型」；§3.2 与 §4.1 却称「无注解参数默认 int」且 `def double(x) = x * 2` 为合法示例。同文档内矛盾。 |
| A9 | 14-生成器.md §5.6 L391 vs §5.7 L416-417 | §5.6 表格列「`yield (2,)` ❌ 非法（单值元组）」；§5.7 却用 `yield ("Alice",)`（单值元组）调用单参函数作为合法用法，§5.2 亦用 `yield ()`。且「应用 (2,) 列表替代」表述不通。 |
| A10 | 99-内置预导入库.md L281 | 乱码（字节 `EF BF BD`）：「trait 不是值，**��**可调用」应为「**不**可调用」。 |
| A11 | 附录B L130 | 乱码（字节 `EF BF BD`）：「1.11 同一性与**��**员关键字」应为「**成**员」。 |
| A12 | 14-生成器.md L622 | 乱码（字节 `EF BF BD`）：「迭**��**器包」应为「迭**代**器包」。 |
| A13 | 01-类型系统.md L414 | 错别字：「**不允的**负索引 `t.-1`」应为「不允许的」。 |
| A14 | 02-变量与绑定.md L254-255 | 「❌ 丢弃值不可用 let 绑定_**」行尾多一下划线；且断言 `let _ = some_func()` 错误，与 00-词法基础.md L309/L327（`let _ = expr` 是「丢弃返回值」合法用法）直接矛盾。 |

## 二、B 级 — 跨文档矛盾

| # | 位置 | 问题 |
|---|------|------|
| B1 | 附录B L147 vs 00 L159 / 01 §7.7 | 附录B 写「`Nil` = 空列表类型别名(设计阶段)」；00/01 明确 `Nil` 是单元类型 `()` 别名，「与空列表 `[]` 无关」。 |
| B2 | 10-并发与异步.md L114/L144 vs 00 L80 / 缺失报告 P2-2 | 10 说「`go` 启动**独立进程（非线程）**」；00 明确 `go expr` → `std::thread::spawn(...)`（**线程**），缺失报告亦同。 |
| B3 | 99 L304 vs 14-生成器.md §四 | 99 标 `PartialFunc`「设计阶段」；14 将 PartialFunc 当已定义语法详述。 |
| B4 | 03e L63-64 vs 13-指针与引用.md §2.3 | 03e 标 `@unsafe`/`@overload`「🔧 设计阶段」；13 已给出 `@unsafe` 完整现行规则。 |
| B5 | 03e L64/L83 vs 03d §2.7 vs 缺失报告 §五 | 装饰器名不一致：03e 用 `@overload`（标「预留」），03d 用 `@overloads`（复数）；缺失报告称 `@overload` 已可用。 |
| B6 | 附录A L63 / 附录B L255 vs 03e-closure §三 L58-62 | 附录A/附录B 笼统写「闭包参数**必须**注解」；03e-closure 明确「**作函数实参时注解可选**」。 |
| B7 | 附录B L205 vs 12-操作符.md L435 | 附录B 说范围 `..` `..=`「优先级**仅高于**赋值、海象、三元」；12 优先级表将 `..` `..=` 与 `:=`、三元并列为 1 级同级。 |
| B8 | 附录A L47 vs 08b §八 | 附录A 示例 `x = comptime: expr`（单行）；08b 明确「❌ `comptime: x = 42` 错误：必须换行缩进」。 |
| B9 | 02 L197-198 vs 12 §1.19 | 02 称 `greet(name~)` 等价 `greet(name = name^)`、`greet1(name~)` 等价 `greet1(name = name)`——同一语法两种展开，且与 12 §1.19「`f(x~)` 等价于 `f(x = x)`」不符。 |
| B10 | 06d L340 vs 06d L347 / 05 L554 | 06d 文字称「`__exit__` 接收 guard 对象作为参数（**不是** `&mut self`）」，但其自身示例与 05 示例均为 `def __exit__(mut self, _guard: File)`。 |

## 三、C 级 — 违反《语法文档编写规范》（禁用措辞 / 未实现内容入正文）

| # | 位置 | 违规 |
|---|------|------|
| C1 | 03e L54-64 | 装饰器表带「状态」列（✅ 已实现 / 🔧 设计阶段）。 |
| C2 | 00 L385 | 「位运算（**设计阶段**）」；且混淆 `~` 前缀位非与后缀命名参数糖。 |
| C3 | 06a L338、L478-479 | 「设计考虑 / 扩展规划」「仍在设计阶段」「有待确定」；且 L364 示例在「默认构造是否可用待确定」的同时直接使用默认构造。 |
| C4 | 06e L77、L87 | 「Compile-env（编译环境 — **未来**）」「Layout（内存布局 — 源代码**已实现**）」；§五整节为未实现内容。 |
| C5 | 12 L422 | 「该运算符**已实现**…区别于本文件其余『规范目标』小节」。 |
| C6 | 99 L304 | 「设计阶段」（PartialFunc）。 |
| C7 | 06h L191 | 正文写「**转译器要求**：实现时须做 `type_refers_to` 环检测（enum 已有，struct 需补齐）」。 |
| C8 | 00 L20、L161 | 「**当前**以普通标识符经 prelude 解析」「**当前**在词法层保留专用 token」「**未来**可统一降级」。 |
| C9 | 附录B L9、L147 | 「**当前**在词法层保留专用 token」「(设计阶段)」。 |
| C10 | 03d L13 | 「正是为了解决…**此前** `..` 一旦写就强制注入」——历史性措辞。 |
| C11 | 01b L155 | 「也消除了**此前文档**里『`:` / `<:` / `>:` 三者混淆』的问题」。 |
| C12 | 05 L164 | 「循环结束后**不再**可见」——「不再」属禁用清单（语义陈述，轻违规）。 |

## 四、D 级 — 格式 / 编号 / 引用问题

| # | 位置 | 问题 |
|---|------|------|
| D1 | 00 L230-231 | 「区间/闭区间」表格**无表头行**，列数（4 列）与上表（2 列）不一致。 |
| D2 | 00 L136 | `is` 表格行紧跟在 blockquote 之后，1.10 表格被引用块打断。 |
| D3 | 附录B L36 | `| iterator | … |` 孤行悬在 1.1 表格之后（表格已结束又续行）。 |
| D4 | 03d | 「三、安全分隔符」下小节编号为 **4.1~4.4**（应为 3.x）。 |
| D5 | 06c L155、L170 | 两个「### 8.4」小节重复编号。 |
| D6 | 08b | §4.2 后直接跳 **§4.5**（缺 4.3/4.4）。 |
| D7 | 12 L434 | 优先级 0 级复合赋值列表**漏列 `**=`**（L142 有 `**=`）。 |
| D8 | 12 L336 | §1.18.1 把「`s? ^`（带空格）」列为 `s?^` 的正确写法，与 §1.18「后缀操作符必须紧贴前值」矛盾。 |
| D9 | 12 L553 | `?` 同时出现在「§3.7 无魔法方法的操作符」表（括号内却列出 `__is_ok__` 等魔法方法）——表内自相矛盾。 |
| D10 | 01 L270 | 「实现。> 详见 **[06c…]**」——`。>` 引用标记夹在句中，markdown 断裂。 |
| D11 | ~~附录B L156 vs L178~~ | ~~软关键字引用不一致~~ → **已复核撤除**：L178「见 §八」指 01b §8 参数约束详解，与 L156「§十一 总表」不冲突。 |
| D12 | 08b L149 | 「`inspect::*`」用 `::`（全文档禁 `::`）；00 L153 `None::<T>` 亦含 `::`（Rust 侧引用，较轻）。 |
| D13 | 06g L111 | 「当 `multi_dispatch = true` 时」——配置字段不存在（06f 字段表只有 `dispatch`），L121 用的是 `dispatch = "by_ret"`。 |
| D14 | 13 L89 | `@unsafe` 示例用 `*int` 裸指针类型，01 类型系统与 13 §一均未定义该类型。 |
| D15 | 99 L415 | 「使用时需 `import std::xxx`」——用 `::` 写导入示例，与全文档「路径用 `.`」冲突。 |
| D16 | 05b L211-212 | 「退**当前最内层**的可跳出构造（**loop 或 block**）」漏了 for/while。 |
| D17 | 06b L138 | 「## 八、枚举支持 trait」空标题，下无内容直接接 §九。 |
| D18 | 03c L124 | 「def **chker**\<P: __Params\>」拼写缺字母。 |
| D19 | 03b L56 | 「`def bar<T>(x) = x` 错误：x 缺少类型注解」与 01 §3.2/03 L24「无注解参数默认 int」措辞冲突（泛型语境，轻）。 |

## 五、核对通过项（抽查，无需改动）

- `check_doc_versions.py` 全部版本头合规（规范版本 3.3，今日运行 EXIT=0）。
- 05b `break NAME` / `break with` 铁律、03c/05b `__Params` 协议、12/附录B 优先级引用链（附录B 明确不维护独立副本）、06d/06g `__implicit_from__`/`__implicit_to__` 配对、14 `iterator` 与 `def` 正交性等均交叉一致。
- 附录A/05/05b 对 `block`「无返回值、拒绝接收」表述一致。

## 六、修复优先级建议

1. **A 级先行**：A1（test 单行矛盾）、A2/A3（算错）、A4（`is` 冲突）、A5（`?:` 不存在）、A9（`yield (2,)` 自相矛盾）、A10-A12（乱码字节修复）。
2. **B 级次之**：B2（go=线程/进程）、B4/B5（@unsafe/@overload 状态与名称）、B1（Nil 定义）、B9（`name~` 展开统一）。
3. **C 级**：按《语法文档编写规范》将禁用措辞改为正面表述，未实现内容移入 overview/缺失语法特性报告.md。
4. **D 级**：随 A/B 修复顺手清理（编号、表格、引用）。
