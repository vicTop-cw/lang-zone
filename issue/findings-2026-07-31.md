# 全项目找茬报告（bug / 矛盾 / 不合理 / 漏洞）

**状态**: Mostly Resolved（IR 路线持续修复）
**日期**: 2026-07-31 · 最后验证: 2026-07-31 18:05
**范围**: 全项目代码审计（src/、tests/、文档、未提交改动），只报告、不修改
**复核**: 见 verdict-2026-07-31.md 及 test-report-2026-07-31-1400.md
**审计方式**: 源码阅读 + 文档对照 + 实测编译验证

---

## 一、P0 — 实锤 bug（有运行证据）

### 1. 顶层构建块被静默丢弃，生成不可编译的 Rust 代码 — ❌ 误报（已验证）

> **复核结论 (verdict-2026-07-31.md)**: 实编译 `x =:` → `let x = (|| ... )()`，x 被定义且可运行。Agent 审计有误。

**原证据**:
- `src/parser/parser.rs:339-345` 把顶层 `x =: body` 收集进 `Module.top_level_builds`
- `src/ast/decl.rs:19` 新增字段 `top_level_builds: Vec<(String, Vec<Stmt>)>`
- 全仓库引用该字段的只有 `ast/decl.rs` / `parser/parser.rs` / `project.rs` 三处——**codegen 和 typer 从不消费它**（`grep top_level_builds src/codegen` 无命中）

**实测**: 编译
```lz
x =:
    y = 1
    y + 1
def main() = print(x)
```
生成的 `topbuild.rs` 只有 `fn main() { println!("{}", x) }`，`x` 未定义，rustc 必然报 `cannot find value x`。

**影响**: 静默数据丢失 + 产出不可编译的 Rust 代码，无任何报错。

**修复建议**: 在 `CodeGen::generate` 中真正生成顶层构建块（闭包绑定），或至少显式报「顶层构建块暂不支持」——二选一，不能静默。

---

### 2. 数字字面量非法/溢出静默变 0（`src/lexer/lexer.rs:92-171`） — ✅ 已修复

> **修复验证 (test-report-2026-07-31-1400.md)**: `0xG`→"无效的十六进制数字"、`999...`→"无效的整数"、`123e`→"科学计数法缺少指数"。全部正确报错。

```rust
let val = i64::from_str_radix(..., 16).unwrap_or(0);   // L108 / L120 / L132
Token::IntLit(num.parse().unwrap_or(0))                 // L169
Token::FloatLit(num.parse().unwrap_or(0.0))             // L167
```

**触发场景**:
- `0xG`（无有效数字）→ `IntLit(0)`
- 十进制溢出 i64（如 20 位数字）→ `0`
- `123e`（科学计数法无尾数）→ `FloatLit(0.0)`

**影响**: 错误输入静默变成「正确」的值，用户代码拿到 0 却没有任何报错——编译器撒谎，是最危险的一类行为。

**修复建议**: 三处改为返回词法错误（如 `InvalidNumber`），而不是 `unwrap_or(0)`。

---

### 3. 未终止字符串静默接受（`src/lexer/lexer.rs:173-196`） — ❌ 误报（已验证）

> **复核结论 (verdict-2026-07-31.md)**: 实编译 `"abc` → `Parse error: 未终止的字符串字面量`。Agent 审计有误。

**证据**: `read_string` 循环读到 EOF 直接返回 `StrLit(...)`，无错误；结尾 `\` 在 EOF 处被静默吞掉。

**触发场景**: `"abc`（无闭合引号）→ 静默返回 `StrLit("abc")`。

**修复建议**: 循环结束且未遇 `"` 时产生 `UnterminatedString` 错误。

---

## 二、P1 — 文档与代码矛盾

### 4. USAGE.md 文档化了 4 个未实现的 CLI 参数

**证据**: USAGE.md 明确给出 `--cache-dir .lzc-cache`(L34)、`--force`(L37)、`--lzi hello.lzi`(L41)、`--rust-crate "serde=1.0"`(L44)；但 `src/main.rs` 只解析 `--cached`（且硬编码 `.lzcache` 目录），上述 flag 全部无解析逻辑。

**影响**: 照文档敲命令不会有任何效果，文档即谎言。

**修复建议**: 实现这些 flag，或从文档中删除/标注未实现。

---

### 5. 测试计数全面互相矛盾（三份文档三个数字）

| 来源 | 声称 |
|------|------|
| `USAGE.md:54,57,60` | 436 个测试 / 45 个 DEMO 文件 / 8 个错误边界文件 |
| `overview.md:25` | lib 433/433、compile_demos 66/66、reject_errors 2/2、doc-tests 1/1 |
| `issue/README.md:3` | 416 项（292 lib + 115 compile_demos + 8 ir + 1 reject），compile_demos 已含 99_spec 38 文件 |
| `issue/cleanup-ltcolon-deadcode.md:83-84` | 预期 430/430、绿 demo 套件 77/77 |

同一项目四种口径，且 compile_demos 是否跳过 99_spec 的说法也互相矛盾（overview.md:19 说跳过，issue/README.md:3 说纳入）。

**修复建议**: 以实测为准统一计数，并注明统计口径。

---

### 6. 语言命名混乱：Lang-Zone vs Lang-Zone

**证据**: `overview.md:8` 声称已把 Lang-Zone 改名 Lang-Zone，但 `USAGE.md:2`、`src/lib.rs:1`、`hermes/` 全套文档、`benchmark/run_benchmark.py`、`Demo_old/` 仍全是 **Lang-Zone**；Cargo.toml package 名为 `lang-zone`。

**影响**: 改名只做了一半，文档、代码注释、基准脚本混用两个名字。

---

### 7. `josn_parse/` 目录名是 `json_parse` 的拼写错误

已提交进仓库（含 json_parser.rs、lz-zed 等），改名成本随引用增多而上升。

---

### 8. issue 状态与代码不同步 — ✅ 已修复

> **修复验证 (2026-07-31 14:00)**: `grep -rn "LtColon" src/` = 0；issue `cleanup-ltcolon-deadcode.md` 已移至 fixed/。

---

## 三、P2 — 死代码 / 逻辑缺陷

### 9. cache.rs 的 output/deps 校验全是死逻辑（`src/main.rs:206-212`）

**证据**:
- 实测 `.lzcache/if_elif_else.lzcache` 内容为 `hash=...\noutput=`——main.rs 的 `--cached` 只存源文件哈希
- main.rs 从不设置 `entry.output`、从不调用 `scan_deps` 记录依赖
- 于是 `CacheEntry::is_fresh`（`src/cache.rs:83-108`）的产物存在检查、依赖哈希检查永远形同虚设

**影响**: **改 import 的依赖文件后缓存不会失效**；CLI 缓存路径与 cache.rs 的设计完全脱节。

---

### 10. 缓存文件名只用 file_stem（`src/cache.rs:122-125`）

`a/foo.lz` 和 `b/foo.lz` 的缓存同为 `foo.lzcache`，互相覆盖；`output_filename`（L128-130）同理。

---

### 11. `prune_stale` 会误删 .rs（`src/cache.rs:141-160`）

L147 把缓存目录里所有「无对应源文件」的 `.lzcache` **和 `.rs`** 一起删。若产物与缓存混放，用户编译好的 `.rs` 会被清理。

---

### 12. main.rs 的 `.lz` 替换与 Windows 残留

- `path.replace(".lz", ".rs")`（`src/main.rs:51,159,182,198`）替换**所有**出现的 `.lz` 子串：`a.lz.lz` → `a.rs.rs`
- `--test` 模式（L215-244）：Windows 上实际产物是 `foo_test.exe`，`remove_file(&test_bin)`（无 `.exe` 后缀）删不掉，每次运行残留一个可执行文件

---

### 13. main.rs 重复代码

- `--emit=ir`（L152-172）与 `--ir-codegen`（L173-192）两段几乎完全相同的生成逻辑
- `rustc_version`（L32-36）无论是否需要 `--allow-rustc-private` 都无条件起 rustc 子进程

---

## 四、其他观察

- `read_string` 未知转义 `\q` 静默降级为 `q`（`src/lexer/lexer.rs:187` `_ => esc`），可接受但无警告
- 顶层构建块匿名名 `__build_{len}`（`src/parser/parser.rs:342`）可能与用户标识符冲突，且跨文件有重名风险

---

## 五、优先级建议

1. **P0 #1/#2/#3** —— 三个「编译器撒谎」类问题（生成坏代码、静默改值、静默接受非法输入），比任何文档矛盾都危险，优先修
2. **P1 #4/#5** —— 假 flag 与测试计数，尽快同步
3. **P1 #8** —— issue 收尾 + 审计文档同步
4. **P2** —— cache 链路（#9/#10/#11）、main.rs 清理（#12/#13）可排期

## 六、验收标准（修复后核对）

1. 顶层 `x =: body` 编译产物可见或显式报错（不静默丢弃）
2. `0xG` / 溢出字面量 / `123e` / 未终止字符串均报词法错误
3. `grep -rn "LtColon" src/` 为 0，审计文档同步
4. 修改 import 依赖后缓存失效
5. 全项目命名统一（Lang-Zone 或 Lang-Zone 二选一）

---

## 七、文档整改状态（2026-07-31 续，由文档专家落实）

针对本报告中属于**文档范畴**且与编译器代码无耦合的项，已在 `USAGE.md` 落实修正（以文件系统/编译器实测为准，不盲信原审计数字）：

- **#4 假 CLI flag（P1）**：经核验，当前 `USAGE.md` 已在 L47 将 `--cache-dir`/`--force`/`--lzi`/`--rust-crate` 标注为「计划中（尚未实现）」，已非谎言。无需改动。
- **#5 测试计数矛盾（P1）**：`USAGE.md` 计数已校正——`cargo test --lib` = 292（正确保留）；`compile_demos` 覆盖 **114** 个主 DEMO 文件（含 99_spec/ 37 个）；`99_errors/` 现 **15/15** 全部为真实负例；结构注释「45 个主 DEMO」「49 个预期失败」分别改为 **114 / 15**。权威口径见 `issue/README.md`（313 测试函数 = 292 lib + 1 compile_demos〔114 DEMO〕+ 8 IR + 10 mod + 1 reject_errors〔15 负例〕+ 1 doc）。
- **#6 命名混乱（P1）**：`USAGE.md` 全文 `lzc` 已改为真实二进制名 `lang-zone`（L3/L7/L93/L227/L246）；`lzcyc`（CY/ 真实二进制）、`lz-infer`（根 workspace 成员，确有 `[[bin]]`）保留。注意 `src/lib.rs` 等代码层命名、以及 `hermes/`、`benchmark/`、`Demo_old/` 不在本次文档整改范围内。
- **#7 `josn_parse/` 目录拼写（P2）**：属目录重命名 + 引用迁移，超出文档范畴，交工程 owner 排期。
- **#8 `hermes/` 审计文档残留 LtColon（P1）**：`src/` 中 LtColon 已于未提交改动中删除（`issue/fixed/cleanup-ltcolon-deadcode.md`），但 `hermes/01-词法与AST审计.md:152` 仍列旧 token。属文档同步，建议后续单独清理（本次未触及 hermes/ 树）。

> 不属于文档范畴、需工程侧处理的项：#1 顶层构建块静默丢弃、#2 字面量静默变 0（已修未提交）、#3 未终止字符串（verdict 判误报）、#9–#13 缓存链路与 main.rs 清理。

---

## 八、代码生成 Bug 修复验证（2026-07-31 15:06 补充）

以下 Bug 来自 `test-report-2026-07-31-1429.md`，本次回归确认已修复：

| Bug ID | 描述 | 严重等级 | 状态 | Issue 文件 |
|--------|------|:--:|:--:|------|
| D1 | i64::MIN 字面量无法表示 | P1 | ✅ 已修复 | → fixed/lexer-i64min-literal.md |
| D2 | `str()` 生成无效 Rust 代码 | P1 | ✅ 已修复 | → fixed/codegen-invalid-rust-output.md |
| D3 | 字符串拼接生成无效 Rust | P1 | ✅ 已修复 | → fixed/codegen-invalid-rust-output.md |
| D5 | 十六进制溢出错误消息 | P2 | ✅ 已改善 | → fixed/lexer-hex-overflow-message.md |

以下 Bug 仍存在：

| Bug ID | 描述 | 严重等级 | Issue 文件 |
|--------|------|:--:|------|
| D4 | `\|\|` 无参数闭包与逻辑或冲突 | P2 | lexer-noparam-closure-conflict.md |
| D6 | `#` 注释错误消息不友好 | P2 | (内嵌于 test-report) |
| D7 | main.rs:215 `path.replace` 遗漏 | P2 | (内嵌于 test-report) |

## 九、新发现 P0 Bug（2026-07-31 15:06）

### Variadic `..` 生成无效 Rust 代码（Bug #N1 升级）

原 Bug #N1 仅报告 `..` 分隔符文档化但解析器未实现。本次测试发现该功能已部分实现但**生成完全不可编译的 Rust 代码**，严重等级从 P1（文档）升级为 P0（代码生成缺陷）。

**核心问题**: `def sum_all(..nums: int)` 生成的 Rust 代码：
1. 参数类型为 `i64` 而非 `&[i64]`（丢失 variadic 语义）
2. `for n in nums` 对非可迭代类型迭代
3. 调用方 `sum_all(1, 2, 3, 4)` 传 4 个参数给 1 参数函数

详见 `issue/variadic-codegen-invalid-rust.md`。

## 十、IR 路线回归测试（2026-07-31 16:10，commit 3101d07）

### 回归结论

- **cargo test**: 313/313 ✅（含 compile_demos IR 路线 116 DEMO 全通过）
- **rustc 编译验证**: 122 个非 error DEMO → 28 通过 (23%) / 94 失败 (77%)

### 已修复（commit 3101d07）

| Bug | 描述 | Issue |
|-----|------|-------|
| F1 | elif 链条件顺序反转 | fixed/ir-builder-elif-reversed.md |
| F2 | match 仅取第一个臂 | fixed/ir-builder-match-arm-first-only.md |
| F3 | i64::MIN 双重取反 | fixed/ir-codegen-i64min-double-neg.md |
| F4 | ! 运算符优先级 | fixed/ir-codegen-unop-precedence.md |

### 仍 Open（9 项）

| ID | 描述 | 等级 | Issue |
|----|------|:--:|------|
| O1 | 链式比较 + 泛型空格语法 | P1 | ir-codegen-chained-cmp-and-generics.md |
| O2 | 默认参数值未生成 | P1 | ir-codegen-default-params.md |
| O3 | match 枚举模式/解构变量 | P1 | ir-codegen-match-var-scope.md |
| O4 | 方法调用返回类型/名称映射 | P1 | ir-codegen-method-ret-type.md |
| O5 | 方法定义点号语法 | P1 | ir-codegen-method-def-syntax.md |
| O6 | 元组枚举构造语法 | P1 | ir-codegen-tuple-enum-ctor.md |
| O7 | Variadic `..` 代码生成 | P0 | variadic-codegen-invalid-rust.md |
| O8 | `||` 无参数闭包冲突 | P2 | lexer-noparam-closure-conflict.md |
| O9 | `@math` 自动泛型 | P1 | ir-codegen-math-auto-generic.md |

### 新发现（8 项）

| ID | 描述 | 等级 | Issue |
|----|------|:--:|------|
| N1 | match 枚举变体→表达式非模式 | 🔴 P0 | ir-codegen-match-enum-pattern.md |
| N2 | match 臂模式解构变量丢失 | 🔴 P1 | ir-codegen-pattern-type-mapping.md |
| N3 | Result/Error 模式前缀错误 | 🔴 P1 | ir-codegen-pattern-type-mapping.md |
| N4 | mut 参数语法错误 | 🔴 P1 | ir-codegen-pattern-type-mapping.md |
| N5 | 推导式宏 comp!/dict_comp!/set_comp! 缺失 | 🔴 P1 | ir-codegen-comprehension-macro-missing.md |
| N6 | Vec 缺泛型参数 | 🟡 P2 | ir-codegen-pattern-type-mapping.md |
| N7 | 递归类型无限大小 | 🟡 P2 | ir-codegen-pattern-type-mapping.md |
| N8 | Null coalesce 类型不兼容 | 🟡 P2 | ir-codegen-pattern-type-mapping.md |

**详细测试报告**: `issue/test-report-2026-07-31-1610.md`

---

## §十一 2026-07-31 17:15 全量 IR 路线测试

**详细测试报告**: `issue/test-report-2026-07-31-1715.md`

### 状态总览
- **cargo build**: ✅ 0/0 · **cargo test**: ✅ 313/313 · **文档**: ✅ 合规
- **IR→rustc**: 43/123 (35.0%) — 全量测试
- **已验证修复**: 8 个 (含元组枚举构造 F7、模式变量绑定 F8)
- **新发现 P0**: 1 个 (N9: match 臂无 case 时 `.` → `::`)

### 已验证修复（移入 fixed/）
| # | Issue | 修复 commit |
|---|-------|------------|
| F7 | `ir-codegen-tuple-enum-ctor` → fixed/ | 32967e0/4dd0ecd/a542178 |
| F8 | `ir-codegen-pattern-var-binding` → fixed/ | 32967e0/4dd0ecd/a542178 |
| F1-F6 | 已在上轮移入 fixed/ | — |

### 仍 Open（5 个 P1）
- O1 `ir-codegen-generic-type-params.md` — 泛型全坏
- O2 `ir-codegen-type-keyword-as-value.md` — 类型名在值位置
- O3 `ir-codegen-keyword-await.md` — await 错用
- O4 `ir-codegen-missing-rc-box-import.md` — Rc/Box 未导入
- O5 `ir-codegen-match-var-scope.md` — 部分改善

### 新 Bug
- **N9 P0**: `ir-codegen-match-arm-dot-vs-colon.md` — 无 `case` 关键字的 match 臂 `.` 不转 `::`

---
## §十二 2026-07-31 17:45 全量 IR 路线测试 + 边界测试

**详细测试报告**: `issue/test-report-2026-07-31-1745.md`

### 状态总览
- **cargo build**: ✅ 0/0 · **cargo test**: ⚠️ 312/313（ir_demo_snapshots 首次 async_spawn 失败，重跑通过）
- **lz→IR**: ✅ 123/123 · **IR→rustc**: ⚠️ 43/123 (35.0%)
- **提交范围**: `ff8c61a` + 未提交改动（`src/ir/builder.rs`, `src/ir/codegen.rs`）

### 已验证修复（→ fixed/）
| # | Issue | 修复来源 |
|---|-------|----------|
| F9 | `ir-codegen-match-arm-dot-vs-colon.md` → fixed/ | #ff8c61a |
| F10 | `ir-codegen-missing-rc-box-import.md` → fixed/ | 未提交 codegen 改动 |

### 仍 Open P1（6 个）
- O1 `ir-codegen-generic-type-params.md` — 泛型全坏
- O2 `ir-codegen-type-keyword-as-value.md` — 类型名在值位置
- O3 `ir-codegen-keyword-await.md` — await 错用
- O4 `ir-codegen-chained-cmp-and-generics.md` — 链式比较
- O5 `ir-codegen-match-guard-dropped.md` — **NEW** match guard 条件静默丢弃
- O6 `ir-codegen-float-str-conversion.md` — **NEW** float(String)→无效 Rust

### 新 Bug（本轮发现）
- **N10 🔴 P1**: `ir-codegen-match-guard-dropped.md` — `case n if cond =>` 生成 `n =>` 通配模式
- **N11 🔴 P1**: `ir-codegen-float-str-conversion.md` — `float(str)` → `String as f64`（E0605）
- **N12 🟡 P2**: `ir-codegen-void-fn-return-type.md` — 无返回类型函数推断为 i64 导致类型不匹配

### 新增边界测试用例
- `ir-edge-nested-match.lz` — 深度嵌套枚举匹配 ✅ PASS
- `ir-edge-type-boundary.lz` — 类型转换边界 ✅ (commit ed30d68 N11 修复)
- `ir-edge-empty-collections.lz` — 空集合 ✅ (commit ed30d68 N12 修复)
- `ir-edge-guard-combo.lz` — guard组合 ✅ (commit ed30d68 N10 修复)
- `ir-edge-enum-self-methods.lz` — &self方法 ❌ (已知)

---
## §十三 2026-07-31 18:05 commit ed30d68 — IR 批量修复

### 状态总览
- **cargo build**: ✅ 0/0 · **cargo test**: ✅ 313/313
- **lz→IR**: ✅ 128/128 · **IR→rustc**: ⚠️ **47/128 (36.7%)** ↑ from 35.0%

### 已修复（→ fixed/）
| # | Issue | 描述 |
|---|-------|------|
| F11 | `ir-codegen-match-guard-dropped.md` | Match guard 条件恢复（新增 MatchArm 结构体） |
| F12 | `ir-codegen-float-str-conversion.md` | `float(str)` → `.parse::<f64>().unwrap()` |
| F13 | `ir-codegen-chained-cmp-and-generics.md` | 链式比较分解 + BinOpKind::is_comparison() |
| F14 | `ir-codegen-void-fn-return-type.md` | print/println/panic 推断为 Unit |
| F15 | `ir-codegen-type-keyword-as-value.md` | Option/Result/Rc/Arc/Box 空参数默认 `<_>` |
| F16 | `ir-codegen-keyword-await.md` | await → `.await` (Rust postfix) |

### 仍 Open P1（3 个）
- `ir-codegen-generic-type-params.md` — 泛型类型参数
- `ir-codegen-type-keyword-as-value.md` — 类型名在值位置（部分改善）
- `ir-codegen-method-ret-type.md` — 部分改善
