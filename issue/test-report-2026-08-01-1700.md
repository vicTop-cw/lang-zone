# IR 全量测试报告 2026-08-01T17:00:00Z

## 环境信息
- **OS**: Windows (x86_64-pc-windows-msvc)
- **分支**: master
- **Commit**: 6b885a5a7d42f004305cacc987a91dbd0d8b1814
- **测试人**: auto-sdet (automation: lz-测试)
- **测试路线**: IR (Intermediate Representation) only

---

## 测试结果汇总

### Phase 2: 全量测试执行

| 测试套件 | 通过 | 失败 | 忽略 | 说明 |
|----------|------|------|------|------|
| `cargo test --lib ir_` (IR builder) | 6 | 0 | 0 | IR builder 单元测试全部通过 |
| `cargo test --lib ir::codegen` (IR codegen) | 3 | 0 | 0 | IR codegen 基础测试通过 |
| `cargo test --test ir_snapshots` (IR 快照) | 8 | 0 | 0 | 43 个 DEMO 文件 IR 生成全部通过 |
| `cargo test --test compile_demos` (DEMO 编译) | 1 | 0 | 0 | 所有 DEMO 通过旧 codegen 路径编译 |
| `cargo test --test reject_errors` (错误拒绝) | 1 | 0 | 0 | 错误边界文件正确拒绝 |
| `cargo test --lib` (全量单元测试) | 292 | 0 | 0 | 所有 292 个单元测试通过 |

### IR Codegen 端到端测试 (--ir-codegen 路径)

| 指标 | 数值 |
|------|------|
| LZ→IR 编译通过率 | 35/83 (42%) |
| IR→rustc 编译通过率 | 28/83 (33%) |

> **对比基准** (2026-08-01 16:39 报告): LZ→IR: 129/129, IR→rustc: 74/129
> 注意：本次仅测试非 spec/error 目录的主 DEMO 文件 (83 个)

---

## Bug 判定与分级

### P0: IR 语义错误 / 数据丢失 / 编译阻断

**当前 P0 问题（已知，未解决）:**
1. **variadic-codegen-invalid-rust** (`issue/variadic-codegen-invalid-rust.md`)
   - Variadic `..` 参数 codegen 未完全处理
   - 状态: 已验证 `DEMO/04_functions/variadic.lz` 的 IR codegen 输出看起来正确
   - 但 `variadic_dotdot.lz` 在 IR codegen 路径下仍然失败

**新发现 P0:**
- `--ir-codegen` 路径有 48/83 文件在 LZ→IR 阶段失败
  - 失败文件包括: match.lz, match_more.lz, struct.lz, struct_more.lz, enum.lz, magic_methods.lz, trait_impl.lz, callable_objects.lz, import_demo.lz, 以及所有 combo-syntax 文件
  - 根因: IR builder 对 struct/enum/magic/trait/import 等复杂语法的支持不完整
  - 这些文件在旧 codegen 路径下全部编译成功

### P1: 功能错误 / 回归失败

**当前 P1 问题（已知，未解决）:**
1. `ir-codegen-comprehension-macro-missing` - 推导式生成未定义的宏调用
2. `ir-codegen-constant-as-type` - 类型别名未正确输出
3. `ir-codegen-generic-type-params` - 泛型调用语法未转换
4. `ir-codegen-match-var-scope` - match 变量作用域问题
5. `ir-codegen-method-ret-type` - 方法返回类型推断错误
6. `ir-codegen-panic-enum-variant-syntax` - panic 中枚举变体语法
7. `ir-codegen-pattern-type-mapping` - 模式类型映射错误
8. `ir-codegen-struct-mut-borrow-missing` - 可变借用缺失
9. `ir-codegen-try-catch-control-flow` - try-catch 控制流
10. `ir-codegen-try-catch-return-type` - try-catch 返回类型
11. `ir-codegen-type-keyword-as-value` - 类型关键字在值位置

**新发现 P1:**
- `identifiers.lz` IR→rustc 失败 (E0599: 返回类型 `()` 不是迭代器)
- `keywords.lz` IR→rustc 失败 (E0308: 类型不匹配, E0728: await 在非 async 上下文)
- `type_conversion.lz` IR→rustc 失败 (E0308: 类型不匹配)
- `generics.lz` IR→rustc 失败 (E0425: 找不到类型值)
- `operators.lz` IR→rustc 失败 (E0425/E0433: 未定义变量/导入)
- `primitives.lz` IR→rustc 失败 (E0382: 移动后使用)
- `checker.lz` IR→rustc 失败 (E0277: Box<dyn Any> 未实现 Clone)

### P2: 边界行为 / 非核心路径

**当前 P2 问题（已知，未解决）:**
1. `ir-codegen-callable-objects-e0618` - __call__ 机制
2. `ir-codegen-closure-move-missing` - 闭包 move 缺失
3. `ir-codegen-empty-vec-type-annot` - 空列表类型标注

---

## IR 一致性校验 (round-trip)

| 测试项 | 结果 |
|--------|------|
| `ir_simple_function` | ✅ 通过 |
| `ir_empty_module` | ✅ 通过 |
| `ir_literals` | ✅ 通过 |
| `ir_if_else` | ✅ 通过 |
| `ir_struct_definition` | ✅ 通过 |
| `ir_let_bindings` | ✅ 通过 |
| `ir_generator_yield` | ✅ 通过 |
| `ir_demo_snapshots` (43 文件) | ✅ 通过 |

---

## 技术债务标记 (TECH_DEBT)

以下项被标记为 TECH_DEBT，不影响当前 IR 测试路线:

1. `src/codegen/` 模块仍为默认 codegen 路径（AST→Rust 直连），IR codegen 尚未成为默认
2. `--ir-codegen` 标志仍为可选路径，LZ→IR 阶段失败率 58%
3. `tests/compile_demos.rs` 使用的是旧 codegen 路径（AST→Rust），不是 IR→Rust 路径

---

## 结论

**本轮测试状态: ⚠️ 未完成**

- Phase 0-4 已全部执行
- 所有 292 个单元测试通过，IR 快照测试通过，DEMO 编译通过
- **存在 1 个 P0 + 11 个 P1 未解决 Bug**（全部为已知 IR codegen 问题）
- IR codegen 端到端通过率仅 33%，远低于目标
- 根据成功标准，本轮任务视为未完成，下一周期自动重试

---

## 文件快照 (SHA256)

| 文件 | Hash |
|------|------|
| IR/design.md | ed29e12a... |
| IR/implementation-plan.md | 58a98f19... |
| IR/migration-roadmap.md | f5f63e55... |
| src/ir/node.rs | 5cc4a50e... |
| src/ir/types.rs | 13054e1e... |
| src/ir/builder.rs | 117bca94... |
| src/ir/codegen.rs | d9304776... |
| tests/ir_snapshots.rs | bd63e512... |
