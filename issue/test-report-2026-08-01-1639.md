# IR 全量编译测试报告 2026-08-01 16:39

## 总结
- **总测试文件**: 129
- **LZ→IR 编译**: 129/129 (100.0%)
- **IR→rustc 编译**: 74/129 (57.4%)
- **失败**: 55

## 改进对比
- 上次报告 (2026-08-01 08:50): 53/129 (41.1%)
- 本次: 74/129 (57.4%) → **+16.3%**

## 本次修复内容

### 已修复/完成
1. **N17 P0**: IR层恢复Box<>类型信息 - 验证codegen已通过`type_refers_to`正确处理递归枚举Box包装
2. **IR Codegen 预置类型注入**: 添加 `__Params` 类型定义到 prelude
3. **HashMap/Dict 方法映射修复**: 
   - `contains` → `contains_key` (HashMap)
   - `IndexGet` → `get().cloned().unwrap()` (HashMap不实现Index trait)
4. **Struct derive 增强**: 所有 struct 定义添加 `#[derive(Debug, Clone)]`
5. **泛型调用语法修复**: `func[type_arg](args)` → `func::<type_arg>(args)` 通过 builder 的 Call 转换

### 错误分布
- **E0425**: 19 files (大部分为文档/占位符文件，含 undefined 变量)
- **E0308**: 5 files (类型不匹配，主要是 closure 类型推断和 combo-syntax)
- **E0277**: 4 files (trait bound 未满足，主要是 `__Params` 的 `Box<dyn Any>` 相关)
- 其余错误每类 1-2 files

### 已知未修复问题
- `checker.lz` 的 `__Params` 系统：`Box<dyn Any>` 需要 `downcast_ref` 才能使用值
- `callable_objects.lz` 的 `__call__` 机制尚未实现
- `import_demo.lz` 和 `import_more.lz` 的多文件导入系统
- `generics.lz` 的 `<int, str>` 尖括号泛型调用语法解析
- `operators.lz` 的 `?.` 安全导航操作符实现
- 多个 combo-syntax 文件的组合语法支持
