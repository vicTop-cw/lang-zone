# Issue 复核结论（2026-07-31）

**复核方式**：逐条对照编译器实际行为（grep 源码 + 实编译 `lang-zone.exe`），不轻信审计 Agent 的"文档对照"结论。
**复核对象**：`issue/findings-2026-07-31.md`（全项目找茬报告）+ `issue/syntax-docs-audit-2026-07-31.md`（SYNTAX 专项审计）。

---

## 一、`findings-2026-07-31.md`（全项目找茬报告）

| # | 该 Agent 声称 | 结论 | 实证 |
|---|---|:---:|---|
| 1 | 顶层裸 `x =:` 被静默丢弃 | ❌ **误报** | 实编译：`x =:` → `let x = (|| ... )()`，`x` 被定义且可运行 |
| 2 | 非法/溢出字面量静默变成 0 | ✅ **真 bug** | `0xG`→`0; G;`、`123...`→`0`、`123e`→`0.0`，**零报错**——典型的"编译器撒谎" |
| 3 | 未闭合字符串被静默接受 | ❌ **误报** | `"abc` → `Parse error: Unexpected token in expression: Dedent` |
| 4 | USAGE.md 记载 4 个未实现 CLI flag | ✅ **真问题** | 主编译器 `src/main.rs` 只解析 `--cached`/`--tokens`/`--ast` 等（`RUST/src/main.rs` 的 `lzrsc` 才支持 `--rust-crate`） |
| 5 | 测试计数跨文档矛盾 | ✅ **真问题** | 多文档计数不一致 |
| 6 | Lang-Zong / Lang-Zone 命名混乱 | ✅ **真问题** | USAGE.md 仍为旧架构 `lzc`/`lzcyc`/`lz-infer` |
| 7 | `josn_parse` 目录名拼写错误 | ✅ **真问题** | 确认存在该目录 |

→ 该报告 **2 处硬误报（#1、#3）**，但 #2 是真实且危险的 bug，#4–#7 是真实的陈旧文档问题。**不能整体采信，也不能整体否定。**

---

## 二、`syntax-docs-audit-2026-07-31.md`（SYNTAX 专项审计，13 条）

| # | 该 Agent 声称 | 结论 | 实证 |
|---|---|:---:|---|
| 1 | `12-操作符` vs `附录B` 优先级表两套数字 | ✅ 真 | 12 用 0–16（`.`=16/`==`=6/`|>`=1/`:`=2）；附录B 用 0–17（`.`=17/`==`=5/`|>`=2/`:`=1/`=`=1） |
| 2 | `12-操作符` 内部 `:=` 文本与表矛盾 | ✅ 真 | L362 文本"最低（仅高于管道）" vs L378 表 `0:= ·1:|> ·2:=` |
| 3 | setup/teardown 状态三处矛盾 | ✅ 真（真相=已实现） | 实编译 `setup_teardown.lz` 通过（exit 0，生成 `// === setup ===`/`// === teardown ===`）；grep 确认 lexer/parser/codegen/ir 全链路打通 |
| 4 | 缺失语法特性报告内部数字/标题矛盾 | ✅ 真 | L4 "292 lib" vs L7 "433"；L25 "P0 全完成" vs P0-1/P0-2 "❌ 真实缺失" |
| 5 | 两份 overview DEMO 计数 66 vs 56+ | ✅ 真 | 文档内计数漂移 |
| 6 | 附录A/B 两份副本且漂移 | ✅ 真 | `overview/` 与根目录各有一份附录A/B |
| 7 | 附录B 关键字表混入非关键字 `Number` | ✅ 真（违反红线） | L35 列 `Number` 为关键字，L37 自否"不是关键字"；红线决策要求 builtin 不入关键字表 |
| 8 | `06a-struct` §2.5 无前缀字段可见性未定义 | ✅ 真（我自己的缺口） | §2.1/§2.3 用 `x: f64` 无前缀，§2.5 只定义 `.`/`._` |
| 9 | `12-操作符` 优先级表缺 `..`/三元/`+=` | ✅ 真（缺口） | §二表无 `..`、`if/else`、`+=` 等复合赋值 |
| 10 | `14-生成器` 两处歧义 | ❌ **误报** | 审计称 L68 vs L600 矛盾，实则两处都说 iterator `return` ≡ `raise`；`yield (2,)` 在 §5.6 一致判非法，与 §5.2 裸 `yield` 不冲突 |
| 11 | Cell/RefCell 被标为"指针类型" | ✅ 真（语义不准） | `13-指针` L20-21、`99-内置` L93-94 将内部可变性容器与 Box/Rc/Arc 并列 |
| 12 | `03-函数.md` 索引页成孤儿 | ✅ 真（轻） | README L15 把 03 指向 `03-函数基础.md`，索引页 `03-函数.md` 未入结构表 |
| 13 | 文档版本日期混乱 | ✅ 真（轻） | 多文件头仍写 07-27，内容已被 07-31 改动 |

→ SYNTAX 审计质量高：**11 条真实、1 条误报（#10）、1 条是缺口而非矛盾（#9）**。该 Agent 这次基本靠谱。

---

## 三、本次已修复（5 项，低风险、有依据）

1. **#7 附录B**：`Number` 从关键字表移除，改入 §1.10 内建列表并标注"非关键字"（对齐红线决策）。
2. **#8 06a-struct §2.5**：补"无前缀 = 公开默认，与 `.name` 等价"；原 §2.1/§2.3 无前缀示例语义明确。
3. **#3 00-词法基础**：setup/teardown 由"设计阶段未实现"改为"已实现，内联到每个 test"（编译器实证）。
4. **#11 13-指针与引用 + 99-内置预导入库**：Cell/RefCell 改标"内部可变性容器，非指针/智能指针"。
5. **#2 12-操作符**：`:=` 文本改为与优先级表一致（2 级，高于 `|>`/`=`）。

---

## 四、待拍板的较大改动（已确认真实，需决策）

- **#1 / #9 优先级表统一**：先核对编译器 parser 的**真实**优先级，再决定 `12-操作符` 还是 `附录B` 为权威、另一份改为引用。**不要凭文档拍脑袋改数字。**
- **#6 附录A/B 去重**：删 `overview/` 副本，或反向以根目录为权威。
- **#4 / #5 overview 报告重写**：计数口径统一到 `cargo test --lib` + `compile_demos` 实际数字。
- **#12 03-函数.md 索引页**：并入 README 结构表，或合并入 `03-函数基础.md`。
- **#13 版本日期**：建议加 CI 校验或统一为"最后审阅日"。

---

## 五、一句话结论

你担心的"乱报"主要发生在 **`findings-2026-07-31.md`**（2 个硬误报，但混着一个真实编译器 bug #2）；而 **`syntax-docs-audit-2026-07-31.md` 的 SYNTAX 审计是扎实的**，13 条里只有 #10 站不住脚。**两份报告要分开看，不能一概而论"都是错的"。**
