# Issue 审计报告 — 2026-07-29

## 摘要

对 `issue/` 下所有 Open 状态文件进行复验审计。验证方法：

1. `cargo build --lib` — 0 errors, 0 warnings ✅
2. `cargo test --test compile_demos` — 77 total / 72 pass / 5 fail

## 核心发现：37 → 5 的大幅进展

原始 **P0 回归**（`parser-new-syntax-regression.md`）申报 **37 个 DEMO 红**。现仅 **5 个红**，**32 个已转绿**（+87% 修复率）。由并发前端/IR agent 完成。

进一步发现：原始 issue 文件中记录的**错误签名已过时**——现存的 5 个新错误与当初申报的错误**不同**（`Plus` → `Eq`，`expected macro name` → `Exclamation`，`got Dot`/`got Comma` → `Expected Else`），说明表层解析已恢复，失败发生在深层子特性。

---

## 各文件审计结果

### ✅ 已修复（4 个，已移入 `fixed/`）

| 文件 | 验证结果 |
|------|----------|
| `fixed/parser-dotdot-in-for.md` | `for i in 0..5` 区间迭代器通过；6 相关 demo 绿 |
| `fixed/build-block-colon-format.md` | 构建块冒号规范已修复（文档类） |
| `fixed/build-block-heading-numbering.md` | 编号断链已校正（文档类） |
| `fixed/lib-compile-breakage-shims.md` | 占位文件已补，lib 编译通过 |

状态：**全部 Fixed ✓**。维持不动。

---

### 🔄 静默修复（3 个，疑已由并发 agent 修复，需验证后关闭）

以下 3 个 issue 对应的 demo **不在当前 5 失败之内**，说明对应语法特性已可解析：

#### 1. `parser-func-def.md`（P1：泛型/闭包/复合 def）

- 原申报 5 个 demo 红（`basic.lz`、`checker.lz`、`closures_more.lz`、`composite.lz`、`generics.lz`）
- 现这些 demo 全部通过 ✅
- 证：`05_expressions/operators.lz`（只报 `Eq`，不报 def 错误）内含 `def parse_num<T>`（泛型）、`def config.get`（扩展方法）、`def parse_int(s: str) -> Result<int, str>`（返回类型）——全部成功解析
- → **工程确认后建议关闭**。**建议动作**：运行 5 个相关 demo + 验证；确认无误后移入 `fixed/`。

#### 2. `parser-import-path.md`（P1：`import a.b.c` / `from..import` / `as`）

- 原申报 2 个 demo 红（`import_demo.lz`、`import_more.lz`）
- 现这两个 demo 全部通过 ✅
- 证：`import std.io.print` 等点号路径用法在 `import_more.lz` 中成功解析
- → **工程确认后建议关闭**。**建议动作**：检查是否所有三态（`import X.Y`、`from X import Y`、`as` 别名）均覆盖；确认后移入 `fixed/`。

#### 3. `parser-comptime-const-typealias.md`（P1：comptime/const/type alias/as）

- 原申报 4 个 demo 红（`const.lz`、`type_aliases.lz`、`type_aliases_more.lz`、`type_conversion.lz`）
- 现这些 demo 全部通过 ✅
- 证：`03_variables/const.lz`、`02_types/type_aliases.lz` + `type_aliases_more.lz`、`02_types/type_conversion.lz` 全部成功 parse
- → **工程确认后建议关闭**。**建议动作**：验证 comptime 块 codegen、type alias 的跨模块可见性；确认后移入 `fixed/`。

---

### 🔴 仍 Open（5 个，有活跃失败 demo）

#### 1. `parser-operators-expr.md`（P1：二元/复合赋值/构造器/mut let）

| 当前失败 | 错误 | 与 issue 原文差异 |
|----------|------|-------------------|
| `05_expressions/operators.lz` | `Unexpected token in expression: Eq` | issue 说 `Plus` → **已修复`+`，`=` 残留** |
| `13_operators/compound_assign_more.lz` | `Unexpected token in expression: Eq` | issue 说 `Eq` → **未变** |

- 现仅 2 红（原 4：`primitives.lz`（基础构造器）与 `mutable_let.lz`（mut let）已变绿 ✅）
- 推测残留：`mx = 10` 裸赋值（非 let）或 `==`/`!=` 比较运算符；复合赋值 `+=` 仍不被识别

#### 2. `parser-control-flow.md`（P1：ternary/guard/try-raise）

| 当前失败 | 错误 | 与 issue 原文差异 |
|----------|------|-------------------|
| `10_error_handling/panic_raise_try.lz` | `Expected Else, got Colon at pos 100` | issue 说 `Expected Colon, got Dot` → **已大幅修复** |
| `07_data_structures/magic_methods.lz` | `Expected Else, got Colon at pos 928` | issue 说 `Expected Colon, got Comma` → **已大幅修复** |

- 现仅 2 红（原 4 + combo-syntax：`ternary.lz`、`guard.lz`、`match_more.lz`、`combo_ternary_walrus.lz` 已变绿 ✅ + combo-syntax 变绿 ✅）
- 推测残留：`if` 表达式用作尾返回值时要求 `else`（panic_raise_try.lz 的 `if b == 0:` 无 else；magic_methods.lz 的 `if self.index < self.items.length():` 有 else 但深层块内某 if 无 else）

#### 3. `parser-struct-enum-match.md`（P1：struct/enum/trait/match 新形态）

| 当前失败 | 错误 | 与 issue 原文差异 |
|----------|------|-------------------|
| `07_data_structures/magic_methods.lz` | `Expected Else, got Colon at pos 928` | issue 说 `Expected Colon, got Comma` → **已大幅修复** |
| `10_error_handling/panic_raise_try.lz` | `Expected Else, got Colon at pos 100` | issue 说 `Expected Colon, got Dot` → **已大幅修复** |

- 现仅 2 红（原 10：`struct.lz`、`struct_more.lz`、`trait_impl.lz`、`enum.lz`、`enum_more.lz`、`match.lz`、`match_more.lz`、`with_defer.lz` 全部变绿 ✅）
- 现有的错误已转为 `Expected Else`（if-expression 问题），非原始 struct/enum 解析失败
- **该 issue 主体实际已修复**，但父级 P0 仍挂 2 红

#### 4. `parser-top-level-decls.md`（P1：模块魔法属性/宏定义/prelude）

| 当前失败 | 错误 | 与 issue 原文差异 |
|----------|------|-------------------|
| `09_macros/macro_demo.lz` | `Unexpected token at top level: Exclamation` | issue 说 `expected macro name at token 3` → **已部分修复** |

- 现仅 1 红（原 3：`identifiers.lz`（模块魔法属性）与 `prelude_demo.lz` 已变绿 ✅）
- 推测残留：`#!bin macro` 真实 shebang 行在宏模块申明中的 `!` 未被顶层解析器识别

#### 5. `parser-new-syntax-regression.md`（P0 总览）

- **原 37 红 → 现 5 红**（32/37 已解决 = 87%）
- 5 红分布在上述 4 个 P1 子集群
- 建议：**将总览改为"5 remaining"**，并引用本审计报告

---

### ❓ 不确定状态（2 个，无失败 demo 映射）

#### 1. `frontend-keyword-downgrade.md`（P2）

- 无失败 demo 直接对应
- 性质：保留字降级为普通标识符（`panic`/`None`/`Some`/`Ok`/`Err`）——IR 迁移前置工作
- 无法通过 compile_demos 验证；需工程确认
- → **状态保留 Open，添加注记"待工程确认是否已完成"**

#### 2. `perf-benchmark-analysis.md`（P2）

- 基准方法分析部分已在 `BENCH/REPORT.md` 重写中标定（文档侧完成）
- codegen loop/sieve 未稳属工程实现
- → **状态保留 Open，添加注记"文档分析已完成；codegen 由工程跟进"**

---

## 建议操作汇总

| 优先级 | 操作 | 对象 |
|--------|------|------|
| P0 | 更新 P0 总览为"37→5 remaining" | `parser-new-syntax-regression.md` |
| P1 | 更新 3 个静默修复 issue → 标记"候选关闭" | `parser-func-def.md`、`parser-import-path.md`、`parser-comptime-const-typealias.md` |
| P1 | 更新 5 个活跃 issue 的 Error 列 + demo 列 | 本审计对应行 |
| P2 | 添加注记到 2 个不确定 issue | `frontend-keyword-downgrade.md`、`perf-benchmark-analysis.md` |
| — | 更新 `issue/README.md` 添加本审计摘要 | — |

---

*审计人：文通通（文档专家）*
*审计时间：2026-07-29 16:36*
*验证方法：`cargo build --lib` + `cargo test --test compile_demos`*
