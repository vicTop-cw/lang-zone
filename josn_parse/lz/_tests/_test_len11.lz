struct Parser =
    input: str
    pos: int

    def try_len(self: Parser)-> bool =
        n = self.input.len()
        self.pos + 1 < n

def main() =
    p = Parser("hello", 0)
    print(p.try_len())
