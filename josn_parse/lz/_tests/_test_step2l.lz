enum JsonValue =
    Null
    Bool(bool)
    Number(f64)
    String(str)
    Array(List<JsonValue>)
    Object(Dict<str, JsonValue>)

def parse_null()-> (int, JsonValue?) =
    return (0, Some(JsonValue.Null))

def main() =
    print("ok")