struct Parser =
    input: str
    pos: int

    def do_check(self: Parser, i: int) =
        while i < self.input.len():
            print(i)
            i += 1

def main() =
    p = Parser("hello", 0)
    p.do_check(1)
