struct Parser =
    input: str
    pos: int

    def do_check(self: Parser) =
        while self.pos < self.input.len():
            print(self.pos)
            self.pos += 1

def main() =
    p = Parser("hello", 0)
    p.do_check()
