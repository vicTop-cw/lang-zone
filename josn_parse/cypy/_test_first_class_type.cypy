# Test: first-class type usage
struct Pair[T]:
    first: T
    second: T

def main() -> int:
    x: int = 1
    # 获取变量的类型
    t = type(x)
    # 使用类型创建实例
    p = Pair {first: 1, second: 2}
    return p.first