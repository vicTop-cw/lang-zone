def test_0() -> int:
    defer:
        let cleanup: int = 0
    return z
    while (-(total <= ((False >= 1) < (0)))):
        break
    let x: int = 42
    guard (False != print((print(1)))) else:
        return -1

def main() -> int:
    return 0