def test_0() -> int:
    x = z
    defer:
        let cleanup: int = 0
    (print((-(-1))) * (((100 <= 100)) / (result)))
    guard (-print(((-100) <= 1))) else:
        return -1

def test_1() -> int:
    guard y else:
        return -1
    return (((-(42 / False))))
    defer:
        let cleanup: int = 0
    let x: bool = False
    return (-total)

def test_2() -> int:
    while ((-(y * (1)))):
        break
    match 42:
        case 0:
            return 0
        case _:
            return -1
    if (y - print((-3.14 == (100 != 100)))):
        return 1
    else:
        return 0
    x = print(((0.0)))
    defer:
        let cleanup: int = 0

def main() -> int:
    return 0