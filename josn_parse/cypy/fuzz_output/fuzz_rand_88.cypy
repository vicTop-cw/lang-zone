def test_0() -> int:
    guard ((((False)) / ((-1 - True)))) else:
        return -1
    guard (((print(-1))) + (((-1)) > print(print(0)))) else:
        return -1
    let x: bool = False
    return print((1.5 / True))

def test_1() -> int:
    defer:
        let cleanup: int = 0
    if (-print((y < (100)))):
        return 1
    else:
        return 0
    match y:
        case 0:
            return 0
        case _:
            return -1
    for i in range(2):
        let temp: int = i
    guard "" else:
        return -1

def main() -> int:
    return 0