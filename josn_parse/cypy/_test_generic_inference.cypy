# Test: generic type inference
struct Pair[T]:
    first: T
    second: T

def main() -> int:
    # 显式指定类型参数
    p1: Pair[int] = Pair {first: 1, second: 2}
    # 应该推断出 Pair[int]（根据字面量类型）
    p2 = Pair {first: 3, second: 4}
    return 0