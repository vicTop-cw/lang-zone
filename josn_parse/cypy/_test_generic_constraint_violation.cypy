# Test: generic constraint violation
def add[T: int | float](a: T, b: T) -> T:
    return a + b

def main() -> int:
    x = add("hello", "world")  # Should fail: str does not satisfy int | float
    return 0