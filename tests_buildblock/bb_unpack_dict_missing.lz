// 调用构建块(~:) — 字典缺少 callee 所需 key 的运行期行为演示
// 编译器不检查 key 是否齐全（使用者责任）；运行期 __Pack::Dict 查找缺失 key 触发 panic。
def need_name(name: str)-> str =
    f"hi {name}"

def main() =
    // 字典缺少 name → 运行期 panic（演示：编译器不负责此类检查）
    g = need_name ~:
        return {age: 5}
    print(g)
