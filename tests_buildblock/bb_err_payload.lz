// 错误用例 2：生成器构建块 yield 的载荷不是元组/字典/结构体/BuildParams
// 期望：语义错误（Parse error: 生成器构建块(*:) 的 yield 必须是元组/字典/结构体...）

struct Pair =
    a: int
    b: int

def drain(items: List<Pair>) =
    print("drain")

def main() =
    xs = drain *:
        yield 1 + 2
        return
    print("x")
