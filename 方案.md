# 方案.md — xlang-3mech 三机制并入 lz（lang-zone）

> 日期：2026-08-18｜基线：lang-zone v161（git@7b29f94，cargo test 全绿）
> 结论先行：**lz 已实现三机制中的 L2（BridgeRegistry）与 L3 的大半（@export codegen），缺的是 L1（extern 声明）与三层打通**。xlang-3mech 并入 lz 是"设计资产 + 规范 + 缺口补丁"并入，不是代码原样搬入。

## 一、lz 现状能力盘点（实测，带文件:行证据）

| 能力 | 状态 | 证据 |
|---|---|---|
| 属性/装饰器语法 `#[...]` | ✅ 已存在 | lexer `'#'` → `Token::At`（src/lexer/lexer.rs:851）；`#!export(X)` → `@export(X)` 特判（lexer.rs:816 段） |
| 装饰器解析挂载 | ✅ 已存在 | `parse_decorator` → `Decorator{name,args}` 挂到 fn/struct（src/parser/parser.rs:544/134-167） |
| export IR 表示 | ✅ 已存在 | `IntrinsicKind::Export(Vec<String>)`（src/ir/node.rs:219；builder.rs:6676 `@export(Rust, Python)`） |
| export codegen | ✅ 已存在（大半） | src/codegen/export.rs：`ExportTarget::{Rust,Python,C}`；@export(Rust)→`#[no_mangle] pub fn`（func.rs:33）；@export(Python)→PyO3 #[pyfunction]+#[pymodule]；@export(C)→extern "C"；DEMO/export_demo.lz |
| 中继注册表（L2） | ✅ 已存在 | `BridgeRegistry`（src/bridge/core.rs:572-582）：Bridge trait + register/find/default + resolve_import_full |
| import 桥接路径 | ✅ 已存在（rust 草案） | `import std::bridge::rust::serde_json → use serde_json`（src/bridge/rust.rs 设计注释）；`StdBridge`（src/bridge/std.rs，已接线） |
| **extern 声明（L1）** | ✅ **已实现**（2026-08-18） | `#[extern(lang)]` 装饰器（`@extern(rust)` 天然可用）+ `Ext` 类型（外部专用句柄）+ `__lz_ext_call` 分发器；DEMO/extern_demo.lz → extern_demo.rs 生成验证通过（ExtHandle struct、open_device/load_numpy 入口正确） |
| Python 正向桥接 | ⚠️ 半成品 | bridge/python.rs 是 PyO3 **反向**桥接草案（lz 导出给 Python），**未在 mod.rs 接线**；export.rs 只生成绑定源码，未挂运行时 |
| 桥接模块接线 | ⚠️ 不完整 | bridge/mod.rs 只挂 core/std/source/ffi/cli；python.rs / rust.rs / wasm.rs / shared.rs 未接线 |
| 调用台账/审计 | ❌ 缺失 | 无 ledger 概念；bridge 无调用轨迹记录 |
| 错误码体系 | ⚠️ 部分 | bridge/core.rs 有 ErrorCode 分层（core.rs:17-67），但 L1/L2 层的 E_SYM/E_TYPE/E_DUP/E_LANG 未统一 |
| 自举双路前端 | ✅ 基础存在 | src/frontend/lz_lexer.lz、lz_parser.lz 有 At/decorator token 处理；tests/dual_route_golden.rs 校验双路一致 |
| 测试基线 | ✅ 320+ 用例 | cargo test 全绿（本次实测 42+ 套件通过） |

## 二、xlang-3mech ↔ lz 映射（并入什么、不并入什么）

| xlang-3mech 资产 | lz 对应物 | 并入方式 |
|---|---|---|
| L1 extern 声明（FFI/ctypes） | **缺** | **新增语法**：`extern "rust" { fn f(i64,i64)->i64 }` + 或复用 `import std::bridge::rust::*` 路径 |
| L2 Registry + call + 类型检查 | BridgeRegistry | **扩展**：补符号注册 API、类型检查、E_SYM/E_TYPE/E_DUP/E_LANG 错误码 |
| L2 Ledger 台账 | 缺 | **新增**：bridge 调用轨迹台账（对齐 Tnr 的审计风格，追加式 TSV） |
| L3 #[export(lang)] proc-macro | @export(Rust/Python/C) codegen | **不搬** proc-macro——lz 的 codegen/export.rs 已生成绑定；搬它的**规范**（L3 必须经 L1/L2 落地） |
| 类型安全分发器模式 | codegen 已内联处理 | 仅参考 |
| spec.md 三机制分层规范 | 缺 | **并入文档**：SYNTAX/ 新增「跨语言接口」章节 |
| 双语言一致性契约 | 缺 | **并入测试**：DEMO 正例 + tests 差异校验（Rust 产物 vs 绑定产物输出一致） |

**核心判断**：xlang-3mech 的价值是"把三机制的分层讲清楚 + 最小验证闭环"，lz 需要的是"规范文档 + extern 语法 + registry 打通 + 台账"，而非重复实现。

## 三、三机制在 lz 的落地设计

### L1 extern 声明（新增语法）

```
extern "rust" {
    fn serde_json_to_str(v: str) -> str;   # → use serde_json::to_string 的声明
}
extern "python" {
    fn numpy_array(n: int) -> PyObject;    # → PyO3 import numpy 的声明
}
```

- lexer：新增 `extern` 关键字 token（Token::Extern）。
- parser：解析 `extern "<lang>" { fn name(sig); }` → `Stmt::ExternBlock { lang, fns }`。
- 双路：Rust 前端（src/frontend/lz_lexer.rs/parser.rs）+ .lz 前端（lz_lexer.lz/lz_parser.lz）同步实现，dual_route_golden 加用例。

### L2 中继注册（扩展现有 BridgeRegistry）

- `BridgeRegistry` 增加：`register_symbol(name, lang, sig)`、`call(name, args)`（类型检查 + 分发 + 台账）。
- extern 块解析后**自动登记**进 registry；@export 产物 codegen 时**登记** export 事件。
- 错误码统一：E_SYM / E_TYPE / E_DUP / E_LANG（对齐 xlang spec，挂到 bridge/core.rs 的 ErrorCode 或独立表）。
- 台账：`<out>/bridge-ledger.tsv` 追加式（ts/event/lang/detail），`lzc --emit-bridge-report` 可审计。

### L3 #[export(lang)]（补齐现有 @export）

- 语法：`#[export(rust)]` ≡ `@export(Rust)`（lexer 已把 `#` 映射为 `@`，零改动）。
- codegen 补全（src/codegen/export.rs）：
  - export(Rust)：现状已生成 `#[no_mangle] pub fn` → **补**：同时生成 registry 登记代码。
  - export(Python)：现状生成 PyO3 绑定源码 → **补**：Cargo.toml pyo3 精确版本注入（依赖铁律）+ 运行时装载。
  - export(C)：现状 extern "C" → 保持。
- 分层约束：export 产物的 extern 声明（L1）+ registry 登记（L2）由 codegen 自动生成，不得绕过。

### 桥接层接线补全

- bridge/mod.rs 挂上 python.rs、rust.rs、wasm.rs、shared.rs（设计已写，接线遗漏）。
- python.rs 增加**正向**桥接（lz 调用 Python：PyO3 Python::import_module 路线），与现有反向桥接互补。

## 四、已知缺口清单

| # | 缺口 | 现状 | 影响 | 优先级 |
|---|---|---|---|---|
| G1 | **extern 声明语法缺失** | ✅ **已实现**（2026-08-18）：`#[extern(lang)]` 装饰器 + `Ext` 类型 + `__lz_ext_call` 分发器；`extern_demo.lz` 生成验证 exit 0 | L1 已可表达 | ~~P0~~ 已关闭 |
| G2 | 17 条错误检测缺口 | div-tools/syntax_probes/ 17 个反例静默通过（重复枚举变体/未知类型/参数个数不匹配等），tests/syntax-matrix-report.md 反例 8/25 | 错误代码可编译通过 | P0 |
| G3 | 字符串切片 byte-index panic | ✅ **已修复**（2026-08-18）：codegen 改 `chars().collect()` char 安全索引（mod.rs:7070/7098）；DEMO/string_index_unicode.lz 运行验证：`"你好世界"[1]`→22909（'好'）、`"ab🚀cd"[2]`→128640（'🚀'），不 panic | 中文/emoji 字符串崩溃 | ~~P0~~ 已关闭 |
| G4 | 桥接模块未接线 | python.rs/rust.rs/wasm.rs/shared.rs 有设计无接线 | 桥接能力不可用 | P1 |
| G5 | 调用台账缺失 | 无 ledger/审计 | 不可追踪 | P1 |
| G6 | D2 codegen 缺口 | impl 块/列表推导/生成器/match 模式不完整（bootstrap/05-自举里程碑台账.md 记录） | 自举 codegen 覆盖不全，不阻塞稳定自举 | P2 |
| G7 | embed 属性宏未实现 | 仅方案（本会话 embed-plan 产出） | `#[embed(rust/py)]` 不可用 | P2 |
| G8 | PyO3 依赖未注入 | export(Python) 生成绑定但 Cargo.toml 依赖机制未定 | Python 导出产物不可编译 | P1 |

## 五、走偏项清单（实现/计划走偏）

| # | 走偏 | 说明 | 纠正 |
|---|---|---|---|
| D1 | bridge 设计与接线不一致 | 6 个 bridge 文件只挂 4 个；rust.rs 的 `import std::bridge::rust::*` 机制有设计无测试 | 接线 + 补 E2E |
| D2 | 错误检测落后于语法解析 | 语法矩阵正例 38/38 但反例 8/25——解析器"宽容"过度 | 按 G2 补齐，反例 25/25 |
| D3 | embed/bridge 概念曾混淆 | 本会话早前把 `import embed.python.numpy`（内嵌）与 `bridge.python.numpy`（桥接系统 Python）搞反，用户已纠正 | 本方案 L1 extern 严格区分两种路径 |
| D4 | @export(Python) 有生成无验证 | export.rs 生成 PyO3 代码，但无产物编译/运行验证 | 补 E2E（生成→cargo build→import 运行） |
| D5 | 自举里程碑与主语法脱节 | bootstrap/ 台账 D2 缺口长期挂账；前端自举文件因 G3 被 ASCII 化规避 | G3 修复后恢复自举前端非 ASCII 字面量 |

## 六、实施顺序

| 阶段 | 内容 | 验收 |
|---|---|---|
| I1 规范落地 | SYNTAX/ 新增「跨语言接口」章节（extern/中继/export 三机制 + 错误码）；方案.md 归档 | 文档评审 |
| I2 L1 extern | ✅ **已完成**（2026-08-18）：lexer/parser 通用装饰器解析（`@extern(rust)` 天然可用）；`extern_demo.lz` 编译通过，生成 ExtHandle struct + `__lz_ext_call` 分发器 + open_device/load_numpy 入口，无 extern 关键字 | dual_route 一致；`extern "rust"` 编译通过 |
| I3 L2 打通 | BridgeRegistry 增 register_symbol/call/台账；extern 块自动登记；E_SYM/E_TYPE/E_DUP/E_LANG | 单测 + 台账落盘 |
| I4 L3 补全 | export codegen 生成 registry 登记；export(Python) 依赖注入；bridge 模块接线 | export_demo 三目标产物可编译 |
| I5 缺口修复 | G3 ✅ **已完成**（2026-08-18，char 安全切片运行验证通过）→ G2（17 条检测）→ G5（台账 E2E） | 反例 25/25；非 ASCII 切片不 panic ✅ |
| I6 收口 | 差异校验（Rust 产物 vs 绑定输出一致）、文档、回归 | cargo test 全绿不减少 |

## 七、风险

- extern 语法与既有 `import std::bridge::*` 路径重叠 → extern 声明是"显式声明"，import 是"路径解析"，两者共存：extern 块编译期转成 bridge 路由登记。
- 双路前端同步成本 → 只改 token/parser 结构，不做语义重逻辑。
- PyO3 依赖引入 → 精确版本锁定 + feature 隔离（未用 Python 导出不启用）。
- 台账与 Tnr 审计体系不一致 → 独立 TSV，后续对齐。

## 八、本次交付物清单

1. 本方案.md（整合方案 + 能力盘点 + 缺口/走偏清单）
2. xlang-3mech 仓库（三机制规范 spec.md + 双语言最小验证，作为 lz 集成的参考实现与验收基线）
