# 06-自举交接续推文档

> 本文档面向下一个接手者：说明 40% 里程碑当前水位、40%→100% 的续推路径、启动/停止/恢复点与风险项。
> 配套：`05-自举里程碑台账.md`（权重与证据）、`01-04`（自举规范）。

---

## 1. 当前水位（2026-08-17，100% 里程碑 v160）

- **100% 里程碑已达成（权重合计 100%）**：
  - 50% 部分不变：A 词法 12%、B 语法 15%、C1/C2/C3 20%、F 10%、G1 4%（合计 61%）。
  - **D1 15%（本轮完成）**：LZ 版 codegen 关键路径——`src/ir/lz_codegen_lib.lz`（rust_type 映射/表达式/语句/Item/模块组装/f-string 模板解析/Range/Dict/Set/HashMap 索引 get/insert/contains 参数剥离/while-true→loop/字符串转义/魔法常量 &str 渲染/f64 字面量 .0 后缀）；`--emit=rs-lz` 递归管线；`lz_emit_rs_lz_matches_native_codegen` 逐字节断言。
  - **D2 10%（本轮完成）**：`bootstrap/work/lz_codegen/diff_rs.ps1` 实测 8 个 DEMO + 1 探针双路 .rs 逐字符一致且编译运行通过。**已知缺口**（不伪装覆盖）：impl 块生成（struct/trait_impl）、列表推导（comprehension）、生成器/闭包/ref 绑定（keywords/primitives）、match 模式、__lz_main 重命名（guard）。
  - **E1 6%（本轮完成）**：`bootstrap/work/lz_builtins/core_subset.lz` 8 个纯函数转译+rustc+运行正确。
  - **E2 4%（本轮完成）**：`lz_builtins/src/runtime/lz_bootstrap_builtins.rs` + `cargo test -p lz_builtins` 8/8。
  - **G2 4%（本轮完成）**：覆盖映射表 15/15（§6），命中率 100%。
- **基线**：cargo test 320/0（50% 基线 319 +1）；DEMO 全量 261/261；bootstrap closed 闭环 13/13 退出码 0；rustc/cargo 1.96.0。
- **版本**：v160（VERSION_PATCH 159 → 160）。

## 2. 续推路线图（100% → 后续维护）

> 100% 里程碑已覆盖全部 7 个自举环节（A–G），权重表全线验证。后续工作为长期维护与深化：

| 序 | 项 | 说明 |
|---|---|---|
| 1 | D2 缺口补齐 | impl 块生成、列表推导、生成器/闭包、match 模式等 codegen 规则逐步覆盖 |
| 2 | LZ 编译器缺陷修复 | E1 阶段发现的 3 个编译器缺陷（str 切片 codegen、bool 提前返回、内联标注） |
| 3 | 覆盖率行级报告 | 安装 cargo llvm-cov 后按 02 长期门槛升级 |
| 4 | D1/D2 全量 DEMO 覆盖 | 将 diff_rs.ps1 输入集扩展到全部 261 个 DEMO |

> C2/C3 已在 50% 里程碑完成，从续推表移除；剩余权重 4+15+10+6+4+4 = 43%（与 50%→100% 口径吻合，含既有完成项未计入的 G1 缓冲口径见 05 §2）。

## 3. 启动命令 / 停止与恢复点

- **一次完整自举构建**：`powershell -File bootstrap\build.ps1 -Mode Closed`（或 `bash bootstrap/build.sh closed`）。退出码含义与日志定位见 `05` §5。
- **回滚**：`bash bootstrap/rollback.sh`（或 `.\bootstrap\rollback.ps1`）。恢复上一个可用构建（backup/good）。
- **停止/恢复总则**：每环节完成即更新台账状态列 + 提交引用 + 产物哈希；恢复时先重放一轮 `closed` 确认产物与 manifest 一致再继续；不要删除历史构建产物与 work 快照。
- **前端 .lz → .rs 同步**：`lang-zone.exe src\frontend\lz_parser.lz --std-dir std` 会在 .lz 旁生成 .rs（须与 .lz 一起提交）；改完 .lz 必跑 `cargo test` + `bash bootstrap/build.sh all`。

## 4. 风险清单

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| 深嵌套/长行触发编译器栈溢出（大组件转译） | 中 | 高 | 沿用扁平化策略（`src/ir/lz_codegen.rs` 中间变量法）；新组件函数拆分规避；v176 大栈已缓解 |
| codegen 残余 E0507 缺口（方法实参 `obj.m(list[i])`、枚举实参 `Some(list[i])`、`match list[i]:`） | 中 | 中 | 当前自举源码不触发；自然化写法注意规避（先 `let t = xs[i].clone()`）；修复方案见 `div-tools/probes/p17_defects.lz` 同批探针与 v162 修复点 `src/ir/codegen/mod.rs` 5053–5078 |
| rustc 非确定性导致 .exe 哈希不一致 | 低 | 低 | 降级口径：.rs 哈希一致 + .exe 运行输出比对一致（05 §5） |
| `--emit=lex-lz/parse-lz` 与默认管线分叉 | 低 | 低 | 两开关只做"独立调用"路径，默认管线不变；回归由 cargo test + DEMO 全量覆盖 |
| CI 清单与现状脱节（run_check.sh/compile_demos 不存在） | 中 | 中 | G 环节待办：以 regression.sh / cargo test 为准更新 CI |

## 5. 与 progress-reports / history-work 的归档关系

- 每环节完成 → `history-work/` 追加工作记录；KPI 变化 → `progress-reports/` 按既有日期目录追加（只增不删不改）。
- 台账 `05` §3 状态列与 §7 修订记录同步更新，哈希登记回填 §4。

## 6. 关键路径覆盖映射表（≥70% 口径，见 05 §6.3）

| # | 关键路径 | 组件 | 命中用例 | 状态 |
|---|---|---|---|---|
| 1 | .lz → .rs 转译（IR 路线） | lzc 主链路 | `tests/lz_ir_bootstrap.rs::lz_written_ir_display_compiles_and_runs` | ✅ |
| 2 | .rs → rustc → 运行（--extern lz_builtins） | 构建链 | 同上 + `tests/lz_frontend_bootstrap.rs` 各用例 | ✅ |
| 3 | LZ 版 lexer 行为对齐（token 流） | A1 | `tests/lz_frontend_bootstrap.rs::lz_lexer_matches_native_golden` | ✅ |
| 4 | LZ 版 lexer 主流程入口（--emit=lex-lz） | A2 | 同上文件 `::emit_lex_lz_flag_runs` | ✅ |
| 5 | LZ 版 parser 行为对齐（解析描述） | B1 | `tests/lz_frontend_bootstrap.rs::lz_parser_matches_golden` | ✅ |
| 6 | LZ 版 parser 主流程入口（--emit=parse-lz） | B2 | 同上文件 `::emit_parse_lz_flag_runs` | ✅ |
| 7 | IR display LZ 化（--emit=ir-lz 回环） | C1 | `tests/lz_ir_bootstrap.rs::lz_emit_ir_lz_roundtrip` | ✅ |
| 8 | 构建闭环双轮一致 + 回滚 | F | `bootstrap/work/fail_drill.sh`（脚本级断言） | ✅ |
| 9 | 失败退出码与日志定位 | F3 | fail_drill 步骤 3 断言 | ✅ |
| 10 | 干净检出重建 | F2 | 本里程碑验证记录（clean checkout 双轮） | ✅ |
| 11 | IR display 双路逐字符一致（--emit=ir vs --emit=ir-lz） | C2 | `tests/lz_ir_bootstrap.rs::lz_emit_ir_lz_matches_ir_byte_exact` + `bootstrap/work/lz_ir/diff_ir.ps1`（8/8） | ✅ |
| 12 | 双路 diff 自动化脚本退出码语义 | C3 | diff_ir.ps1 实测退出码 0（8/8 一致） | ✅ |
| 13 | Rust codegen 双路逐字符一致（常规 .rs vs --emit=rs-lz） | D1 | `tests/lz_ir_bootstrap.rs::lz_emit_rs_lz_matches_native_codegen`（逐字节断言 + rustc 编译 + 运行） | ✅ |
| 14 | D2 输入集双路 .rs diff + 编译运行 | D2 | `bootstrap/work/lz_codegen/diff_rs.ps1`（实测 8 个 DEMO + 1 探针逐字符一致） | ✅ |
| 15 | LZ 版 builtins 并入 crate | E1/E2 | `cargo test -p lz_builtins`（8/8，lz_bootstrap_builtins 单测） | ✅ |

**命中率：15/15 = 100% ≥ 70% ✓**

## 7. 修订记录

- 2026-08-17 v2（50% 里程碑，v159）：水位升至 50%（权重 61%）；续推路线图移除已完成的 C2/C3（剩 39%）；覆盖映射表新增 C2/C3 两条路径（12/12）。C2/C3 详情见 05 §7 修订记录 v3。
