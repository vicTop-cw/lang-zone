// bootstrap test8: 综合测试 (struct + list + enum)
enum Color:
  Red
  Green
  Blue

struct Point =
  x: int
  y: int

def sum_list() -> int =
  nums = [10, 20, 30]
  nums[0] + nums[1] + nums[2]

def main() =
  print(15)
  print(sum_list())
