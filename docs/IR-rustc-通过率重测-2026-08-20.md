---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 9f2a11add43fbf12a546606fb2b962ab_63a0a9309c6111f184de525400f8a581
    ReservedCode1: eQSSqtxEL7AfsfWitwwIQHfsvQA+0Icgx1PX6wJWw/1EM98IvSgfdtDXty1ALI59M7Isi6kzpH3IVEkhQoYfbkBO0b3AV/gfwNTdH6WZh3znRCPSSDSRuXwi6Oec8vQWoJPS0i3pqX0xlcH+eynfILpT/WmYu3s5otcrzFq1yU94Trsc4OtsYbcfd8c=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 9f2a11add43fbf12a546606fb2b962ab_63a0a9309c6111f184de525400f8a581
    ReservedCode2: eQSSqtxEL7AfsfWitwwIQHfsvQA+0Icgx1PX6wJWw/1EM98IvSgfdtDXty1ALI59M7Isi6kzpH3IVEkhQoYfbkBO0b3AV/gfwNTdH6WZh3znRCPSSDSRuXwi6Oec8vQWoJPS0i3pqX0xlcH+eynfILpT/WmYu3s5otcrzFq1yU94Trsc4OtsYbcfd8c=
---

# IR→rustc 通过率重测报告（v164）

> 版本：v164（e08e9ef）｜ 日期：2026-08-20 ｜ 目的：刷新 KPI「IR→rustc 通过率」（旧数据 56.6%/73 of 129 引用自旧文档，已过时）

## 一、语料规模与方法

- **语料来源**：DEMO/（01_basics~08_modules、boundary-coverage、g6、lz_std 等）、div-tools/probes（含 .diffwork 工作噪声）、div-tools/syntax_probes、benchmark/src、src/frontend、src/ir、std 等现存 .lz 用例，复制到隔离目录 `temp/corpus_ir_pass_121927`（401 个）。
- **分类**：按命名规则识别 expected-reject 46 个；其余 evaluable **355** 个。
- **流程**：`lang-zone <src.lz>` 单文件模式生成 .rs（IR codegen）→ `rustc --edition 2021 --extern lz_builtins -A warnings` 编译；lz 失败（rc≠0）记为 lz_fail，lz 成功但 rustc 失败记为 rustc_fail，rustc 成功记为 pass。
- **降噪**：首轮并行（6 workers）出现 148 个 `linking_with_link.exe_failed`，串行复测 10/10 通过，判定为并发链接噪声；**最终以串行全量重跑 287 个 lz 通过语料为准**（耗时 112.3s）。
- 数据文件：`temp/ir_pass_results.json`（首轮全量）、`temp/corpus_ir_pass_121927/`（语料隔离副本）。

## 二、结果总览

| 指标 | 数值 |
|---|---|
| evaluable 语料 | 355 |
| **通过（lz→.rs→rustc 全链路 rc=0）** | **266** |
| 通过率 | **74.9%**（旧数据 56.6%，+18.3pp） |
| lz 失败 | 68（55 semantic / 11 parse / 2 other） |
| rustc 失败 | 21 |
| expected-reject 语料 | 46 中 44 被 lz 正确拒绝（符合预期，另有 2 个未按预期拒绝，列入 lz 侧观察项） |

## 三、rustc 失败类别（21 个，主战场）

| 类别 | 计数 | 代表性用例 | 错误实质 |
|---|---|---|---|
| E0308 类型不匹配 | 10 | p22_str_index.lz（`s[idx]` codegen 为 `__cs[__i] as i64` 但函数返回 String）；p27_opt_elif.lz；.diffwork/lexer_*.lz（6 个，内部工作噪声） | 分支/索引返回类型提升不一致，未统一 `.to_string()` 或类型降级 |
| E0425 未绑定符号 | 7 | round15_use.lz、polish_27_use.lz、p5_alias.lz（use/import 导入符号未注入作用域）；p16_lzi.lz（extern 声明在单文件模式未生成可调用 shim）；lit_probe.lz（format! 中 name/val 未绑定） | use/import 作用域 codegen 缺失；extern 无 registry 注入时无符号 |
| E0599 方法不存在 | 2 | p24_slice_method.lz（`src.slice(0,3)`，builtins 未提供 `slice` 映射，suggestion 提示 `lz_slice`）；ps1.lz | lz_builtins 方法名映射缺口（slice → lz_slice） |
| E0609 字段不存在 | 1 | p22_option_pattern.lz | Option 模式解构字段访问 codegen 不匹配 |
| E0605 外部 crate 解析 | 1 | p41_full_tokenize.lz | 外部依赖引用在单文件编译无 extern 注入 |

## 四、lz 失败类别（68 个）

| 类别 | 计数 | 目录分布 | 代表性用例 | 实质 |
|---|---|---|---|---|
| semantic | 55 | div-tools/probes 19、DEMO/01_basics 9、DEMO/06_control_flow 7、DEMO/boundary-coverage 7、DEMO/lz_std 7 等 | enums.lz（未知类型 Tag，前向引用）；functions_advanced.lz（未绑定变量 args，变参）；variadic_dotdot.lz | 语义检查器对合法语法误报（前向引用/变参 args/类型推断）为主，部分是语料本身依赖未实现特性 |
| parse | 11 | 分布同上 | p13_comp_list.lz、p2_pipe.lz 等 | 语法未支持或探针用例超出冻结语法 |
| other | 2 | — | — | 其他入口错误 |

## 五、瓶颈分析（决定 G6 后续 codegen 提升方向）

1. **优先级 P0 · E0308 类型提升（10/21 rustc 失败）**：字符串索引、分支表达式返回类型在 codegen 中未统一（i64 vs String），修 `s[idx]` 索引路径与 if/else 分支类型收敛即可直接提升约 4.8pp（17/355）。
2. **优先级 P0 · E0425 use/import 作用域注入（7/21）**：`use X::Y` / `from X import Y` 的导入符号未进 codegen 作用域；extern 声明在无 registry 单文件模式不生成 shim。修复后预期 +2.0pp（7/355）。
3. **优先级 P1 · builtins 方法映射（E0599×2、E0609×1）**：`String::slice`→`lz_slice` 等别名映射补齐，低成本小收益。
4. **优先级 P1 · lz 语义误报收敛（68 个）**：55 个 semantic 失败中相当比例为前向引用/变参 args/类型推断误报，收敛误报可把通过率上限推高至 85%+；但需逐例确认语料合法性，属中成本。
5. **噪声说明**：.diffwork/ 下 6 个 E0308 为 probes 内部工作目录噪声；并行链接噪声（148 个）已串行复测排除。

## 六、结论

- 当前 **IR→rustc 通过率 = 74.9%（266/355）**，较旧数据 56.6% 提升 18.3pp；rustc 侧真实失败仅 21 个（5.9%），主要瓶颈集中在 E0308 类型提升与 E0425 导入作用域。
- 建议 G6 后续 codegen 提升顺序：**① E0308 类型收敛 → ② E0425 use/extern 作用域 → ③ builtins 方法别名 → ④ 语义误报收敛**。
- 本任务为纯测量，无代码修复，不 bump 版本。
*（内容由AI生成，仅供参考）*
