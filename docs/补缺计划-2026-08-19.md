---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 9f2a11add43fbf12a546606fb2b962ab_03e8e98c9b8c11f19467525400287e28
    ReservedCode1: 0KsfGNGQ3l0hqXWNHtpw6H1pNl2in58V2+jTS3qJOjqo1dOfwPfwfpRHj9xZFm4A9LQszc9EmNJAeUyUgPpfNJU6cHL03Q5eWzy+Stp2Wpbp/efsIENgnx1VcNeg6VWrotx0he8mXzD52htWlgiB+usxOV9KFtQKmDfVqpfUfy6pHuvyBM5zJ3ZcQGk=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 9f2a11add43fbf12a546606fb2b962ab_03e8e98c9b8c11f19467525400287e28
    ReservedCode2: 0KsfGNGQ3l0hqXWNHtpw6H1pNl2in58V2+jTS3qJOjqo1dOfwPfwfpRHj9xZFm4A9LQszc9EmNJAeUyUgPpfNJU6cHL03Q5eWzy+Stp2Wpbp/efsIENgnx1VcNeg6VWrotx0he8mXzD52htWlgiB+usxOV9KFtQKmDfVqpfUfy6pHuvyBM5zJ3ZcQGk=
---

# LZ 补缺主计划（单一事实源）

> 版本：v1（2026-08-19）｜ 基线：v162（a4a6a25 / ec37e78）｜ 状态：执行中
> **本文档是 lz 补缺工作的唯一主计划。** 方案.md 保留为「xlang-3mech 并入方案」参考；规划建议-语法冻结版-2026-08-19.md 数据过时，降级为旁证，不再作为排期依据。
> 所有 Agent 派发、提交、验收一律引用本文档的编号（G#/I#/S#）。

---

## 一、现状基线（2026-08-19 复核）

| 项 | 事实 |
|---|---|
| 版本 | v162（a4a6a25 Ext+extern+G3；ec37e78 FIST T4+IR 序列化+测试扩充），v163 待 bump |
| 回归 | 392 tests passed / 0 failed（v162 全量记录） |
| 已关闭缺口 | G1（extern L1）、G3（char 安全切片） |
| 已落地 FIST T4 | incr.rs（模块级缓存+依赖图+级联失效+rayon）、hotreload.rs（watch）、lsp.rs（lsp）——**增量编译已存在，不再重复排期** |
| 并发外部工作 | Cy 后端开发中（codegen_cython.rs 中间态，crate 暂不可编译）→ 阻塞 cargo test 与提交 |
| 语法冻结 | **待用户确认**。本文档默认「不新增语法结构」为约束，但冻结范围以用户拍板为准 |

---

## 二、缺口清单总表

| # | 缺口 | 优先级 | 状态（2026-08-19） | 验收锚点 |
|---|---|---|---|---|
| G1 | extern 声明语法缺失 | P0 | ✅ 已关闭（v162） | extern_demo.lz exit 0 |
| G2 | 17 条错误检测缺口（重复枚举变体/未知类型/参数个数不匹配等） | P0 | 🔧 实施中（parser/lexer 已改，syntax-matrix-report 已动，**未编译验证**） | 反例 25/25 拒绝 |
| G3 | 字符串切片 byte-index panic | P0 | ✅ 已关闭（v162） | 非 ASCII 切片不 panic |
| G4 | 桥接模块未接线（含 Python 正向桥接 Mojo 式 from python import） | P1 | 🟡 代码已完成，待统一验证（python.rs shim + resolve/gen 函数 + 单测） | bridge 六文件全接线 + E2E |
| G5 | 调用台账缺失 | P1 | 🟡 代码已完成，待统一验证（ledger.rs 追加式 TSV + `lzc emit-bridge-report` CLI） | 台账可审计 |
| G6 | D2 codegen 缺口（impl 块/列表推导/生成器/match） | P2 | ⬜ 未开始 | codegen 覆盖补齐 |
| G7 | embed 属性宏未实现（#[embed(rust/py)]） | P2 | 🔧 实施中（parser/expr/stmt 有改动，**未编译验证**） | embed 内嵌代码段可展开运行 |
| G8 | PyO3 依赖未注入 | P1 | 🟡 代码已完成，待统一验证（Cargo.toml pyo3 0.22.6 + py-bridge feature + docs/py-bridge-struct.md） | export(Python) 产物可编译；**作为 Cy 后端对齐基准** |

**外部依赖**：G2/G4/G5/G7/G8 的最终验收都依赖 crate 可编译（cargo test 全绿），而 Cy Agent 的中间态阻塞了编译。**在 Cy 稳定前，所有「待统一验证」保持黄色，不关闭。**

---

## 三、实施顺序与进度

| 阶段 | 内容 | 进度（2026-08-19） |
|---|---|---|
| I1 | 规范落地（SYNTAX 跨语言接口章节 + 方案归档） | ✅ 完成 |
| I2 | L1 extern | ✅ 完成（v162） |
| I3 | L2 中继打通（register_symbol/call、extern 自动登记、错误码 E_SYM/E_TYPE/E_DUP/E_LANG、台账联动） | 🟡 核心 API 完成；extern 自动登记接线点已定位（ir/builder.rs:6693、ir/codegen/mod.rs:583/914），**待 Cy 稳定后接线验证** |
| I4 | L3 补全（export registry 登记、export(Python) 依赖注入、bridge 接线） | 🟡 G4/G8 完成；export codegen 生成 registry 登记待验证 |
| I5 | 缺口修复（G2 → G5） | 🔧 G3 ✅；G2 实施中；G5 代码完成待验证 |
| I6 | 收口（差异校验、文档、回归、bump v163 提交） | ⬜ 待 Cy 稳定后统一执行 |

---

## 四、主线 KPI（来自语法冻结版，重新校准）

| KPI | 现状 | 目标 | 说明 |
|---|---|---|---|
| IR→rustc 通过率 | 56.6%（73/129，**引用自旧文档，待重新测量**） | ≥90% | codegen 后端主战场，与 G6 重叠，合并推进 |
| 反例通过率 | 8/25 | 25/25 | = G2 |
| 回归测试 | 392 | 不减少 | 每次提交校验 |
| 行级覆盖率 | 无 | llvm-cov 行 90%+ | 阶段 2 项，不阻塞本轮 |

**FIST T4 已完成项不再重复排期**：增量编译（incr.rs）、热重载（hotreload.rs）、LSP（lsp.rs）已落地；仅 hotreload/lsp 逻辑完整性待收尾（含 lsp.rs unused import 清理）。

---

## 五、明确不做（红线）

1. 不新增任何关键字、运算符、语法结构（语法冻结前提，待用户确认）
2. 不新增 extern 块语法——`#[extern(lang)]` 装饰器已实现，不引入 `extern "rust" {}`
3. 不碰 Cy 后端（codegen_cython.rs、CY/ 目录）——由用户另一 Agent 负责，结构对齐 docs/py-bridge-struct.md
4. 不在老 AST 路线（src/codegen/）新增功能，只作迁移对照
5. 不引入字节码 VM（除非热重载成为硬需求）
6. 不抄 std/modules/*.toml 进 lz.std

---

## 六、防目标偏离执行规则（本计划生效条款）

1. **单一事实源**：本文档是唯一排期依据。其他规划文档（方案.md、规划建议-语法冻结版、LZ_UPGRADE_PLAN.md）只作参考，禁止据此另起任务。
2. **编号制**：所有 Agent 派发、代码提交、验收报告必须引用 G#/I# 编号。无编号的工作不进主线。
3. **对账机制**：每次提交后 1 小时内更新本文档状态列（✅关闭/🟡待验证/🔧实施中/⬜未开始）。
4. **数据新鲜度**：规划类内容落笔前必须对账 git log + cargo test 数量；文档头部必须标注基线版本。
5. **验收锚点制**：Agent 自报"完成"不算数，必须对照第二/四节的硬指标（反例 25/25、392 tests 不减少、export 产物可编译等）。
6. **并发边界**：多个 Agent 并行时，各 Agent 只动自己编号范围内的文件；发现对方中间态/越界，停下汇报主 Agent，不代改。
7. **阻塞透明**：遇到外部阻塞（如 Cy 中间态），主计划状态保持黄色，不降级验收标准。

---

## 七、当前阻塞与下一步

**阻塞**：Cy Agent 编辑 codegen_cython.rs 中间态 → crate 不可编译 → cargo test / 提交延后。
**下一步**（按序）：
1. 等 Cy Agent 提交稳定（用户通知）
2. file-agent 统一验证：接线 extern 自动登记 → cargo test 全绿（392+ 不减少）→ 反例 25/25 复核 → bump v163 → 提交
3. 提交后对账本文档，G2/G4/G5/G7/G8 按实际结果转 ✅ 或回退 🔧
4. 独立主线：G6（D2 codegen 补齐）与 IR→rustc 通过率提升，可与 Cy 稳定并行推进（不依赖编译验证的部分先行）
*（内容由AI生成，仅供参考）*
