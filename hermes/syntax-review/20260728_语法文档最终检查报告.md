# LZ 语法文档最终检查报告 v10.0

> 检查范围：`E:\IDEProjects\AI\lang-zone\SYNTAX\` 全部 29 份文档
> 检查日期：2026-07-28
> 基于上一轮 v9.0 报告，本轮重新全量检查

---

## 变更摘要

| 类别 | v9.0 | 本轮已修复 | 本轮新增 | 合计 |
|:----:|:----:|:--------:|:--------:|:----:|
| P0 关键矛盾 | 0 | 0 | **0** | **0** |
| P1 重要欠缺 | 9 | 0 | **3** | **12** |
| P2 文档质量 | 28 | 0 | **2** | **30** |
| **合计** | **37** | **0** | **5** | **42** |

> 本轮修复率 0%（v9.0 的 37 个问题均未修复），新增 5 个问题（3 P1 + 2 P2）。P0 问题仍为零。

---

## P0 — 关键矛盾 / 阻塞问题（0 项）

> 无 P0 问题。

---

## P1 — 重要欠缺 / 遗漏（12 项）

### 1. `99-内置预导入库.md` 全局函数签名与魔法方法签名不匹配 [仍存在]

**文件**：[99-内置预导入库.md](99-内置预导入库.md)

| 全局函数 | 文档签名 | 实际魔法方法签名 | 差异 |
|---------|---------|----------------|------|
| `str(x)` | `(T) -> str` | `fn fmt(&self, f: &mut Formatter) -> fmt::Result` | 参数和返回值完全不同 |
| `clone(x)` | `(T) -> T` | `fn clone(&self) -> Self` | 借用 self vs 消费 |
| `drop(x)` | `(T) -> ()` | `fn drop(&mut self)` | 可变借用 vs 消费 |

**建议**：在全局函数表中标注这些函数是魔法方法的**包装器**（wrapper），并添加交叉引用链接。

---

### 2. `sort`/`reverse` 与 `filter`/`map` 签名风格不一致 [仍存在]

**文件**：[99-内置预导入库.md](99-内置预导入库.md)

| 函数 | 文档签名 |
|------|---------|
| `sort(xs)` | `(List<T>) -> List<T>` |
| `reverse(xs)` | `(List<T>) -> List<T>` |
| `map(xs, f)` | `(Iter<T>, fn(T)->R) -> Iter<R>` |
| `filter(xs, f)` | `(Iter<T>, fn(T)->bool) -> Iter<T>` |

`sort`/`reverse` 接受 `List<T>` 而 `map`/`filter` 接受 `Iter<T>`，风格不统一。

**建议**：统一为迭代器风格，或明确说明原因。

---

### 3. `__new__`/`__init__` 设计存在未决问题 [仍存在]

**文件**：[06a-struct.md](06a-struct.md) §6.4

- "实现了 `__new__` 后，编译器默认的关键字构造是否仍可用**有待确定**"
- "与 trait `Constructible` 的交互仍在**设计阶段**"

**建议**：尽快确定构造器规则，至少在文档中标注"实验性"状态。

---

### 4. `lazy` 导入实现状态矛盾 [仍存在]

**涉及文件**：
- [00-词法基础.md](00-词法基础.md) §1.6：`lazy` 标注为"设计阶段"
- [07-模块与导入.md](07-模块与导入.md) §2.5：将 `lazy import` 作为正常工作功能完整文档化，无任何状态标注

**建议**：统一状态标注。

---

### 5. `go` 并行进程实现状态矛盾 [仍存在]

**涉及文件**：
- [00-词法基础.md](00-词法基础.md) §1.4：`go` 标注为"设计阶段，编译器未实现"
- [10-并发与异步.md](10-并发与异步.md) §三：将 `go` 作为正常工作功能完整文档化，无任何状态标注

**建议**：统一状态标注。

---

### 6. `~` 位非运算符实现状态矛盾 [仍存在]

**涉及文件**：
- [00-词法基础.md](00-词法基础.md) §5.1：`~` 标注为"设计阶段，编译器未实现一元 `~`"
- [12-操作符.md](12-操作符.md) §1.2：将 `~` 作为正常工作运算符文档化，映射到魔法方法 `__invert__`
- [04-表达式.md](04-表达式.md) §六：`~a` 作为位非运算正常列出
- [06d-内置魔法trait和全局函数.md](06d-内置魔法trait和全局函数.md)：`__invert__` → `HasInvert` 完整映射

**建议**：统一状态标注。如果已实现，更新 00-词法基础.md。

---

### 7. `PartialFunc` 实现状态矛盾 [仍存在]

**涉及文件**：
- [99-内置预导入库.md](99-内置预导入库.md) §3.6：`PartialFunc` 标注为"设计阶段"
- [14-生成器.md](14-生成器.md) §四：将 `PartialFunc` 作为正常工作功能完整文档化，提供详细示例

**建议**：统一状态标注。

---

### 8. `Nil` 类型状态标注不一致 [仍存在]

**文件**：[00-词法基础.md](00-词法基础.md) §1.11

`Never` 和 `Unit` 均已移除"设计阶段"标注，但 `Nil` 仍标注为"设计阶段，当前使用 `[]` 直接表示"。如果 `Nil` 确实是设计阶段，应保持；如果已可用，应移除标注。

---

### 9. `03-函数.md` 索引页描述与 `03-函数基础.md` 实际内容不符 [仍存在]

**文件**：[03-函数.md](03-函数.md)

索引页描述 `03-函数基础.md` 包含"装饰器、闭包"，但实际装饰器和闭包在 [03e-复合综合.md](03e-复合综合.md) 中。`03-函数基础.md` 仅包含基础 `def` 声明、参数和返回类型。

**建议**：修正索引页描述，或调整内容归属。

---

### 10. [NEW] `01-类型系统.md` 与 `03-函数基础.md` `@math` 无注解参数默认类型矛盾

**涉及文件**：
- [01-类型系统.md](01-类型系统.md) §3.5 推断规则表：`@math` 宏参数 → 默认 `int` + Number trait 标记
- [03-函数基础.md](03-函数基础.md) §2.2：无注解参数，需要数学表达（**默认 Number**）

两处对 `@math` 装饰器的无注解参数默认类型描述不一致：
- 类型系统文档说默认 `int`（具体类型）+ Number trait
- 函数基础文档说默认 `Number`（trait 类型）

`int` 是具体类型而 `Number` 是 trait，两者语义不同。如果 `@math` 将参数标记为 `Number` trait 对象，那么 `sq(x, y) = x * x + y * y` 中 `x` 和 `y` 的类型是 `Number` 而非 `int`。

**建议**：统一 `@math` 的无注解参数默认类型描述。明确是 `Number` trait 还是 `int + Number`。

---

### 11. [NEW] `02-变量与绑定.md` `const` 示例矛盾

**文件**：[02-变量与绑定.md](02-变量与绑定.md) §四

第 104-105 行将运行时调用作为 `const` 的有效示例：

```lz
const LEN: int = some_list.len()     // 运行时方法调用
const NOW: str = get_time()           // 运行时函数调用
```

但第 186 行明确将运行时表达式标记为错误：

```lz
const X: int = rand()       // 错误：运行时表达式，需用 comptime 包裹
```

`some_list.len()` 和 `get_time()` 同样是运行时调用，与 `const` 的编译期常量语义矛盾。如果这些示例确实有效，则需要说明它们需要 `comptime` 包裹；否则应修正示例。

**建议**：修正第 104-105 行的示例，将其放入 `comptime:` 块中，或删除这些有歧义的示例。

---

### 12. [NEW] `03-函数基础.md` 缺少 `async def` 的引用

**涉及文件**：
- [03-函数基础.md](03-函数基础.md)：未提及 `async def`
- [10-并发与异步.md](10-并发与异步.md) §一：`async def` 有完整定义和示例

`async def` 是 LZ 的重要函数形式，但函数基础文档未提及。读者从函数文档出发无法了解异步函数的存在。

**建议**：在 03-函数基础.md 中新增 `async def` 小节或至少添加交叉引用链接。

---

## P2 — 文档质量问题（30 项）

### 13. [仍存在] `03-函数.md` 与 `03-函数基础.md` 命名混淆

**涉及文件**：
- [03-函数.md](03-函数.md)：纯索引页（hub）
- [03-函数基础.md](03-函数基础.md)：实际函数基础文档

README 中 `03-函数.md` 的标题是"函数总览"但实际上是索引页，容易造成导航混淆。

**建议**：将 03-函数.md 的 README 条目改为"函数总览（索引）"或重命名文件。

---

### 14. [仍存在] `^` 运算符双重语义

**文件**：[12-操作符.md](12-操作符.md)

- §1.6：`^` 是位异或运算符（二元，`a ^ b`）
- §1.14：`^` 是所有权移动后缀（`x^`）

虽然二元 vs 后缀的上下文可区分，但同一符号的双重角色可能造成阅读混淆。

---

### 15. [仍存在] `?` 和 `?.` 优先级相同但语义不同

**文件**：[12-操作符.md](12-操作符.md) §二

`?`（错误传播）和 `?.`（安全导航）在优先级 15 中并列，但 `?` 是后缀运算符而 `?.` 是成员访问前缀。两者优先级相同可能在某些表达式中产生歧义。

---

### 16. [仍存在] README.md 重复条目

**文件**：[README.md](README.md)

第 15-16 行出现重复条目：

```
| 03 | [函数总览](03-函数.md) | 03 系列索引页 → 基础/泛型/检查站/可变参数/复合综合 |
| 03索引 | [函数总览](03-函数.md) | 03 系列 hub（纯索引页） |
```

同一文件被列出两次，标签不同，造成混淆。

**建议**：删除重复条目，保留一条。

---

### 17. [仍存在] `05-控制流.md` 多余分隔符

**文件**：[05-控制流.md](05-控制流.md)

第 414-416 行为连续三个空行，其中包含两个 `---` 分隔符，导致"十二、错误语法边界"与"十三、else 关键字用法汇总"章节之间出现多余分隔。

**建议**：删除多余的空行和分隔符。

---

### 18. [仍存在] `12-操作符.md` 空代码块

**文件**：[12-操作符.md](12-操作符.md)

第 122-123 行存在一个空的 ` ```lz ` 代码块：

```markdown
> 注意：`^` 作为后缀时表示所有权移动...

```lz
```

### 1.7 复合赋值运算符
```

**建议**：删除空代码块。

---

### 19. [仍存在] `99-内置预导入库.md` 末尾多余闭合代码围栏

**文件**：[99-内置预导入库.md](99-内置预导入库.md)

文件最后一行（第 454 行）存在一个孤立的 ` ``` ` 闭合围栏，没有对应的开围栏。

**建议**：删除多余的闭合围栏。

---

### 20. [仍存在] `_` 作为函数参数占位符未在函数文档中说明

**涉及文件**：
- [00-词法基础.md](00-词法基础.md) §4.3：`f(1, _, 3)` 调用时省略参数，编译器自动命名
- [03-函数基础.md](03-函数基础.md)：未提及 `_` 作为参数占位符的用法

**建议**：在 03-函数基础.md 中补充 `_` 参数占位符的说明。

---

### 21. [仍存在] `03e-复合综合.md` 中 `@math` 装饰器示例缺少 `raises` 标注

**文件**：[03e-复合综合.md](03e-复合综合.md) §2.2

```lz
@math
def sq(x, y) = x * x + y * y
```

`@math` 装饰器将无注解参数默认标记为 `Number`，但 `sq` 函数没有返回类型标注。根据 [03-函数基础.md](03-函数基础.md)，无注解返回默认 `-> int`，但 `Number` trait 可能返回 `f64`。返回类型推断规则在此处不清晰。

**建议**：为 `@math` 装饰的函数添加返回类型标注，或明确说明 `@math` 的返回类型推断规则。

---

### 22. [仍存在] `Nil` 与 `[]` 空列表语义关系未展开

**文件**：[00-词法基础.md](00-词法基础.md) §1.11

```lz
| `Nil` | `()` | 空列表字面量（设计阶段，当前使用 `[]` 直接表示） |
```

`Nil` 映射为 Rust `()`，但注释说它是"空列表字面量"。`()` 和 `[]`（空列表）在 Rust 中是完全不同的类型。文档未说明 `Nil` 如何与 `List<T>` 类型交互。

**建议**：明确 `Nil` 的类型语义，或说明它只是 `[]` 的类型别名。

---

### 23. [仍存在] `#!no_std` 属性宏缺少详细说明

**文件**：[08-宏与编译期.md](08-宏与编译期.md) §五

属性宏列表中包含 `#!no_std`，但仅有名称，没有任何说明其用途、影响范围或使用限制。

**建议**：补充 `#!no_std` 的详细说明。

---

### 24. [仍存在] `__cmp__` 魔法方法在 `06d` 速查索引中归属为"比较"但与 `PartialOrd` 关系不明确

**文件**：[06d-内置魔法trait和全局函数.md](06d-内置魔法trait和全局函数.md)

速查索引中 `__cmp__` 标注为 `std::cmp::Ord`，`__lt__`/`__le__`/`__gt__`/`__ge__` 标注为 `std::cmp::PartialOrd`。文档提到"需要全序比较时，实现 `__cmp__`"，但 `__cmp__` 与 `__lt__` 等的关系（是否互斥？是否自动派生？）未在速查索引中说明。

**建议**：在速查索引或 §五比较章节中补充 `__cmp__` 与 `__lt__` 系列的互斥/派生关系。

---

### 25. [仍存在] `14-生成器.md` 代码块内混入 blockquote 格式

**文件**：[14-生成器.md](14-生成器.md) 第 330-346 行

在生成器构建块的代码示例中，第 338 行出现了一个 `>` blockquote 混入代码块内：

```lz
def gen() -> int =
    yield 1
    yield 2
    yield 3
    yield from gen()       // 无限循环 1,2,3,1,2,3...

> **`yield from gen()` 类型匹配**：`yield from gen()` 将另一个生成器的产出摊平到当前生成器...
```

`>` blockquote 打断了代码块，导致 Markdown 渲染异常。

**建议**：将 blockquote 移到代码块外部，或将注释放入代码块内使用 `//` 注释。

---

### 26. [仍存在] `04-表达式.md` 空代码块

**文件**：[04-表达式.md](04-表达式.md) 第 92-93 行

在 `in` 语义表与 §4.3 `is 类型判断` 之间有一个空的 ` ```lz ` 代码块：

```markdown
| `Set<T>` | 成员检查 |

```lz
```

### 4.3 is 类型判断
```

**建议**：删除空代码块。

---

### 27. [仍存在] `06a-struct.md` 乱码字符

**文件**：[06a-struct.md](06a-struct.md) 第 134 行

```
| `mut self` | 可变引用 `&mut Self` | 可修改字 |
```

"可修改字" 应为 "可修改字段"。末尾两个字符为乱码。

**建议**：修正为 "可修改字段"。

---

### 28. [仍存在] `06g-魔法综合.md` 乱码字符

**文件**：[06g-魔法综合.md](06g-魔法综合.md) 第 484 行

```
## 九、布尔值判定（Truthiness
```

标题末尾为乱码，应为 "Truthiness）"。

**建议**：修正为 "## 九、布尔值判定（Truthiness）"。

---

### 29. [仍存在] `99-内置预导入库.md` 乱码字符

**文件**：[99-内置预导入库.md](99-内置预导入库.md) 第 170 行

```
| `.pop()` | 弹出末 |
```

"弹出末" 应为 "弹出末尾"。

**建议**：修正为 "弹出末尾"。

---

### 30. [仍存在] `continue` 带值形式缺少示例

**文件**：[05-控制流.md](05-控制流.md) §6.3

文档说明 `continue` 支持 `continue expr` 带值形式（仅 loop 体中有效），但给出的示例只展示了普通 `continue`（无值），没有提供带值形式的示例：

```lz
def skip_negatives(xs: List<int>) =
    total = 0
    for x in xs:
        if x < 0:
            continue             // 跳过，继续下一轮
        total += x
    total
```

**建议**：补充 `continue expr` 在 `loop` 体中的带值示例。

---

### 31. [仍存在] `*` 运算符双重语义（取指针 vs 乘法）

**涉及文件**：
- [12-操作符.md](12-操作符.md) §1.1：`*` 是乘法运算符（二元，`a * b`）
- [13-指针与引用.md](13-指针与引用.md) §四：`*` 是取指针前缀运算符（`*x` 获取 `Box<T>` 指针）

与 `^` 类似（位异或 vs 所有权移动），`*` 同时承担二元乘法与一元取指针两种角色。虽然上下文（二元 vs 前缀）可区分，但文档未在 12-操作符.md 中统一说明 `*` 的双重语义。

**建议**：在 12-操作符.md 中补充 `*` 作为取指针前缀运算符的说明，与 `^` 的双重语义说明保持一致。

---

### 32. [仍存在] `03d-可变参数.md` 表格中 `f(lst: List<T>, ..)` 语义描述不清

**文件**：[03d-可变参数.md](03d-可变参数.md) §2.3

完整模式表中：

| 签名 | 传递约束 | args | kwargs | 说明 |
|------|------|:---:|:---:|------|
| `f(lst: List<T>, ..)` | lst: 位置或关键字 | — | — | `..` 单独无作用 |

而 `f(a: int, ..)` 的说明是"去掉尾巴关键字能力"。两者都是单个 `..` 在末尾，但语义描述不同——前者说"无作用"，后者说"去掉关键字能力"。这造成理解混淆。

**建议**：统一语义描述，明确单个 `..` 在末尾时的实际效果。

---

### 33. [仍存在] `00-词法基础.md` `Number` 归类有误

**文件**：[00-词法基础.md](00-词法基础.md) §1.1

`Number` 被归类在"声明关键字"中，与 `def`、`struct`、`enum`、`trait` 等并列。但 `Number` 是一个 trait 名称（数值 trait），不是声明关键字。它不能像 `def`/`struct` 那样用于声明新实体。

**建议**：将 `Number` 移至独立的"内置 Trait"分类，或标注其特殊性。

---

### 34. [仍存在] `00-词法基础.md` 缺少装饰器关键字

**涉及文件**：
- [00-词法基础.md](00-词法基础.md) §1：关键字表中无 `@overload`、`@tail_call`、`@deprecated`、`@math`
- [03e-复合综合.md](03e-复合综合.md) §2：`@overload`、`@tail_call`、`@math` 有完整文档

`@overload`（多分派）、`@tail_call`（尾调用优化）、`@deprecated`（弃用标记）、`@math`（数值 trait 标记）在 `03e-复合综合.md` 中有详细文档，但未出现在 `00-词法基础.md` 的关键字表中。

**建议**：在关键字表中新增"装饰器关键字"分类，列出 `@overload`、`@tail_call`、`@deprecated`、`@math`。

---

### 35. [仍存在] `00-词法基础.md` 缺少属性宏关键字

**涉及文件**：
- [00-词法基础.md](00-词法基础.md) §1：关键字表中无 `#!bin macro`、`#!export`、`#!no_std`
- [08-宏与编译期.md](08-宏与编译期.md) §一、§二、§五：这些属性宏有完整文档

`#!bin macro`（宏模块声明）、`#!export(Rust)`/`#!export(Python)`（导出属性）、`#!no_std`（禁用标准库）是 LZ 的重要组成部分，但未出现在关键字表中。

**建议**：在关键字表中新增"属性宏"分类，列出 `#!bin macro`、`#!export`、`#!no_std`。

---

### 36. [仍存在] `01-类型系统.md` 缺少指针类型引用

**涉及文件**：
- [01-类型系统.md](01-类型系统.md)：未提及 `Box<T>`、`Rc<T>`、`Arc<T>`
- [13-指针与引用.md](13-指针与引用.md)：详细定义了这三种指针类型及其 `dyn` 隐式规则

`Box<T>`、`Rc<T>`、`Arc<T>` 是 LZ 的核心指针类型，但在类型系统总览文档中完全缺失。读者从类型系统文档出发无法了解这些类型的存在。

**建议**：在 01-类型系统.md 中新增"指针类型"小节，至少包含 `Box<T>`、`Rc<T>`、`Arc<T>` 的简要说明及交叉引用链接。

---

### 37. [仍存在] `01-类型系统.md` 缺少 `Ordering`、`Formatter` 类型

**涉及文件**：
- [01-类型系统.md](01-类型系统.md)：未提及 `Ordering`、`Formatter`
- [06d-内置魔法trait和全局函数.md](06d-内置魔法trait和全局函数.md)：`__cmp__` 返回 `Ordering`，`__fmt__`/`__str__` 接受 `&mut Formatter`

`Ordering`（全序比较结果）和 `Formatter`（格式化输出器）在魔法方法中被频繁引用，但类型系统文档未定义它们。

**建议**：在 01-类型系统.md 中补充 `Ordering` 和 `Formatter` 的简要说明。

---

### 38. [仍存在] `01-类型系统.md` 缺少 `Future<T>` 和 `Futures<T1, T2, ...>` 类型

**涉及文件**：
- [01-类型系统.md](01-类型系统.md)：未提及 `Future<T>` 或 `Futures<T1, T2, ...>`
- [10-并发与异步.md](10-并发与异步.md) §二：`spawn`/`go` 返回 `Future<T>`，`Futures<T1, T2, ...>` 支持 1~12 个类型参数

`Future<T>` 和 `Futures<T1, T2, ...>` 是并发系统的核心类型，但在类型系统文档中缺失。

**建议**：在 01-类型系统.md 中补充 `Future<T>` 和 `Futures<T1, T2, ...>` 的简要说明。

---

### 39. [仍存在] `01-类型系统.md` 缺少 `Nil` 类型

**涉及文件**：
- [01-类型系统.md](01-类型系统.md)：未提及 `Nil`
- [00-词法基础.md](00-词法基础.md) §1.11：`Nil` 映射为 `()`，标注为"设计阶段"

`Nil` 在 `00-词法基础.md` 中被列为特殊字面量，但类型系统文档未提及。`Never` 和 `Unit` 已在 01-类型系统.md 中有说明，`Nil` 应该也有一席之地。

**建议**：在 01-类型系统.md 中补充 `Nil` 类型说明，或明确标注其"设计阶段"状态。

---

### 40. [仍存在] `03-函数基础.md` 缺少 `comptime def` 编译期函数引用

**涉及文件**：
- [03-函数基础.md](03-函数基础.md)：未提及 `comptime def`
- [08b-comptime编译期.md](08b-comptime编译期.md) §4.5：`comptime def` 有完整定义和示例

`comptime def` 是 LZ 的重要函数形式（编译期函数），但函数基础文档未提及。读者从函数文档出发无法了解这一功能。

**建议**：在 03-函数基础.md 中新增 `comptime def` 小节或至少添加交叉引用链接。

---

### 41. [NEW] `06d-内置魔法trait和全局函数.md` 缺少 `__new__` 和 `__init__` 魔法方法

**涉及文件**：
- [06d-内置魔法trait和全局函数.md](06d-内置魔法trait和全局函数.md)：魔法方法速查表中无 `__new__` 和 `__init__`
- [06a-struct.md](06a-struct.md) §六：`__new__` 和 `__init__` 有完整文档和示例

`__new__`（分配器）和 `__init__`（初始化器）是 struct 构造的核心魔法方法，在 `06a-struct.md` 中有详细说明，但未出现在 `06d` 的魔法方法速查索引中。`06d` 自称"全部内置魔法方法 → Rust trait → 全局函数映射表"，遗漏这两个方法使其不完整。

**建议**：在 06d 速查索引中补充 `__new__` 和 `__init__`，说明其 trait 映射和分类。

---

### 42. [NEW] `06f-magic用法.md` 缺少 `magic __new__` 和 `magic __init__` 的说明（交叉引用断裂）

**涉及文件**：
- [06a-struct.md](06a-struct.md) §6.2：`magic __new__` 是内联语法糖，"详见 06f-magic用法.md"
- [06f-magic用法.md](06f-magic用法.md)：全文未提及 `__new__` 或 `__init__`

`06a-struct.md` 的交叉引用指向 `06f-magic用法.md` 以获取 `magic __new__` 的详细说明，但目标文档完全没有相关内容。这是一个交叉引用断裂。

**建议**：在 06f-magic用法.md 中补充 `magic __new__` 和 `magic __init__` 的内联语法说明，或修正 06a-struct.md 中的交叉引用。

---

## 已修复问题（0 项）

> 本轮 v9.0 报告的 37 个问题均未修复。

---

## 总结

本轮重新全量检查 29 份文档，发现：**v9.0 的 37 个问题全部未修复**，同时新增 5 个问题（3 P1 + 2 P2）。总计 **42 个问题**（0 P0, 12 P1, 30 P2）。

新增问题暴露了三个新的缺陷领域：

1. **跨文档类型矛盾**（P1-10）— `@math` 无注解参数在 `01-类型系统.md` 中说是 `int + Number`，在 `03-函数基础.md` 中说是 `Number`
2. **示例逻辑矛盾**（P1-11）— `const` 的正例和反例使用同类运行时调用，自相矛盾
3. **函数文档覆盖不全**（P1-12）— `async def` 在函数基础文档中缺失
4. **魔法方法索引不完整**（P2-41）— `06d` 速查索引遗漏 `__new__`/`__init__`
5. **交叉引用断裂**（P2-42）— `06a` 指向 `06f` 但 06f 无相关内容

**优先建议**：
1. 修复乱码字符（P2-27, P2-28, P2-29）— 最简单，直接改
2. 修复格式问题：空代码块、多余分隔符、重复条目、blockquote 混入（P2-16~19, P2-25, P2-26）— 纯 Markdown 修正
3. 修复跨文档矛盾（P1-10, P1-11）— `@math` 默认类型和 `const` 示例
4. 补充 `00-词法基础.md` 关键字表（P2-34, P2-35）— 装饰器和属性宏
5. 完善 `01-类型系统.md` 类型覆盖（P2-36~P2-39）— 指针、辅助、并发、Nil 类型
6. 统一实现状态标注（P1-4~P1-8）— 一次性扫清 `lazy`/`go`/`~`/`PartialFunc`/`Nil` 状态矛盾
7. 补充函数文档交叉引用（P1-12, P2-40）— `async def`、`comptime def`
8. 完善魔法方法索引（P2-41, P2-42）— 补充 `__new__`/`__init__`，修复交叉引用
9. 解决全局函数签名与魔法方法签名不匹配（P1-1）— 长期改进项