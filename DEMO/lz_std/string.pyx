# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["CharIter", "join", "format", "main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__"]

cdef class CharIter:
    cdef public str s
    cdef public Py_ssize_t pos
    def __init__(self, str s, Py_ssize_t pos):        self.s = s
        self.pos = pos

# impl str
# str.len → 注入到 str
# str.is_empty → 注入到 str
# str.contains → 注入到 str
# str.starts_with → 注入到 str
# str.ends_with → 注入到 str
# str.find → 注入到 str
# str.rfind → 注入到 str
# str.char_at → 注入到 str
# str.slice → 注入到 str
# str.slice_from → 注入到 str
# str.slice_to → 注入到 str
# str.replace → 注入到 str
# str.trim → 注入到 str
# str.trim_start → 注入到 str
# str.trim_end → 注入到 str
# str.is_whitespace → 注入到 str
# str.to_lower → 注入到 str
# str.to_upper → 注入到 str
# str.repeat → 注入到 str
# str.split → 注入到 str
# str.lines → 注入到 str
# str.chars → 注入到 str
# str.__len__ → 注入到 str
# str.__add__ → 注入到 str
# str.__eq__ → 注入到 str
# str.__str__ → 注入到 str

# impl Iterator for CharIter
# type Item = str
# Iterator.__next__ → 注入到 CharIter
# Iterator.size_hint → 注入到 CharIter

cdef str join(list parts, str separator):
    ((begin
    # Return { value: Some(Expr { kind: Lit(Str("")), ty: Str, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }) }

) if parts.is_empty() else None)
    result = parts.first().unwrap_or("")
    i = 1
    while i < parts.len():    
        result = result + separator + parts.get(i).unwrap_or("")
        i = i + 1
    return result

cdef str format(str tpl, list args):
    result = ""
    pos = 0
    arg_idx = 0
    while pos < tpl.len():    
        # match tpl.slice_from(pos).find("{}")
        idx = __scrutinee__[0]
        if isinstance(__scrutinee__, Option) && True  # bind idx:        
            result = result + tpl.slice(pos, pos + idx)
            ((begin
    # Assign { target: Expr { kind: Var("result"), ty: Str, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, value: Expr { kind: BinOp { op: Add, lhs: Expr { kind: Var("result"), ty: Str, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, rhs: Expr { kind: MethodCall { receiver: Expr { kind: MethodCall { receiver: Expr { kind: Var("args"), ty: Named { path: "List", args: [Str] }, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, method: "get", args: [Expr { kind: Var("arg_idx"), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }] }, ty: Option(Str), span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, method: "unwrap_or", args: [Expr { kind: Lit(Str("")), ty: Str, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }] }, ty: Any, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } } }, ty: Str, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } } }
    # Assign { target: Expr { kind: Var("arg_idx"), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, value: Expr { kind: BinOp { op: Add, lhs: Expr { kind: Var("arg_idx"), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, rhs: Expr { kind: Lit(Int(1)), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } } }, ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } } }

) if arg_idx < args.len() else None)
            pos = pos + idx + 2
        None = __scrutinee__
        elif True  # bind None:        
            result = result + tpl.slice_from(pos)
            break
    return result

cdef void main():
    assert_eq!("hello".len(), 5)
    assert!("".is_empty())
    assert!(not "x".is_empty())
    assert!("hello world".contains("world"))
    assert!(not "hello".contains("xyz"))
    assert!("hello.txt".starts_with("hello"))
    assert!("hello.txt".ends_with(".txt"))
    assert!(not "hello.txt".ends_with(".md"))
    assert_eq!("hello hello".find("hello"), Some(0))
    assert_eq!("hello hello".rfind("hello"), Some(6))
    assert_eq!("abc".find("xyz"), None)
    assert_eq!("hello".slice(1, 4), "ell")
    assert_eq!("hello".slice_from(2), "llo")
    assert_eq!("hello".slice_to(3), "hel")
    assert_eq!("hello world".replace("world", "lz"), "hello lz")
    assert_eq!("aaa".replace("a", "b"), "bbb")
    assert_eq!("  hello  ".trim(), "hello")
    assert_eq!("  hello".trim_start(), "hello")
    assert_eq!("hello  ".trim_end(), "hello")
    assert_eq!("Hello".to_upper(), "HELLO")
    assert_eq!("Hello".to_lower(), "hello")
    assert_eq!("ab".repeat(3), "ababab")
    parts = "a,b,c".split(",")
    assert_eq!(parts, ["a", "b", "c"])
    result = ""
    for ch in "abc".chars():    
        result = result + ch
    assert_eq!(result, "abc")
    assert_eq!(join(["a", "b", "c"], "-"), "a-b-c")
    assert_eq!(format("Hello, {}!", ["World"]), "Hello, World!")
    print("string.lz — all tests passed!")

str __name__ = "main"
str __file__ = "DEMO/lz_std/string.lz"
str __package__ = "lz_std"
str __path__ = "DEMO/lz_std"
str __doc__ = ""
bint __is_macro__ = False
