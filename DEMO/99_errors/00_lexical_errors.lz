// ============================
// LZ 错误边界合集 — 词法基础 (00-词法基础.md)
// 这些代码在编译器中应产生错误/警告。
// 以注释形式展示，不实际运行。
// ============================

// --- 1. 冒号后不换行 ---
// while True: print("...")     // ❌ 单行 while 块体不支持
// for x in xs: print(x)        // ❌ 单行 for 块体不支持
// guard let Some(v) = opt else: return  // ❌ guard let else 必须换行缩进

// --- 2. 混用缩进风格 ---
// def f() =
//   2空格缩进                  // ❌ 必须 4 空格

// --- 3. 路径分隔符 :: ---
// import std::io               // ❌ :: 产生词法错误，应用 .
// Option::Some(42)             // ❌ 应用 Option.Some(42)

// --- 4. 错误类型名 ---
// let x: void = ...            // ❌ void 不是类型
// let s: string = "hi"         // ❌ 应为 str
// let n: null = ...            // ❌ 应为 None

// --- 5. 分号 ---
// let x = 1;                   // ❌ 不支持分号

// --- 6. # 作为注释 ---
// # 这不是注释                  // ❌ # 是属性宏标记
