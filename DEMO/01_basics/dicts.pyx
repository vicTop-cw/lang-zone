# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__"]

cdef void main():
    person = Dict(_k0 = "name", _v0 = "LZ", _k1 = "lang", _v1 = "LZ")
    assert_eq!(person["name"], "LZ")
    assert_eq!(person["lang"], "LZ")
    scores = Dict(_k0 = "a", _v0 = 1, _k1 = "b", _v1 = 2)
    scores["a"] = 10
    assert_eq!(scores["a"], 10)
    assert_eq!(scores["b"], 2)
    scores["c"] = 3
    assert_eq!(scores["c"], 3)
    assert_eq!(scores["a"], 10)
    scores.remove("b")
    assert_eq!(len(scores), 2)
    assert!("b" not in scores)
    assert!("a" in scores)
    assert!("c" in scores)
    assert!("x" not in scores)
    assert!(scores.contains("a"))
    assert!(not scores.contains("zz"))
    assert_eq!(len(scores), 2)
    assert_eq!(len(person), 2)
    assert_eq!(person["name"], "LZ")
    assert_eq!(scores["a"], 10)
    assert_eq!(scores["c"], 3)
    config = Dict(_k0 = "size", _v0 = 14)
    config["count"] = 2
    config["size"] = 16
    assert_eq!(config["size"], 16)
    assert_eq!(config["count"], 2)
    config.remove("size")
    assert_eq!(len(config), 1)
    assert!("size" not in config)
    assert!("count" in config)
    nested = Dict(_k0 = "outer", _v0 = Dict(_k0 = "inner", _v0 = 42))
    assert_eq!(nested["outer"]["inner"], 42)
    empty = Dict()
    assert_eq!(len(empty), 0)
    print("R9 dicts OK")

str __name__ = "main"
str __file__ = "DEMO/01_basics/dicts.lz"
str __package__ = "01_basics"
str __path__ = "DEMO/01_basics"
str __doc__ = ""
bint __is_macro__ = False
