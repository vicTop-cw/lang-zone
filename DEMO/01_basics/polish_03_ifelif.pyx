# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__", "grade", "classify2", "pick"]

cdef void main():
    None
    assert_eq!(grade(95), "A")
    assert_eq!(grade(85), "B")
    assert_eq!(grade(75), "C")
    assert_eq!(grade(65), "D")
    assert_eq!(grade(50), "F")
    assert_eq!(grade(90), "A")
    assert_eq!(grade(89), "B")
    assert_eq!(grade(60), "D")
    assert_eq!(grade(59), "F")
    a = 10
    bigger = ("big" if a > 5 else "small")
    assert_eq!(bigger, "big")
    smaller = ("big" if a < 5 else "small")
    assert_eq!(smaller, "small")
    x = 7
    desc = ((("pos-big" if x > 5 else "pos-small")) if x > 0 else "non-pos")
    assert_eq!(desc, "pos-big")
    y = 3
    desc2 = ((("pos-big" if y > 5 else "pos-small")) if y > 0 else "non-pos")
    assert_eq!(desc2, "pos-small")
    z = -1
    desc3 = ((("pos-big" if z > 5 else "pos-small")) if z > 0 else "non-pos")
    assert_eq!(desc3, "non-pos")
    level = 0
    n = 25
    ((((begin
    # Let { name: "level", ty: Int, value: Expr { kind: Lit(Int(3)), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, is_mut: true, is_ref: false }

) if n > 20 else (begin
    # Let { name: "level", ty: Int, value: Expr { kind: Lit(Int(2)), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, is_mut: true, is_ref: false }

)) if n > 10 else (begin
    # Let { name: "level", ty: Int, value: Expr { kind: Lit(Int(1)), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, is_mut: true, is_ref: false }

)) if n > 0 else (begin
    # Let { name: "level", ty: Int, value: Expr { kind: UnOp { op: Neg, operand: Expr { kind: Lit(Int(1)), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } } }, ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, is_mut: true, is_ref: false }

))
    assert_eq!(level, 3)
    result = ""
    ((begin
    # Let { name: "temp", ty: Str, value: Expr { kind: Lit(Str("from-block")), ty: Str, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, is_mut: false, is_ref: false }
    # Let { name: "result", ty: Any, value: Expr { kind: Var("temp"), ty: Any, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, is_mut: true, is_ref: false }

) if True else None)
    assert_eq!(result, "from-block")
    None
    assert_eq!(classify2(0), "zero")
    assert_eq!(classify2(3), "three")
    assert_eq!(classify2(10), "many")
    None
    combined = pick(("yes" if 1 > 0 else "no"), "-suffix")
    assert_eq!(combined, "yes-suffix")
    p = 15
    q = 20
    r = 25
    ((begin
    # ExprStmt { expr: Expr { kind: Call { callee: Expr { kind: Var("assert!"), ty: Any, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, args: [Expr { kind: Lit(Bool(true)), ty: Bool, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }], type_args: [] }, ty: Unit, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } } }

) if (p < q) and (q < r) else (begin
    # ExprStmt { expr: Expr { kind: Call { callee: Expr { kind: Var("assert!"), ty: Any, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, args: [Expr { kind: Lit(Bool(false)), ty: Bool, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }], type_args: [] }, ty: Unit, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } } }

))
    print("P3 ifelif OK")

str __name__ = "main"
str __file__ = "DEMO/01_basics/polish_03_ifelif.lz"
str __package__ = "01_basics"
str __path__ = "DEMO/01_basics"
str __doc__ = ""
bint __is_macro__ = False
cdef str grade(Py_ssize_t score):
    ("A" if score >= 90 else ("B" if score >= 80 else ("C" if score >= 70 else ("D" if score >= 60 else "F"))))

cdef str classify2(Py_ssize_t v):
    ("zero" if v == 0 else ("one" if v == 1 else ("two" if v == 2 else ("three" if v == 3 else "many"))))

cdef str pick(str a, str b):
    a + b

