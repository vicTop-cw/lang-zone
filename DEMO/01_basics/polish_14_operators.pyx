# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["Money", "Vec2", "main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__", "total_money"]

cdef class Money:
    cdef public Py_ssize_t amount
    def __init__(self, Py_ssize_t amount):        self.amount = amount

cdef class Vec2:
    cdef public Py_ssize_t x
    cdef public Py_ssize_t y
    def __init__(self, Py_ssize_t x, Py_ssize_t y):        self.x = x
        self.y = y

# impl Money
# Money.__add__ → 注入到 Money
# Money.__eq__ → 注入到 Money
# Money.__lt__ → 注入到 Money

# impl Vec2
# Vec2.__add__ → 注入到 Vec2
# Vec2.__sub__ → 注入到 Vec2
# Vec2.__mul__ → 注入到 Vec2
# Vec2.__eq__ → 注入到 Vec2

cdef void main():
    m1 = Money(_KwArg(name = "amount", value = 10))
    m2 = Money(_KwArg(name = "amount", value = 5))
    m3 = m1.__add__(m2)
    assert_eq!(m3.amount, 15)
    assert!(m1.__eq__(~Money(_KwArg(name = "amount", value = 10))))
    assert!(m1 != m2)
    assert!((m1.__add__(m2)).__eq__(~Money(_KwArg(name = "amount", value = 15))))
    assert!(m2.__lt__(m1))
    assert!(not (m1.__lt__(m2)))
    assert!(m1.__lt__(Money(_KwArg(name = "amount", value = 100))))
    assert_eq!(m1.amount < m2.amount, False)
    v1 = Vec2(_KwArg(name = "x", value = 3), _KwArg(name = "y", value = 4))
    v2 = Vec2(_KwArg(name = "x", value = 1), _KwArg(name = "y", value = 2))
    sum = v1.__add__(v2)
    assert_eq!(sum.x, 4)
    assert_eq!(sum.y, 6)
    diff = v1.__sub__(v2)
    assert_eq!(diff.x, 2)
    assert_eq!(diff.y, 2)
    scaled = v1.__mul__(2)
    assert_eq!(scaled.x, 6)
    assert_eq!(scaled.y, 8)
    combo = (v1.__add__(v2)).__sub__(Vec2(_KwArg(name = "x", value = 1), _KwArg(name = "y", value = 1)))
    assert_eq!(combo.x, 3)
    assert_eq!(combo.y, 5)
    assert!((v1.__add__(v2)).__eq__(~Vec2(_KwArg(name = "x", value = 4), _KwArg(name = "y", value = 6))))
    assert!((v1.__mul__(0)).__eq__(~Vec2(_KwArg(name = "x", value = 0), _KwArg(name = "y", value = 0))))
    None
    combined = total_money(Money(_KwArg(name = "amount", value = 1)), Money(_KwArg(name = "amount", value = 2)))
    assert_eq!(combined.amount, 3)
    print("P14 operators OK")

str __name__ = "main"
str __file__ = "DEMO/01_basics/polish_14_operators.lz"
str __package__ = "01_basics"
str __path__ = "DEMO/01_basics"
str __doc__ = ""
bint __is_macro__ = False
cdef Money total_money(Money a, Money b):
    a.__add__(b)

