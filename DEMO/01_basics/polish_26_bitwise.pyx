# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__"]

cdef void main():
    assert_eq!((5 & 3), 1)
    assert_eq!((5 | 3), 7)
    assert_eq!((5 ^ 3), 6)
    assert_eq!((1 << 4), 16)
    assert_eq!((16 >> 2), 4)
    assert_eq!((1 << 10), 1024)
    assert_eq!(((240 | 15)), 255)
    assert_eq!(((255 & 15)), 15)
    assert_eq!(((1 << 8) | (1 << 2)), 260)
    a = 10
    a = a + 5
    assert_eq!(a, 15)
    a = a - 3
    assert_eq!(a, 12)
    a = a * 2
    assert_eq!(a, 24)
    a = a / 4
    assert_eq!(a, 6)
    a = a % 4
    assert_eq!(a, 2)
    b = 1
    b = b << 4
    assert_eq!(b, 16)
    b = b >> 2
    assert_eq!(b, 4)
    assert!((True and True) and (False or True))
    assert!(not (True and False))
    assert!((5 > 3) and (2 < 4))
    assert_eq!(7 / 2, 3)
    assert_eq!(7 / 2, 3.5)
    assert!(-7 % 3 == -1 or 7 % 3 == 1)
    print("P26 bitwise OK")

str __name__ = "main"
str __file__ = "DEMO/01_basics/polish_26_bitwise.lz"
str __package__ = "01_basics"
str __path__ = "DEMO/01_basics"
str __doc__ = ""
bint __is_macro__ = False
