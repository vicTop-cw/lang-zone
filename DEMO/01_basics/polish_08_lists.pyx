# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__", "append_all"]

cdef void main():
    matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
    assert_eq!(matrix[0][0], 1)
    assert_eq!(matrix[2][2], 9)
    assert_eq!(matrix[1][1], 5)
    assert_eq!(len(matrix), 3)
    assert_eq!(len(matrix[0]), 3)
    transposed = []
    for j in Range(start = 0, end = 3):    
        row = []
        for i in Range(start = 0, end = 3):        
            row.push(matrix[i][j])
        transposed.push(row)
    assert_eq!(transposed[0], [1, 4, 7])
    assert_eq!(transposed[1], [2, 5, 8])
    assert_eq!(transposed[2], [3, 6, 9])
    nums = [0, 1, 2, 3, 4, 5]
    assert_eq!(nums[Range(start = 1, end = 4)], [1, 2, 3])
    assert_eq!(nums[Range(start = 0, end = 6)], [0, 1, 2, 3, 4, 5])
    chain = [3, 1]
    chain.push(2)
    chain.sort()
    assert_eq!(chain, [1, 2, 3])
    chain.reverse()
    assert_eq!(chain, [3, 2, 1])
    data = [4, 2, 7, 1, 9]
    data_len = len(data)
    total = 0
    for v in data:    
        total = total + v
    assert_eq!(total, 23)
    assert_eq!(data_len, 5)
    flat = []
    for row in matrix:    
        for cell in row:        
            (flat.push(cell) if cell % 2 == 0 else None)
    assert_eq!(flat, [2, 4, 6, 8])
    None
    combined = append_all([1, 2], [3, 4])
    assert_eq!(combined, [1, 2, 3, 4])
    editable = [1, 2, 3]
    editable[0] = 100
    editable[2] = 300
    assert_eq!(editable, [100, 2, 300])
    words = ["a", "b", "c"]
    assert_eq!(len(words), 3)
    assert_eq!(words[0], "a")
    empty = []
    assert_eq!(len(empty), 0)
    empty2 = []
    assert_eq!(len(empty2), 0)
    cube = [[[1], [2]], [[3], [4]]]
    assert_eq!(cube[0][0][0], 1)
    assert_eq!(cube[1][1][0], 4)
    print("P8 lists OK")

str __name__ = "main"
str __file__ = "DEMO/01_basics/polish_08_lists.lz"
str __package__ = "01_basics"
str __path__ = "DEMO/01_basics"
str __doc__ = ""
bint __is_macro__ = False
cdef list append_all(list xs, list ys):
    out = xs
    for y in ys:    
        out.push(y)
    return out

