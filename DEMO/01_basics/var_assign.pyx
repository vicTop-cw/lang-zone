# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__"]

cdef void main():
    a = 10
    assert_eq!(a, 10)
    b = 20
    assert_eq!(b, 20)
    c = "str"
    assert_eq!(c, "str")
    d = True
    assert_eq!(d, True)
    e = 3.5
    assert_eq!(e, 3.5)
    x = 1
    x = 2
    assert_eq!(x, 2)
    x = x + 10
    assert_eq!(x, 12)
    s = "hello"
    s = "world"
    assert_eq!(s, "world")
    n = 10
    n = n + 5
    assert_eq!(n, 15)
    n = n - 3
    assert_eq!(n, 12)
    n = n * 2
    assert_eq!(n, 24)
    n = n / 4
    assert_eq!(n, 6)
    n = n % 4
    assert_eq!(n, 2)
    f = 1.5
    f = f + 2
    assert_eq!(f, 3.5)
    st = "foo"
    st = st + "bar"
    assert_eq!(st, "foobar")
    first = "LZ"
    last = "lang"
    full = first + "-" + last
    assert_eq!(full, "LZ-lang")
    assert_eq!(len(full), 7)
    assert_eq!("abc"[0], 97)
    assert_eq!("abc"[2], 99)
    assert_eq!("abc" + "def", "abcdef")
    p = 3
    q = 4
    r = p * p + q * q
    assert_eq!(r, 25)
    acc = 0
    acc = acc + p
    acc = acc + q
    assert_eq!(acc, 7)
    annotated = 2.5
    assert_eq!(annotated, 2.5)
    inferred = 100
    assert_eq!(inferred, 100)
    shadow = 1
    ((begin
    # Let { name: "shadow", ty: Int, value: Expr { kind: Lit(Int(2)), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, is_mut: false, is_ref: false }
    # ExprStmt { expr: Expr { kind: Call { callee: Expr { kind: Var("assert_eq!"), ty: Any, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, args: [Expr { kind: Var("shadow"), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }, Expr { kind: Lit(Int(2)), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }], type_args: [] }, ty: Unit, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } } }

) if True else None)
    assert_eq!(shadow, 1)
    msg = "Hello World"
    assert!(msg.contains("World"))
    assert!(not msg.contains("xyz"))
    low = "MiXeD".to_lower()
    assert_eq!(low, "mixed")
    up = "MiXeD".to_upper()
    assert_eq!(up, "MIXED")
    rep = "a-b-c".replace("-", "+")
    assert_eq!(rep, "a+b+c")
    print("R2 variables OK")

str __name__ = "main"
str __file__ = "DEMO/01_basics/var_assign.lz"
str __package__ = "01_basics"
str __path__ = "DEMO/01_basics"
str __doc__ = ""
bint __is_macro__ = False
