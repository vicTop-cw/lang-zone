# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__"]

cdef void main():
    s = "hello"
    assert_eq!(s, "hello")
    assert_eq!(len(s), 5)
    assert_eq!("foo" + "bar", "foobar")
    a = "LZ"
    assert_eq!(a + "!", "LZ!")
    assert_eq!("abc"[0], 97)
    assert_eq!("abc"[2], 99)
    assert_eq!("hello"[1], 101)
    assert!("hello world".contains("world"))
    assert!(not "hello world".contains("xyz"))
    assert!("hello".starts_with("he"))
    assert!("hello".ends_with("lo"))
    assert_eq!("MiXeD".to_lower(), "mixed")
    assert_eq!("MiXeD".to_upper(), "MIXED")
    assert_eq!("a-b-c".replace("-", "+"), "a+b+c")
    part_count = 0
    for p in "1,2,3".split(","):    
        part_count = part_count + 1
    assert_eq!(part_count, 3)
    name = "LZ"
    age = 30
    msg = f"name={name}, age={age}"
    assert_eq!(msg, "name=\"LZ\", age=30")
    val = 42
    combined = f"val is {val}"
    assert_eq!(combined, "val is 42")
    assert_eq!(str(123), "123")
    assert_eq!(str(3.5), "3.5")
    empty = ""
    assert_eq!(len(empty), 0)
    assert_eq!(empty, "")
    raw = "  Hello World  "
    cleaned = raw.trim()
    assert_eq!(cleaned, "Hello World")
    assert_eq!(cleaned.to_lower(), "hello world")
    assert!("Hello World".contains("World"))
    assert_eq!(len(cleaned), 11)
    print("R17 strings OK")

str __name__ = "main"
str __file__ = "DEMO/01_basics/strings.lz"
str __package__ = "01_basics"
str __path__ = "DEMO/01_basics"
str __doc__ = ""
bint __is_macro__ = False
