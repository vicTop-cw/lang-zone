# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["Dog", "Circle3", "Square3", "main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__", "show_name", "total_area", "area_plus"]

cdef class Dog:
    cdef public str n
    def __init__(self, str n):        self.n = n
    cdef str name(self):    
        self.n

cdef class Circle3:
    cdef public Py_ssize_t radius
    def __init__(self, Py_ssize_t radius):        self.radius = radius
    cdef Py_ssize_t area(self):    
        3 * self.radius * self.radius

cdef class Square3:
    cdef public Py_ssize_t side
    def __init__(self, Py_ssize_t side):        self.side = side
    cdef Py_ssize_t area(self):    
        self.side * self.side

cdef void main():
    dog = Dog(_KwArg(name = "n", value = "Rex"))
    c = Circle3(_KwArg(name = "radius", value = 3))
    sq = Square3(_KwArg(name = "side", value = 4))
    None
    assert_eq!(show_name(dog), "Rex")
    None
    assert_eq!(total_area(c), 27)
    assert_eq!(total_area(sq), 16)
    None
    assert_eq!(area_plus(c), 28)
    assert_eq!(area_plus(sq), 17)
    assert_eq!(dog.name(), "Rex")
    assert_eq!(c.area(), 27)
    assert_eq!(sq.area(), 16)
    print("P16 duck OK")

str __name__ = "main"
str __file__ = "DEMO/01_basics/polish_16_duck.lz"
str __package__ = "01_basics"
str __path__ = "DEMO/01_basics"
str __doc__ = ""
bint __is_macro__ = False
class HasName:
    """Duck type constraint: HasName"""
    # def name(...) -> str

class HasArea:
    """Duck type constraint: HasArea"""
    # def area(...) -> Py_ssize_t

cdef str show_name(object x):  # generic<T>
    x.name()

cdef Py_ssize_t total_area(object x):  # generic<T>
    x.area()

cdef Py_ssize_t area_plus(object x):  # generic<T>
    x.area() + 1

