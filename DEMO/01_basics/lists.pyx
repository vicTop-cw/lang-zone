# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__"]

cdef void main():
    xs = [1, 2, 3]
    assert_eq!(xs, [1, 2, 3])
    assert!(xs != [1, 2])
    assert_eq!(len(xs), 3)
    assert_eq!(xs[0], 1)
    assert_eq!(xs[1], 2)
    assert_eq!(xs[2], 3)
    ys = [1, 2]
    ys.push(3)
    assert_eq!(ys, [1, 2, 3])
    ys.push(4)
    assert_eq!(ys, [1, 2, 3, 4])
    last = ys.pop()
    assert_eq!(last, Some(4))
    assert_eq!(ys, [1, 2, 3])
    zs = [1, 3]
    zs.insert(1, 2)
    assert_eq!(zs, [1, 2, 3])
    zs.remove(1)
    assert_eq!(zs, [1, 3])
    ws = [10, 20, 30]
    ws[1] = 99
    assert_eq!(ws, [10, 99, 30])
    big = [0, 1, 2, 3, 4, 5]
    assert_eq!(big[Range(start = 1, end = 3)], [1, 2])
    assert_eq!(big[Range(start = 2, end = 5)], [2, 3, 4])
    assert_eq!(big[Range(start = 0, end = 2)], [0, 1])
    sum = 0
    for v in [1, 2, 3, 4]:    
        sum = sum + v
    assert_eq!(sum, 10)
    grid = [[1, 2], [3, 4]]
    assert_eq!(grid[0][1], 2)
    assert_eq!(grid[1][0], 3)
    unsorted = [3, 1, 2]
    unsorted.sort()
    assert_eq!(unsorted, [1, 2, 3])
    rev = [1, 2, 3]
    rev.reverse()
    assert_eq!(rev, [3, 2, 1])
    assert!([1, 2, 3].contains(2))
    assert!(not [1, 2, 3].contains(9))
    empty = []
    assert_eq!(len(empty), 0)
    assert_eq!(empty, [])
    merged = [1, 2]
    for v in [3, 4]:    
        merged.push(v)
    assert_eq!(merged, [1, 2, 3, 4])
    combo = [5, 1]
    combo.push(3)
    combo.sort()
    combo.reverse()
    assert_eq!(combo, [5, 3, 1])
    assert_eq!(len(combo), 3)
    assert_eq!(combo[1], 3)
    print("R8 lists OK")

str __name__ = "main"
str __file__ = "DEMO/01_basics/lists.lz"
str __package__ = "01_basics"
str __path__ = "DEMO/01_basics"
str __doc__ = ""
bint __is_macro__ = False
