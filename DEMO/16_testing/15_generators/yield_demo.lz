// ============================
// LZ Generators — yield
// ============================

iterator counter(n: int) -> int =
    for i in 0..n:
        yield i

def main() =
    for x in counter(5):
        print(x)
