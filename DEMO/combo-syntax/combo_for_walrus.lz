// combo-syntax（绿色回归测试）：for 循环 + 海象运算符 :=
// for 迭代器内用 := 绑定每次取出的元素并派生子值
def main() =
    for i in [1, 2, 3, 4]:
        let n = (v := i * 2)   // := 绑定派生值
        print(n)               // 预期：2 4 6 8
