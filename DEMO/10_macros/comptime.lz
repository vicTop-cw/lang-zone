// ==== Comptime Compile-Time Evaluation ====
const BUFFER = 4096

comptime:
    let doubled = BUFFER * 2
    print(f"compile-time: BUFFER*2 = {doubled}")

const ANSWER = comptime 42
const PI = comptime 3.14159

def main() =
    print(f"ANSWER={ANSWER}")
    print(f"PI={PI}")
