// ==== Generic Functions ====
def identity<T>(x: T) -> T = x

def first<T>(a: T, b: T) -> T = a

def pair<T, U>(a: T, b: U) -> (T, U) =
    (a, b)

def main() =
    print(f"id(42) = {identity(42)}")
    print(f"first(a,b) = {first(1, 2)}")
    let p = pair(1, "hello")
    print(f"pair = {p}")
