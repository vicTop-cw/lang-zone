// ✅ __unapply__ 提取器模式 — 结构体解构 match
// 出处：SYNTAX/06a-struct.md
struct Point =
    x: int
    y: int
    magic __unapply__(self) -> (int, int) = (self.x, self.y)

def main() =
    let p = Point(x: 3, y: 4)
    match p:
        case Point(px, py) => print(px + py)
