// 规范目标特性（当前解析器未实现）
// 出处：SYNTAX/12-操作符.md §1.19 — ~ 后缀命名参数糖（重排形参）
// 预期语法：以命名参数传入，顺序按形参名匹配，不依赖书写顺序
def name(a: int, b: int) -> int = a + b

def main() =
    a = 1
    b = 2
    print(name(b~, a~))    // name(b = b, a = a) → a=1, b=2 → 结果 3
