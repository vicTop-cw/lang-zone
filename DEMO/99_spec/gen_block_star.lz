// ✅ 已实现 — 生成器构建块 *:
// 出处：SYNTAX/11-构建块.md / SYNTAX/14-生成器.md
def build_params() =
    let params = *:
        yield ("a", 1)
        yield ("b", 2)
    params

def main() =
    print(build_params())
