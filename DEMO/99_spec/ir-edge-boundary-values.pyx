# Generated by lzcyc (IR backend)
# cython: language_level=3
# cython: boundscheck=False

import cython

class _Moved:
    def __getattr__(self, name): raise RuntimeError('access to moved value')
    def __bool__(self): raise RuntimeError('use of moved value')
    def __repr__(self): return '<_Moved>'
_MOVED = _Moved()
def _MovedCheck(v): raise RuntimeError('use of moved value')

__name__ = "main"
__file__ = "main.lz"
__all__ = ["numeric_boundary", "string_boundary", "collection_boundary", "boolean_edge", "nested_expressions", "match_edge", "control_flow_edge", "main", "__name__", "__file__", "__package__", "__path__", "__doc__", "__is_macro__"]

cdef void numeric_boundary():
    big = 9223372036854775807
    zero = 0
    one = 1
    pi = 3.141592653589793
    zero_f64 = 0
    pow_zero = 2 ** 0
    shift_max = 1 << 63
    print(pow_zero, shift_max, big)

cdef void string_boundary():
    empty = ""
    concat = "Hello, " + "World!"
    repeated = "a" in "abc"
    not_found = "z" in "abc"
    print(concat, repeated)

cdef void collection_boundary():
    empty_list = []
    single_list = [42]
    in_empty = 1 in empty_list
    in_single = 42 in single_list
    first = single_list[0]
    print(first, in_empty, in_single)

cdef void boolean_edge():
    t = True
    f = False
    chain1 = 1 < 2 < 3 and 3 < 4
    not_cmp = not True == False
    not_not = not not True
    print(chain1, not_cmp)

cdef void nested_expressions():
    a = 1
    b = 2
    c = 3
    deep1 = ((a + b) * (c - a)) / (b + 1)
    deep2 = a + b + c + a + b + c
    print(deep1, deep2)

cdef Py_ssize_t match_edge(Py_ssize_t x):
    # match x
    if __scrutinee__ == 0:    
        0
    elif __scrutinee__ == 1:    
        1
    _ = __scrutinee__
    elif True  # bind _:    
        x * 2

cdef Py_ssize_t control_flow_edge(Py_ssize_t n):
    ((begin
    # Return { value: Some(Expr { kind: Lit(Int(0)), ty: Int, span: Span { start: 0, end: 0, line: 0, col: 0, file: None } }) }

) if n <= 0 else None)
    result = 0
    for i in Range(start = 1, end = n):    
        ((begin
    # Continue

) if i % 2 == 0 else None)
        result = result + i
    return result

cdef void main():
    numeric_boundary()
    string_boundary()
    collection_boundary()
    boolean_edge()
    nested_expressions()
    print(match_edge(5))
    print(control_flow_edge(10))
    print("boundary tests done")

str __name__ = "main"
str __file__ = "DEMO/99_spec/ir-edge-boundary-values.lz"
str __package__ = "99_spec"
str __path__ = "DEMO/99_spec"
str __doc__ = ""
bint __is_macro__ = False
