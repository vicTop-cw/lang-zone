// 规范目标特性（当前解析器未实现）
// 出处：SYNTAX/12-操作符.md §1.19 — ~ 后缀命名参数糖（混合传参）
// 预期语法：位置参数 + 命名参数糖混合
def name(a: int, b: int) -> int = a + b

def main() =
    b = 2
    print(name(1, b~))     // 位置 a=1, 命名 b=2 → 结果 3
