// 规范目标特性（codegen 已支持占位偏应用；本文件为规范目标归档）
// 出处：SYNTAX/00-词法基础.md §4.3.1 — 占位偏应用（单洞）
// 预期语法：单洞脱糖为单参数闭包
def add(a: int, b: int) -> int = a + b

def main() =
    let add10 = add(_, 10)
    print(add10(5))        // 15
