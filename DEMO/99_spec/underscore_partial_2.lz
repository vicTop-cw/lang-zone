// 占位偏应用（多洞）：`f(_, _, _)` 多洞按顺序成为闭包参数 __1, __2
// 出处：SYNTAX/00-词法基础.md §4.3.1 — 占位偏应用（多洞）
// 预期语法：多洞按顺序成为闭包参数 __1, __2
def sum3(a: int, b: int, c: int, d: int) -> int = a + b + c + d

def main() =
    let f = sum3(_, 2, 3, _)
    print(f(1, 4))         // sum3(1, 2, 3, 4) = 10
