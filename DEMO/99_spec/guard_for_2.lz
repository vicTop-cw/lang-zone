// for 守卫 + else：`for x in xs if cond: ... else:`
// 出处：SYNTAX/05-控制流.md §3.5 — for 守卫 + else
// 预期语法：
def main() =
    for i in [1, 2, 3] if i > 1:
        print(i)
    else:
        print("all filtered")
