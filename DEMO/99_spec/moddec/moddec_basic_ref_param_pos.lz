// 修饰符装饰器语料 — ref @ param（期望：编译通过 + 产物断言）
// 规范：SYNTAX/02-变量与绑定.md §八
// 备注：`ref` 形参在「按值返回」体（`-> int = v`）下命中 lz 既有 codegen 限制
//       （生成 `fn f(v: &i64) -> i64`，rustc E0308；关键字形式同样失败，与装饰器无关）。
//       改用「只读消费（print）」体，仍验证装饰器落于形参位。
def f(@ref v: int) =
    print(v)

def main() =
    x = 7
    f(x)
