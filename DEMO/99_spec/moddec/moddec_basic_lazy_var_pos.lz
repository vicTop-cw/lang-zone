// 修饰符装饰器语料 — lazy @ var（期望：编译通过 + 产物断言）
// 规范：SYNTAX/02-变量与绑定.md §八
def load() -> int =
    print("lazy-eval")
    99

def main() =
    @lazy cfg = load()
    print("ready")
    print(cfg)
    print(cfg)
